// Wasmoon Executor
//
// This package used to keep most logic in a single file (executor.mbt).
// It has been split into smaller files for maintainability:
// - exec_instr.mbt: instruction dispatch + execution helpers
// - instantiate.mbt: module instantiation and linking helpers
