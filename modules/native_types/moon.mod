name = "Milky2018/native_types"

version = "0.12.6"

repository = "https://github.com/Milky2018/wasmoon.git"

license = "Apache-2.0"

keywords = [ "compiler", "native-code", "abi", "metadata" ]

description = "Target-independent native ABI and code metadata types"

preferred_target = "wasm-gc"

supported_targets = "all"
