These self-signed localhost credentials are public, test-only fixtures. The PFX
uses an empty password for the Windows Schannel test server. They are never
used by the CLI or production client. Regenerate both formats together before
the certificate expires in September 2036.
