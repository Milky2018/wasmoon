# WASIp1 Issue Tracker

Last updated: 2026-04-07

## Status Legend

- `TODO`: not started
- `IN_PROGRESS`: currently being fixed
- `BLOCKED`: blocked by platform/public API limitations or external dependency constraints
- `DONE`: fixed and verified locally

## Issues

| ID | Source | Problem | Status | Notes |
| --- | --- | --- | --- | --- |
| WASIP1-001 | `jit/jit_ffi/wasi.c` | High: `poll_oneoff` clock semantics are still not spec-correct in JIT (absolute clock handling and event emission coupling with `poll_result == 0`). | DONE | JIT `poll_oneoff` now keeps per-subscription clock-id semantics and emits expired clock events even when fd events are also ready. Locked by test `poll_oneoff keeps expired realtime clock when fd is ready` in `testsuite/wasi_jit_wbtest.mbt`. |
| WASIP1-002 | `wasi/context.mbt`, `wasi/functions.mbt`, `jit/jit_ffi/wasi.c` | High: rights/capability model is not enforced; fd rights are effectively “all rights”, and `fd_fdstat_set_rights` is a no-op. | DONE | Aligned to wasmtime p1 behavior: `fd_fdstat_set_rights` returns `ENOTSUP` on valid fd / `EBADF` on invalid fd; `fd_fdstat_get` derives rights from descriptor type/access mode with preopen directory base/inheriting sets in both interpreter and JIT. This matches wasmtime’s descriptor-flags/open-mode driven rights model rather than a separate mutable per-fd rights table. |
| WASIP1-003 | `wasi/functions.mbt`, `jit/jit_ffi/wasi.c` | High: errno mapping is still coarse and inconsistent between interpreter/JIT; multiple failure paths collapse to `EIO`. | DONE | Expanded `errno -> WASI errno` mapping tables in both `wasi/ffi_native.c` and `jit/jit_ffi/wasi.c` to cover additional wasmtime-relevant cases (deadlock/quota/msgsize/notsock/notrecoverable/proto/timedout/etc.), materially reducing fallback-to-`EIO` behavior and aligning interpreter/JIT mappings. Unknown host errno still intentionally falls back to `EIO`. |
| WASIP1-004 | `wasi/functions.mbt` | High: interpreter `fd_renumber` semantics are unsafe (`dup2` target and fd-table bookkeeping are not fully coherent). | DONE | Reworked to descriptor-table move semantics (no `dup2` on WASI fd numbers), require existing `to` fd (`EBADF` on unused target), and keep preopen mapping coherent. Verified by `testsuite/wasi_jit_wbtest.mbt` test `fd_renumber rejects unused target with EBADF (8)`. |
| WASIP1-005 | `wasi/functions.mbt` | Medium: interpreter `fd_fdstat_set_flags` uses hardcoded native flag constants, which is cross-platform fragile. | DONE | Interpreter now rejects `DSYNC/RSYNC/SYNC` with `EINVAL` and toggles only `APPEND/NONBLOCK` using host constants from FFI. JIT side updated to same acceptance policy and preserves existing `F_GETFL` bits. Verified by `fd_fdstat_set_flags rejects DSYNC with EINVAL (28)`. |
| WASIP1-006 | `wasi/context.mbt`, `jit/jit_ffi/wasi.c` | Medium: preopen path confinement is lexical normalization + join, lacking capability-level symlink-safe containment. | DONE | Added canonical containment checks (with existing-parent fallback for missing leaf paths) in interpreter (`wasi/ffi_native.c` + `wasi/context.mbt`) and mirrored in JIT (`jit/jit_ffi/wasi.c`). Path escapes through symlinks now return `ENOTCAPABLE` instead of opening outside preopen roots. Locked by `wasi: path_open rejects symlink escape outside preopen` and `wasi jit: path_open rejects symlink escape outside preopen`. |
| WASIP1-007 | `wasi/functions.mbt` | Medium: accepted sockets are tracked as `CharacterDevice`, causing filetype reporting mismatch for socket fds. | DONE | `sock_accept_impl` now registers accepted sockets as `SocketStream`, and `fd_fdstat_get` reports socket stream (`filetype=6`). Covered by `wasi/wasi_wbtest.mbt` synthetic socket fd assertion. |
| WASIP1-008 | `wasi/functions.mbt`, `jit/jit_ffi/wasi.c` | Medium: interpreter has weaker explicit guest-memory bounds validation than JIT (`EFAULT` parity risk, trap behavior differences). | DONE | Added explicit `check_mem_range` / `check_iovecs_mem_range` validation and `EFAULT` returns across `wasi/functions.mbt` entrypoints that consume guest pointers (args/env, clock, fd read/write/pread/pwrite/seek/tell, fdstat/filestat/prestat, path APIs, `poll_oneoff`, socket recv/send/accept). Added regression tests in `wasi/wasi_wbtest.mbt` and JIT parity tests in `testsuite/wasi_jit_wbtest.mbt`. |
| WASIP1-009 | `wasi/functions.mbt`, `jit/jit_ffi/wasi.c` | High: `fd_sync` / `fd_datasync` still treat stdio as success; wasmtime p1 expects file-only semantics (`BADF` for non-file descriptors). | DONE | Both interpreter and JIT now use file-only gating (stdio/dir/sock => `BADF`). Verified by updated tests `wasi: fd_sync and fd_datasync use file-only descriptor gating` and `wasi jit: fd_sync stdin returns EBADF (8)` / `fd_datasync stdout returns EBADF (8)`. |
| WASIP1-010 | `wasi/functions.mbt`, `jit/jit_ffi/wasi.c` | High: `fd_advise` currently behaves as broad no-op; wasmtime p1 expects file/seekable-file gating and `BADF` on directory-like descriptors. | DONE | `fd_advise` now rejects stdio/preopen/dir with `BADF` and succeeds for regular files only in both runtimes. Locked by `wasi: fd_advise is file-only`. |
| WASIP1-011 | `wasi/functions.mbt`, `jit/jit_ffi/wasi.c` | High: `fd_allocate` currently performs host allocation/truncate; wasmtime p1 expects file fd check then `NOTSUP`. | DONE | `fd_allocate` now does descriptor validation only, then returns `NOTSUP` for regular files. Verified by `wasi: fd_allocate returns ENOTSUP for files and BADF otherwise` and `wasi jit: fd_allocate regular file returns ENOTSUP (58)`. |
| WASIP1-012 | `wasi/functions.mbt`, `jit/jit_ffi/wasi.c` | Medium: `fd_filestat_set_size` / `fd_filestat_set_times` stdio and descriptor-type errno behavior diverges from wasmtime p1 gating. | DONE | `fd_filestat_set_size` is now file-only (`BADF` on stdio/dir). `fd_filestat_set_times` now rejects stdio with `BADF` and still supports non-stdio descriptors. Verified by updated wasi and jit wbtests for stdin cases. |
| WASIP1-013 | `wasi/functions.mbt`, `jit/jit_ffi/wasi.c` | High: `fd_renumber` rejects stdio with `EINVAL`; wasmtime p1 allows `from == to` (including `0 -> 0`) and uses `BADF` for missing target. | DONE | Implemented `fd_renumber(0,0)==success`, missing target => `BADF`, and non-identity stdio renumber => `BADF` in both runtimes. Verified by `wasi: fd_renumber allows identity stdio and rejects moving stdio` and jit parity tests (`stdin identity succeeds`, `stdin to missing target returns EBADF`). |
| WASIP1-014 | `wasi/functions.mbt`, `jit/jit_ffi/wasi.c` | Medium: `proc_raise` currently sends host signal; wasmtime p1 returns `NOTSUP`. | DONE | `proc_raise` now returns `ENOTSUP` in interpreter and JIT. Verified by `wasi: proc_raise returns ENOTSUP` and `wasi jit: proc_raise returns ENOTSUP (58)`. |
| WASIP1-015 | `wasi/functions.mbt`, `jit/jit_ffi/wasi.c` | Medium: Preview1 `sock_*` currently performs host socket syscalls; wasmtime p1 returns `NOTSOCK` for valid descriptors. | DONE | `sock_accept/recv/send/shutdown` now return `NOTSOCK` for valid descriptors and `BADF` for invalid ones, matching wasmtime p1 behavior model. Verified by new wasi wbtest and explicit jit errno assertions (`57`). |
| WASIP1-016 | `wasi/functions.mbt`, `jit/jit_ffi/wasi.c` | Medium: `poll_oneoff` still has subtle parity risks (clock flags width and fd subscription eligibility mismatch across interpreter/JIT). | DONE | Interpreter now reads clock flags as u16 semantics; JIT now rejects non-regular fd subscriptions (`BADF`) to match interpreter/wasmtime TODO-socket policy. Existing poll parity tests remain passing in both wbtest suites. |
| WASIP1-017 | `wasi/wasi_wbtest.mbt`, `testsuite/wasi_jit_wbtest.mbt` | Medium: local tests still encode legacy errno expectations for several p1 calls; this can hide or lock in non-wasmtime semantics. | DONE | Updated/added wasi and jit wbtests to assert new errno expectations (BADF/NOTSUP/NOTSOCK and renumber/proc_raise cases). Validation commands: `moon test wasi/wasi_wbtest.mbt`, `moon test testsuite/wasi_jit_wbtest.mbt`, `moon test` (all green). |
| WASIP1-018 | `wasi/functions.mbt`, `jit/jit_ffi/wasi.c` | Medium: `clock_time_get` / `clock_res_get` currently accept CPU-time clock IDs (2/3), but wasmtime p1 returns `BADF` for unsupported CPU clocks. | DONE | Interpreter/JIT now return `BADF` for clock IDs 2/3 while keeping existing handling for 0/1 and unknown IDs. Added explicit wbtests in `wasi/wasi_wbtest.mbt` and `testsuite/wasi_jit_wbtest.mbt`. Validation: `moon check`, `moon test wasi/wasi_wbtest.mbt`, `moon test testsuite/wasi_jit_wbtest.mbt`, `moon test`. |
| WASIP1-019 | `wasi/functions.mbt`, `jit/jit_ffi/wasi.c` | High: `fd_close` currently rejects stdio with `BADF`, but wasmtime p1 closes stdio descriptors when present. | DONE | Interpreter/JIT now allow closing stdio once and return `BADF` on repeated close. Closed stdio descriptors are treated as invalid in subsequent operations (`fd_read`, `poll_oneoff`, etc.). |
| WASIP1-020 | `wasi/functions.mbt`, `jit/jit_ffi/wasi.c` | Medium: `fd_prestat_dir_name` returns `BADF` for non-preopen fds, while wasmtime p1 returns `NOTDIR`. | DONE | `fd_prestat_dir_name` now returns `NOTDIR` for valid non-preopen descriptors and `BADF` only for invalid/closed descriptors in both runtimes. |
| WASIP1-021 | `wasi/functions.mbt`, `jit/jit_ffi/wasi.c` | High: `path_open` ignores `dirflags` and infers descriptor type from `oflags`, diverging from wasmtime host-open result typing and lookupflags handling. | DONE | Added `dirflags` validation/handling (SYMLINK_FOLLOW bit), `fdflags` sync-bit `NOTSUP` behavior, and host-result descriptor typing via `fstat` instead of `oflags` inference in interpreter/JIT. |
| WASIP1-022 | `wasi/functions.mbt`, `jit/jit_ffi/wasi.c` | High: `fd_read`/`fd_write` lack strict descriptor-kind gating; directories can hit host syscalls and produce host errno instead of p1 `BADF`. | DONE | `fd_read`/`fd_write` now gate by descriptor kind (stdio/file only per call direction) and return `BADF` for directory/preopen/non-file descriptors before host syscalls. |
| WASIP1-023 | `wasi/functions.mbt`, `jit/jit_ffi/wasi.c` | Medium: `fd_seek`/`fd_tell` currently call host seek on non-file descriptors; wasmtime uses `get_seekable` (stdio => `SPipe`, others => `BADF`). | DONE | `fd_seek`/`fd_tell` now enforce seekable regular-file gating: open stdio => `SPipe`, closed stdio => `BADF`, directory/preopen/non-file => `BADF`. |
| WASIP1-024 | `wasi/functions.mbt`, `jit/jit_ffi/wasi.c`, `wasi/ffi_native.c` | High: `fd_readdir` semantics diverge (head entries `.`/`..`, cookie progression, inode/cookie mapping). | DONE | Reworked interpreter/JIT `fd_readdir` to include synthetic `.`/`..` entries, cookie stream starting at those heads, and partial dirent-header writes when buffer is truncated (wasmtime-compatible behavior). |
| WASIP1-025 | `wasi/functions.mbt`, `jit/jit_ffi/wasi.c` | Medium: stdio filetype/filestat modeling is simplified (always character device, `nlink=1`), but wasmtime maps non-tty stdio to `Unknown` and uses `nlink=0`. | DONE | Added tty-aware stdio filetype reporting (`isatty => character_device`, otherwise `unknown`) and changed stdio `fd_filestat_get` `nlink` to `0` in interpreter/JIT. |
| WASIP1-026 | `wasi/functions.mbt`, `jit/jit_ffi/wasi.c` | Low: `clock_res_get` currently returns fixed `1ms`, while wasmtime p1 uses host realtime/monotonic clock resolution values. | DONE | `clock_res_get` now queries host realtime/monotonic resolution (`clock_getres` / platform fallback) instead of returning a fixed value, while preserving `BADF`/`EINVAL` paths. |
| WASIP1-027 | `wasi/functions.mbt`, `jit/jit_ffi/wasi.c`, `wasi/wasi_wbtest.mbt`, `testsuite/wasi_jit_wbtest.mbt` | Medium: vectored I/O parity gap remained; JIT still consumed all iovecs for `fd_read/write/pread/pwrite` while wasmtime p1 consumes only the first non-empty iovec/ciovec. | DONE | Interpreter/JIT now both process only the first non-empty iovec/ciovec for `fd_read`, `fd_write`, `fd_pread`, `fd_pwrite`; added wbtests for interpreter semantics and JIT-vs-interpreter parity. |
| WASIP1-028 | `wasi/functions.mbt`, `jit/jit_ffi/wasi.c`, `wasi/wasi_wbtest.mbt`, `testsuite/wasi_jit_wbtest.mbt` | Medium: `path_open` base-fd classification mismatch: using a regular-file fd as `dirfd` returned `BADF` instead of wasmtime p1 `NOTDIR`. | DONE | `path_open` now returns `NOTDIR` for regular-file `dirfd` while keeping `BADF` for stdio/invalid descriptors in interpreter and JIT. Added wbtests for both runtimes via `path_open` on non-directory base fd. |
| WASIP1-029 | `wasi/functions.mbt`, `jit/jit_ffi/wasi.c`, `wasi/wasi_wbtest.mbt`, `testsuite/wasi_jit_wbtest.mbt` | Medium: `fd_readdir` on a non-directory descriptor returned `NOTDIR`/host-dependent errno instead of wasmtime p1 `BADF`. | DONE | Tightened descriptor-kind gating so `fd_readdir` now treats non-directory descriptors as `BADF` in interpreter and JIT; added wbtests to lock `EBADF(8)` parity on regular-file fd input. |
| WASIP1-030 | `wasi/functions.mbt`, `jit/jit_ffi/wasi.c`, `wasi/wasi_wbtest.mbt`, `testsuite/wasi_jit_wbtest.mbt` | Medium: `poll_oneoff` lacked wasmtime p1 single-relative-clock fast path (`nbytes=1`, relative CPU clock acceptance). | DONE | Added single-subscription relative clock fast path in interpreter/JIT: sleep directly and emit one clock event with `nbytes=1`, matching wasmtime behavior (including relative CPU-clock id acceptance). Added wbtests for interpreter and JIT parity. |
| WASIP1-031 | `wasi/functions.mbt`, `jit/jit_ffi/wasi.c`, `wasi/wasi_wbtest.mbt`, `testsuite/wasi_jit_wbtest.mbt` | Medium: `path_open` rejected unknown rights bits with `EINVAL`, while wasmtime p1 derives only read/write access bits and does not hard-fail on extra rights flags. | DONE | Removed strict rights-bit rejection in interpreter/JIT `path_open`; now unknown rights bits are ignored and only read/write bits influence access mode. Added wbtests for valid-dirfd and invalid-dirfd behavior. |
| WASIP1-032 | `wasi/functions.mbt`, `jit/jit_ffi/wasi.c`, `wasi/wasi_wbtest.mbt`, `testsuite/wasi_jit_wbtest.mbt` | Medium: `path_open` hard-rejected `SYNC/DSYNC/RSYNC` fdflags with `ENOTSUP`, diverging from wasmtime p1 open-path behavior. | DONE | Removed eager `ENOTSUP` rejection for sync fdflags in interpreter/JIT `path_open`; these flags are no longer hard-failed up front. Added wbtests to lock non-rejection behavior on a real preopen file path. |
| WASIP1-033 | `wasi/functions.mbt`, `jit/jit_ffi/wasi.c`, `wasi/wasi_wbtest.mbt`, `testsuite/wasi_jit_wbtest.mbt` | Low: `path_open` validated `dirflags` strictly and returned `EINVAL` on unknown bits, while wasmtime p1 does not hard-fail unknown lookupflag bits up front. | DONE | Removed strict `dirflags` bitmask rejection in interpreter/JIT `path_open`. Added wbtests confirming unknown dirflags bits are not rejected when other inputs are valid. |
| WASIP1-034 | `wasi/functions.mbt`, `jit/jit_ffi/wasi.c`, `wasi/wasi_wbtest.mbt`, `testsuite/wasi_jit_wbtest.mbt` | Medium: `path_link` currently allows `LOOKUPFLAGS_SYMLINK_FOLLOW`, while wasmtime p1 expects `EINVAL` for this flag in preview1 path_link behavior. | DONE | Interpreter/JIT `path_link` now reject `old_flags & LOOKUPFLAGS_SYMLINK_FOLLOW` with `EINVAL` (after fd/path-base validation). Added wbtests `wasi: path_link rejects LOOKUPFLAGS_SYMLINK_FOLLOW` and `wasi jit: path_link rejects LOOKUPFLAGS_SYMLINK_FOLLOW`; both pass. |
| WASIP1-035 | `wasi/functions.mbt`, `jit/jit_ffi/wasi.c`, `wasi/wasi_wbtest.mbt`, `testsuite/wasi_jit_wbtest.mbt` | Medium: `poll_oneoff` `FD_READ` event `nbytes` is fixed at `1`; wasmtime p1 reports regular-file remaining bytes (`size - position`) and hangup when at EOF. | DONE | Interpreter/JIT `poll_oneoff` now compute `FD_READ` event `(nbytes, flags)` from file `fstat + tell`: remaining bytes for regular files, hangup when at EOF, stdio-read fallback `(1,0)`. Added wbtests `wasi: poll_oneoff fd_read reports remaining file bytes` and `wasi jit: poll_oneoff fd_read reports remaining file bytes`. |
| WASIP1-036 | `wasi/context.mbt`, `wasi/functions.mbt`, `jit/jit_ffi/wasi.c`, `wasi/wasi_wbtest.mbt`, `testsuite/wasi_jit_wbtest.mbt` | Medium: lexical path escapes (absolute path or `..` escaping preopen root) returned `ENOTCAPABLE`, but wasmtime p1 expects `EPERM` for these lexical-deny cases. | DONE | Interpreter `sanitize_guest_path/resolve_path` and JIT `sanitize_guest_path/resolve_path_with_errno` now return `EPERM` for absolute/`..` lexical escapes. Verified against local `wasmtime` probe (`path_open` exits with 63) and added wbtests for interpreter+JIT. (Canonical containment errno was finalized in WASIP1-038.) |
| WASIP1-037 | `wasi/functions.mbt`, `jit/jit_ffi/wasi.c`, `wasi/wasi_wbtest.mbt`, `testsuite/wasi_jit_wbtest.mbt` | Medium: path arguments containing embedded NUL were truncated by C-string conversion, diverging from wasmtime p1 (`EINVAL` on Unix path APIs for NUL-containing path bytes). | DONE | Added explicit NUL-byte rejection on guest path bytes before host path resolution in interpreter path readers and JIT path entrypoints, returning `EINVAL` rather than truncating. Added wbtests `wasi: path_open with embedded NUL returns EINVAL` and `wasi jit: path_open with embedded NUL returns EINVAL`. |
| WASIP1-038 | `wasi/functions.mbt`, `jit/jit_ffi/wasi.c`, `wasi/wasi_wbtest.mbt`, `testsuite/wasi_jit_wbtest.mbt` | Medium: canonical path containment escapes (via symlink traversal) and `path_symlink` absolute-target handling still diverged from wasmtime p1 errno behavior (`ENOTCAPABLE`/accepted instead of `EPERM`). | DONE | Updated interpreter/JIT containment errno mapping to return `EPERM` for post-canonicalization base-escape cases. Also made `path_symlink` reject absolute `old_path` with `EPERM`. Rebased escape wbtests to use relative symlink target (`../`) and assert `EPERM(63)` for open escape; added explicit `path_symlink` absolute-target EPERM tests for interpreter and JIT parity. |
| WASIP1-039 | `wasi/context.mbt`, `wasi/functions.mbt`, `jit/jit_ffi/wasi.c`, `wasi/wasi_wbtest.mbt` | Medium: containment checks always followed the final path component, causing entry-oriented APIs (`path_unlink_file`/`path_symlink` and peers) to reject escaped symlink entries with `EPERM` instead of operating on the directory entry like wasmtime. | DONE | Added follow-vs-nofollow-leaf resolution mode in interpreter/JIT path resolvers. Switched entry-oriented APIs (`path_unlink_file`, `path_remove_directory`, `path_create_directory`, `path_rename`, `path_symlink`, `path_readlink`, `path_link`) and flag-driven `path_open`/`path_filestat_*` paths to use nofollow-leaf containment when appropriate. This restores rerun stability and aligns with wasmtime behavior where unlinking an escaped symlink entry is allowed. |
| WASIP1-040 | `wasi/functions.mbt`, `jit/jit_ffi/wasi.c`, `wasi/wasi_wbtest.mbt`, `testsuite/wasi_jit_wbtest.mbt` | Medium: invalid UTF-8 path bytes were accepted/normalized instead of returning wasmtime-compatible `EILSEQ(25)`. | DONE | Added strict UTF-8 validation for path byte slices before path decoding in interpreter and JIT. Path APIs now return `EILSEQ` for invalid UTF-8 and keep `EINVAL` for embedded NUL. Added wbtests `wasi: path_open with invalid UTF-8 returns EILSEQ` and `wasi jit: path_open with invalid UTF-8 returns EILSEQ`. |
| WASIP1-041 | `wasi/functions.mbt`, `jit/jit_ffi/wasi.c`, `wasi/wasi_wbtest.mbt`, `testsuite/wasi_jit_wbtest.mbt` | Medium: empty path handling for resolver-based APIs diverged (`path_open` on empty path should return `ENOENT`, not resolve to base directory). | DONE | Aligned resolver wrappers (interpreter/JIT) to reject empty paths with `ENOENT` for resolver-based path operations while preserving `path_symlink` old-target empty-string behavior. Added wbtests `wasi: path_open empty path returns ENOENT` and `wasi jit: path_open empty path returns ENOENT`. |
| WASIP1-042 | `wasi/functions.mbt`, `jit/jit_ffi/wasi.c`, `wasi/wasi_wbtest.mbt`, `testsuite/wasi_jit_wbtest.mbt` | Medium: `path_open` on regular-file paths with a trailing slash diverged from wasmtime (`ENOENT` expected on macOS behavior path), because trailing-slash semantics were lost during guest-path normalization. | DONE | Added trailing-slash-aware gate in interpreter/JIT `path_open`: when the guest path ends with `/`, require target metadata to be directory (respecting follow/nofollow lookup mode), otherwise return `ENOENT`. Added wbtests `wasi: path_open trailing slash on regular file returns ENOENT` and `wasi jit: path_open trailing slash on regular file returns ENOENT`. |

## Current Work Queue

- No open item in current parity batch (`WASIP1-001` ~ `WASIP1-042` are DONE and verified locally).
