# ISS-419: Make parallel moves emergency-spill aware

## Metadata
- Type: epic
- Status: closed
- Priority: 0
- Labels: backend, machv, regalloc, jit, agent
- Assignee: codex
- Created: 2026-08-13
- Updated: 2026-08-13
- External ref: none

## Description
The AArch64 JIT can silently corrupt a stack-slot cycle because the parallel
move resolver reuses a transfer scratch register that still holds the saved
cycle value. The Lane parser reproducer then creates a self-referential object,
recursively releases it, and eventually reports a misleading stack exhaustion
trap.

Replace the multi-purpose scratch convention with an explicit parallel-move
plan that uses one dedicated transfer scratch per register bank and an optional
emergency spill area.

## Design
Keep cycle linearization target-neutral in `machv/vcode`. A legalization pass
tracks the transfer scratch lifetime, parks a live cycle value in an abstract
emergency location when a stack-to-stack move needs the scratch, and returns an
explicit resolved plan. Target frame planners own the physical emergency area,
and target emitters consume only legalized steps.

## Acceptance Criteria
- [x] Target-neutral move plans preserve parallel assignment semantics with one transfer scratch per register bank.
- [x] AArch64 assigns disjoint roles to move-transfer, stack-address, and instruction/call scratch registers.
- [x] AArch64 and x64 frames reserve emergency storage only when a move plan requires it.
- [x] The Lane parser JIT reproducer exits successfully from a cold artifact cache.
- [x] Persistent JIT artifacts are invalidated and scratch-role documentation is current.
- [x] Native checks, tests, and module-boundary audit pass.

## Relationships
- Depends on: ISS-420, ISS-421, ISS-422, ISS-423, ISS-424
- Parent: none
- Related: ISS-399
- Discovered from: none

## Notes
- 2026-08-13: Root cause confirmed by forcing a fresh codegen revision and replacing the clobbered stack-cycle transfer with two distinct temporary registers.
- 2026-08-13: ISS-425 records a strict regalloc-validation gap discovered after closure; the default native and WAST gates did not exercise `MACHV_REGALLOC_VALIDATION=1`.

## Close Notes
- 2026-08-13: Replaced scratch-candidate reuse with one target-neutral transfer scratch per bank plus explicit emergency spill/reload steps. AArch64 uses X15/V18 and x64 uses R15/XMM13; both targets own an optional verified 16-byte raw frame area. Lane exits 0 for cold JIT, persisted-cache JIT, and interpreter execution. All 2613 native tests and the module-boundary audit pass.
