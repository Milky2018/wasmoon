# ISS-071: Move subtype indirect-check slow path out of MachV instruction core

## Metadata
- Type: task
- Status: closed
- Priority: 0
- Labels: machv, instr, milkir_machv, wasmoon_jit, typecheck, runtime, architecture, migration, agent
- Assignee: unassigned
- Created: 2026-07-06
- Updated: 2026-07-06
- External ref: none

## Description
`TypeCheckSubtypeIndirect(Int, ExternalName)` encodes Wasm GC subtype-check
semantics in MachV instruction core. It combines a fast exact-match compare with
a Wasmoon runtime helper call on the slow path.

## Design
Move the existing fast-path plus helper slow-path behavior to Wasmoon-owned
lowering or a generic target-level conditional-helper sequence. The MachV core
must not expose subtype-check instruction names or Wasm type-index payloads.

If the current MachV block representation cannot express the full slow-path
sequence cleanly in one change, add a precise TODO at the temporary generic
boundary and keep the pseudo-op name/payload generic rather than Wasm-specific.

## Acceptance Criteria
- [x] `TypeCheckSubtypeIndirect(Int, ExternalName)` is removed or replaced by a generic non-Wasm mechanism.
- [x] Fast exact-match behavior is preserved.
- [x] Slow-path foreign helper call behavior and clobber metadata are preserved.
- [x] Relevant GC subtype and `call_indirect` WAST tests pass.
- [x] Public `.mbti` diffs are reviewed with `moon info --target native`.
- [x] `git diff --check` passes.
- [x] `moon check --target native --warn-list +73 --diagnostic-limit=200` passes.
- [x] `moon test --target native --warn-list +73 --diagnostic-limit=200` passes.

## Relationships
- Depends on: ISS-070
- Parent: ISS-062
- Related: ISS-065
- Discovered from: ISS-062

## Notes
- 2026-07-06: This issue must preserve the current hot exact-match path; replacing it with an unconditional helper call is not acceptable.

## Close Notes
- 2026-07-06: Replaced MachV `TypeCheckSubtypeIndirect(Int, ExternalName)` with generic `CallExternalIfI32NeImm(Int, ExternalName)`, preserving the current exact-match fast path and foreign helper slow path. Wasm-specific subtype semantics now live in `milkir_machv` lowering via the selected helper symbol, while MachV only models a generic conditional helper call. Kept a precise TODO at the generic pseudo-op boundary for future explicit CFG lowering. Verified with `moon info --target native`, `moon check --target native --warn-list +73 --diagnostic-limit=200`, `moon test --target native --warn-list +73 --diagnostic-limit=200`, `./install.sh`, and targeted WAST runs for GC subtype/cast tests plus `call_indirect`, `return_call_indirect`, `type-rec`, and `type-equivalence`.
