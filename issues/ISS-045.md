# ISS-045: Clarify Store C-heap ownership comments

## Metadata
- Type: chore
- Status: closed
- Priority: 3
- Labels: wasmoon, runtime, comments, cleanup, migration, agent
- Assignee: unassigned
- Created: 2026-07-06
- Updated: 2026-07-06
- External ref: none

## Description
`modules/wasmoon/runtime/store.mbt` initializes a `MarkSweepHeap` inside `Store::with_c_heap` with a `Keep for fallback` comment. The comment is ambiguous because it can imply an interpreter fallback path in JIT mode. The actual structure has both a MoonBit heap field and an optional C heap; the comment should describe state ownership precisely.

## Design
Reword the comment only. Prefer wording such as `used when C heap is disabled` or `kept for interpreter-owned GC operations`, depending on the surrounding code. Do not change heap initialization or GC behavior.

## Acceptance Criteria
- [x] `Store::with_c_heap` no longer contains the ambiguous `Keep for fallback` wording.
- [x] The replacement comment accurately describes why the `heap` field still exists when `c_heap` is enabled.
- [x] No behavior changes are included.
- [x] `git diff --check` passes.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-042
- Discovered from: ISS-042

## Notes
- 2026-07-06: Split from ISS-042 after discussion of interpreter/JIT ownership wording.

## Close Notes
Closed by replacing the ambiguous `Keep for fallback` comment in `Store::with_c_heap` with `Used only when c_heap is None.` This documents the existing ownership split without changing heap initialization or GC behavior.

Validation: `moon fmt`, `moon info --target native`, `git diff --check`, and `moon check --target native --warn-list +73 --diagnostic-limit=200`.
