# ISS-578: Harden native ABI boundaries and retire duplicate WASI execution

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: native, wasi, ffi, testing
- Assignee: codex
- Created: 2026-09-23
- Updated: 2026-09-23
- External ref: https://github.com/Milky2018/wasmoon/pull/520

## Description
Public native array APIs must validate their extents. The JIT retains obsolete Preview 1 guest entry points despite routing both engines through the portable host. Partial allocation failures can leave invalid preopen state. Native stubs are excluded from the existing sanitizer instrumentation.

## Design
Keep public API compatibility through checked MoonBit wrappers over private externs. Remove bypassed guest implementations and their registries/bridges while preserving context APIs. Make context initialization failure-safe. Separate native host responsibilities with explicit private interfaces, without splitting shared JIT ABI state mechanically. Extend declarative native validation rather than intercepting compiler commands.

## Acceptance Criteria
- [x] Invalid buffer extents and undersized outputs are rejected before native calls, with public API regressions.
- [x] Obsolete WASI guest implementations and bridge declarations are removed.
- [x] Partial preopen allocation failure leaves safely destructible state.
- [x] Handwritten native code has explicit warning and sanitizer coverage with a native positive control.
- [x] Strict native checks, full tests, package installation, and cross-platform CI validate the changes.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-577
- Discovered from: none

## Notes
The shared path inline helpers remain appropriate. Preserve VMContext layout and existing public context APIs.

Local progress: 2557 native tests and 167 script tests (53 platform skips) pass. All 35 declared C stubs pass strict Clang syntax compilation on macOS. The production context allocation probe exercises each allocation failure, quiet/normal initialization, reinitialization, and repeated destruction. Public APIs remain unchanged. Networking and resolver worker state now have separate translation units; shared ABI/layout retention is documented in the host package README. Cross-platform CI is pending.

Final local validation: strict MoonBit checks pass without warnings; all 2557 native tests and 167 script tests pass (53 platform skips). Isolated real package installation executes WASI successfully on both engines. The expanded sanitizer closure passes 40 tests, the allocation-failure probe passes under ASan/UBSan, and separate MoonBit/native positive controls both report ASan errors as expected. Cross-platform CI remains the outstanding acceptance gate.

CI follow-up: restore Windows socket flag definitions after source extraction; replace the stale bridge-count threshold with exact target coverage; remove trailing blank lines caught by the committed-diff gate. Linux expanded sanitizer CI passed in run 35813736786.

Run 35814873233 passed both Windows native builds, installation, HTTP, P3, core/component/misc and P1 gates. Linux strict C checking exposed an allocation variable used only on Windows/macOS; scope its declaration accordingly. Move strict C compilation ahead of expensive builds and collect every failing translation unit in one run.

Strict Windows C compilation revealed narrowing conversions, POSIX-only unused helpers, and deprecated strdup spelling. Fix their types and platform scopes without disabling diagnostics. The previous combined PowerShell check/install step masked check failure with the later successful install; separate steps now preserve failure status.

Windows MSVC strict C checks passed in run 35816406355. Clang then identified three remaining POSIX preopen helpers compiled on Windows; guard those helpers with their platform-specific callers.

## Close Notes

PR #520 merged into main on 2026-09-23 as c46cc67b. CI run 35816748478 passed at b041c9ec on Linux AMD64, macOS ARM64, Windows Clang, Windows MSVC, and Linux ASan/UBSan. Strict native C compilation, isolated package installation, runtime tests, external corpora, and sanitizer positive controls passed. This supersedes the historical pending validation notes above. Registry publication remains separate; version 0.16.0 has not been replaced.
