# ISS-022: Move cwasm and native JIT glue into wasmoon_jit

## Metadata
- Type: task
- Status: in_progress
- Priority: 0
- Labels: wasmoon_jit, cwasm, jit_ffi, native, migration, agent
- Assignee: unassigned
- Created: 2026-07-04
- Updated: 2026-07-05
- External ref: none

## Description
Move the real `.cwasm` artifact implementation, `jit_ffi` C/assembly glue, executable memory integration, runtime helper resolution, traps, GC helpers, WASI native glue, stack switching, and JIT engine integration into `modules/wasmoon_jit`.

## Design
`wasmoon_jit` is Wasmoon-specific and may depend on Wasmoon runtime concepts where necessary, but reusable compiler infrastructure must not depend on it. It consumes `machv_emit` outputs, resolves runtime helper relocations, owns executable-memory allocation, and wraps output in Wasmoon-owned `.cwasm` metadata. Do not create a generic `exec_mem` module in this migration unless a separate issue justifies it.

## Acceptance Criteria
- [x] `modules/wasmoon_jit` physically owns the real `jit_ffi` native sources and MoonBit bindings.
- [x] `.cwasm` reader/writer tests move from `modules/wasmoon/cwasm` to `modules/wasmoon_jit` or an explicit Wasmoon-owned subpackage.
- [ ] JIT engine code consumes MilkIR/MachV/machv_emit/regalloc modules through stable interfaces instead of the legacy `wasmoon/ir` and `wasmoon/vcode` pipeline.
- [ ] `machv_emit` remains free of Wasmoon runtime helper resolution and native glue.
- [ ] JIT WAST correctness gates pass or failures have explicit follow-up issues.

## Relationships
- Depends on: ISS-016, ISS-021
- Parent: ISS-014
- Related: ISS-023
- Discovered from: ISS-014

## Notes
- 2026-07-04: This is not satisfied by the current manifest-only `wasmoon_jit` boundary.
- 2026-07-04: The real `.cwasm` reader/writer moved from `modules/wasmoon/cwasm` to `modules/wasmoon_jit/cwasm`. The moved package now owns neutral artifact metadata (`FuncAddrFixup`, `CallFixup`, `GcSafepoint`) instead of importing legacy `wasmoon/vcode` or `wasmoon/vcode/emit`; `wasmoon/vcode` provides explicit adapter functions for the remaining legacy JIT pipeline. The old `modules/wasmoon/cwasm` package was deleted, and `moon check --target native --warn-list +73 --diagnostic-limit=200` plus `moon test --target native --warn-list +73 --diagnostic-limit=200` passed with 1972 tests.
- 2026-07-04: The real `jit_ffi` package, including C sources, headers, assembly stubs, native-stub configuration, and MoonBit FFI declarations, moved from `modules/wasmoon/jit/jit_ffi` to `modules/wasmoon_jit/jit_ffi`. Existing wasmoon runtime/JIT/emitter packages now import the moved package instead of retaining a duplicate native glue implementation.
- 2026-07-04: `wasmoon_jit.plan_integration_for_target` now takes an explicit `machv_emit.EmitTarget`, records it in `JitIntegrationPlan`, and calls `machv_emit.emit_function_for_target`.
- 2026-07-04: Added `wasmoon_jit.plan_milkir_integration_for_target`, which lowers MilkIR through `x64_target`/`aarch64_target`, applies `machv_regalloc.allocate_and_apply`, and emits with `machv_emit.emit_function_for_target`. This is the first Wasmoon JIT-facing pipeline entry that consumes the reusable compiler-infrastructure modules instead of requiring a prebuilt legacy VCode function. Full native gate passed with 1999 tests. Remaining work: encode MachV terminators and switch the production JIT execution path off legacy `wasmoon/ir`/`wasmoon/vcode`.
- 2026-07-04: After `machv_emit` gained basic terminator emission, the MilkIR JIT integration test now produces both X64 arithmetic bytes and a real `ret` byte from a MilkIR `Return` terminator. Full native gate passed with 2000 tests.
- 2026-07-04: Wasmoon root now exposes `plan_wasm_function_with_compiler_infra`, a Wasmoon-owned planning path that lowers a real Wasm function through `wasm_frontend`, runs MilkIR optimization, and calls `wasmoon_jit.plan_milkir_integration_for_target`. This is a callable bridge from Wasmoon module data into the reusable compiler infrastructure; executable-memory installation and replacing the legacy CLI/JIT compile loop remain to be migrated.
- 2026-07-04: `wasmoon_jit` can now wrap a reusable `JitIntegrationPlan` as a moved `.cwasm` artifact via `CwasmArtifact::from_jit_plan`, linking `machv_emit.CodeObject` metadata through `link_code_object` instead of requiring the old VCode artifact path.
- 2026-07-04: `modules/wasmoon/preflight` no longer imports legacy `wasmoon/ir`, `wasmoon/vcode/lower`, or `wasmoon/vcode/regalloc`. Its JIT preflight check now lowers real Wasm functions through `wasm_frontend`, optimizes MilkIR, and verifies the `wasmoon_jit.plan_milkir_integration_for_target` path without installing or executing machine code. Full native gate passed with 2009 tests.
- 2026-07-04: `wasmoon_jit` now has a MilkIR floating-point pipeline regression: an F64 `Fadd` function lowers through `x64_target`, allocates float registers via `machv_regalloc`, and emits real SSE2 bytes through `machv_emit`. This required replacing the integer-only `allocatable_regs()` placeholders in `x64_target` and `aarch64_target` with machine-env-backed int/float/vector register sets.
- 2026-07-04: Copy-first migrated JIT reference encoding helpers (`nullref`, funcref index/pointer tags, externref tags, and GC heap references) from `modules/wasmoon/jit` into `modules/wasmoon_jit`. The old `wasmoon/jit` file now contains compatibility wrappers only, so Wasmoon no longer owns a duplicate implementation of these encoding rules. Added `wasmoon_jit` roundtrip tests; local `wasmoon_jit`/legacy JIT gates passed with 77 tests.
- 2026-07-04: Copy-first migrated the C-managed GC heap wrapper and low-level JIT GC helper surface from `modules/wasmoon/jit` into `modules/wasmoon_jit`. The old `wasmoon/jit` package now exposes `CHeap`, `CHeapError`, and `GCSetupError` as type aliases plus compatibility wrapper functions only. Runtime, WAST, and testsuite packages import `wasmoon_jit` so alias-target methods resolve without reintroducing duplicate implementations. Local `wasmoon_jit`, legacy JIT, runtime, WAST, and testsuite gates passed with 658 tests.
- 2026-07-04: Copy-first migrated the pure JIT call profiler and tiered compilation strategy from `modules/wasmoon/jit` into `modules/wasmoon_jit`. The old package now provides type aliases plus the `get_temperature` compatibility wrapper only; no profiler/strategy implementation remains in Wasmoon. Copied the profiler and strategy whitebox tests to `wasmoon_jit`; local new/legacy JIT gates passed with 118 tests.
- 2026-07-04: Copy-first migrated DWARF/JIT debug-info builder and backtrace metadata types from `modules/wasmoon/jit` into `modules/wasmoon_jit`. The old package now exposes `DWARFBuilder`, `FunctionInfo`, and `BacktraceFrame` as type aliases only. Local `wasmoon_jit`, legacy JIT, testsuite, and CLI command gates passed with 657 tests.
- 2026-07-04: Wasmoon runtime, WAST support, testsuite helpers, and CLI run command now import and call `wasmoon_jit` directly for migrated reference encoding and GC helper APIs (`CHeap`, funcref/externref/heap/i31 encoding, `gc_setup`/`gc_teardown`, and related context helpers). The old `wasmoon/jit` compatibility wrappers remain for unmigrated callers, but these Wasmoon packages no longer route those APIs through the legacy package. Local runtime/WAST/testsuite/CLI command gates passed with 594 tests.
- 2026-07-04: Copy-first migrated native runtime primitives from `modules/wasmoon/jit/ffi_jit.mbt` into `modules/wasmoon_jit/native_runtime.mbt`: `TrapDetails`, hostcall scratch accessors, `MemoryInfo`, memory descriptor allocation/read/write helpers, `JITTable`, WASI/spectest trampoline pointer helpers, C read/write helpers, and `ExecCode`. The old `ffi_jit.mbt` now forwards those APIs to `wasmoon_jit`; Wasmoon runtime, WAST, testsuite, and CLI call sites import `wasmoon_jit` directly for the migrated surface. The remaining `JITContext` code is marked `TODO(ISS-022)` because it still generates entry trampolines through legacy `wasmoon/vcode/emit`. Full native `moon check` and `moon test` passed with 2076 tests.
- 2026-07-04: Migrated JIT debug/trap metadata (`WasmTrapFrame`, `TrapReport`, `JITFunctionDebug`, `JITDebugDB`) from legacy `modules/wasmoon/jit/jit_runtime.mbt` into `modules/wasmoon_jit/debug_metadata.mbt`. The old JIT package now exposes type aliases only, while CLI dump-on-trap code constructs and passes `wasmoon_jit` metadata directly. Full native `moon info`, `moon fmt`, `moon check`, and `moon test` passed with 2076 tests.
- 2026-07-04: Migrated the WASI Preview 1 and spectest import trampoline registry from legacy `modules/wasmoon/jit/jit_runtime.mbt` into `modules/wasmoon_jit/import_trampolines.mbt`. The old JIT package now forwards `get_import_trampoline` and `is_jit_supported_module`; WAST support, testsuite helpers, and CLI import planning call `wasmoon_jit` directly. Added `wasmoon_jit` whitebox coverage for known WASI/spectest imports and unsupported imports. Full native `moon info`, `moon fmt`, `moon check`, and `moon test` passed with 2079 tests.
- 2026-07-04: `JITModule` loading and the legacy VCode emitter now resolve runtime-helper fixup ids through `wasmoon_jit.resolve_runtime_func_fixup_idx` instead of reading helper addresses from legacy `wasmoon/vcode/emit`. This keeps native helper resolution in `wasmoon_jit` while the old emitter remains only as an id producer until named MachV relocations replace it. Full native `moon info`, `moon fmt`, `moon check`, and `moon test` passed with 2070 tests.
- 2026-07-04: Removed the remaining main-code `wasmoon/vcode` dependency from `modules/wasmoon/jit` by making `JITModule::gc_set_func_safepoints` consume moved `wasmoon_jit/cwasm.GcSafepoint` records directly instead of converting through legacy `@vcode.GcSafepoint`. Full native `moon info`, `moon fmt`, `moon check`, and `moon test` passed with 2070 tests.
- 2026-07-04: Added `wasmoon_jit` coverage showing F64 `Fneg` and `Fabs` can lower from MilkIR through target lowering, `machv_regalloc`, and the expanded `machv_emit` scalar float encoder to real X64 bytes. This removes another unsupported opcode from the reusable JIT-facing pipeline. Full native `moon info`, `moon fmt`, `moon check`, and `moon test` passed with 2071 tests.
- 2026-07-04: Added `wasmoon_jit` coverage showing I64 signed division can lower from MilkIR through x64 target legalization, `machv_regalloc`, and `machv_emit` to real X64 `IDIV` bytes. The production CLI/JIT execution path still remains on legacy VCode until the new path emits complete `.cwasm` metadata and executable installation glue.
- 2026-07-04: Added `wasmoon_jit` coverage showing I64 bitwise not can lower from MilkIR `Bnot` through target lowering, `machv_regalloc`, and `machv_emit` to real X64 `NOT` bytes.
- 2026-07-04: Added `wasmoon_jit` coverage showing I64 signed comparison can lower from MilkIR `Icmp(Slt)` through target lowering, `machv_regalloc`, and `machv_emit` to real X64 `SETL`/`MOVZX` boolean materialization bytes.
- 2026-07-04: Added `wasmoon_jit` coverage showing I64 shift-left can lower from MilkIR `Ishl` through x64 target RCX legalization, `machv_regalloc`, and `machv_emit` to real X64 CL-count shift bytes.
- 2026-07-04: Added `wasmoon_jit` coverage showing I64 signed remainder can lower from MilkIR `Srem` through x64 target RAX/RDX legalization, `machv_regalloc`, and `machv_emit` to real X64 `IDIV` remainder bytes.
- 2026-07-04: Added `wasmoon_jit` coverage showing I64 `Ctz` can lower through the reusable AArch64 target, `machv_regalloc`, and `machv_emit` to real `RBIT + CLZ` bytes.
- 2026-07-04: Added `wasmoon_jit` coverage showing I64 `Umulh` can lower through the reusable AArch64 target, `machv_regalloc`, and `machv_emit` to real `UMULH` bytes.
- 2026-07-04: Added `wasmoon_jit` coverage showing I64 `Smulh` can lower through x64 target RAX/RDX legalization, `machv_regalloc`, and `machv_emit` to real X64 one-operand `IMUL` bytes.
- 2026-07-04: Added `wasmoon_jit` coverage showing I64 `Rotr` can lower through x64 target RCX legalization, `machv_regalloc`, and `machv_emit` to real X64 CL-count `ROR` bytes.
- 2026-07-04: Added `wasmoon_jit` coverage showing I64 `Rotr` can lower through the reusable AArch64 target, `machv_regalloc`, and `machv_emit` to real AArch64 `RORV` bytes.
- 2026-07-04: Added `wasmoon_jit` coverage showing I64 `Rotl` can lower through the reusable AArch64 target's explicit count transform, `machv_regalloc`, and `machv_emit` to real AArch64 `RORV` bytes.
- 2026-07-04: Added `wasmoon_jit` coverage showing scalar `Bitcast` can lower through both reusable x64 and AArch64 target pipelines, `machv_regalloc`, and `machv_emit` to real GPR/FPR bit-transfer bytes.
- 2026-07-04: Added `wasmoon_jit` coverage showing explicit-width sign-extension ops lower through both reusable x64 and AArch64 pipelines, `machv_regalloc`, and `machv_emit` to real sign-extension bytes.
- 2026-07-04: Added `wasmoon_jit` coverage showing scalar `Fpromote`/`Fdemote` lower through reusable x64 and AArch64 pipelines, `machv_regalloc`, and `machv_emit` to real float-width conversion bytes.
- 2026-07-04: Added `wasmoon_jit` coverage showing scalar `Fceil` and `Fnearest` lower through reusable x64 and AArch64 pipelines, `machv_regalloc`, and `machv_emit` to real scalar float rounding bytes. `Ffloor` and `Ftrunc` share the same migrated emitter path and have byte-level `machv_emit` coverage.
- 2026-07-04: Added `wasmoon_jit` coverage for stack spill traffic emitted by the reusable pipeline: the test forces x64 allocation with two registers, lets `machv_regalloc` insert `Load(StackSlot)`/`Store(StackSlot)`, then verifies `machv_emit` emits real RSP-relative load/store bytes.
- 2026-07-04: Added `wasmoon_jit` coverage showing generic `Uextend` and `Ireduce` lower through reusable target pipelines, `machv_regalloc`, and `machv_emit` to real scalar integer width-conversion bytes.
- 2026-07-04: Added `wasmoon_jit` coverage showing MilkIR `CallPtr` lowers through x64 target Wasm-style indirect-call ABI constraints, allocates the fixed scratch/argument/result registers through `machv_regalloc`, and emits real X64 `call r11` bytes through `machv_emit`.
- 2026-07-04: Added `wasmoon_jit` coverage showing MilkIR `Fconst` lowers through reusable x64 and AArch64 target legalization as `IntConst + Bitcast`, then through `machv_regalloc` and `machv_emit` to real constant materialization plus bit-transfer bytes.
- 2026-07-04: Added `wasmoon_jit` coverage showing Int-result MilkIR `Select` lowers through reusable x64 and AArch64 pipelines to real `CMOVcc` and `CSEL` machine-code bytes.
- 2026-07-04: Added `wasmoon_jit` coverage showing scalar MilkIR `Fcmp` lowers through reusable x64 and AArch64 target pipelines, `machv_regalloc`, and `machv_emit` to real unordered-aware float comparison bytes (`UCOMIS*`/parity on x64 and `FCMP`/`CSET` on AArch64).
- 2026-07-04: Added `wasmoon_jit` coverage showing generic MilkIR `Load`/`Store` lower through reusable x64 and AArch64 target pipelines, `machv_regalloc`, and `machv_emit` to real integer memory bytes instead of relying on legacy Wasmoon VCode memory emission.
- 2026-07-04: Added `wasmoon_jit` coverage showing scalar floating-point MilkIR `Load`/`Store` lower through reusable x64 and AArch64 target pipelines to real `MOVSD` and `LDR/STR D` memory bytes.
- 2026-07-04: Added `wasmoon_jit` coverage showing V128 MilkIR `Load`/`Store` lower through reusable x64 and AArch64 target pipelines to real `MOVDQU` and `LDR/STR Q` memory bytes.
- 2026-07-04: Added `wasmoon_jit` coverage showing MilkIR `StackAddr` lowers through reusable target lowering, `machv_regalloc`, and `machv_emit` to real X64 `LEA` stack-address bytes while preserving MilkIR stack-slot metadata in MachV.
- 2026-07-04: Added `wasmoon_jit` coverage showing MilkIR `Popcnt` lowers through reusable x64 target legalization, `machv_regalloc`, and `machv_emit` to real arithmetic machine code instead of the unsupported `IntPopcnt` opcode.
- 2026-07-04: Added `wasmoon_jit` coverage showing F64 MilkIR `Select` lowers through reusable x64 and AArch64 target legalization, `machv_regalloc`, and `machv_emit` to real `CMOVcc`/`CSEL` machine-code bytes via integer bitcasts. Production JIT execution still remains on the legacy VCode path until executable installation and `.cwasm` metadata are fully migrated.
- 2026-07-04: Added `wasmoon_jit` coverage showing I64 `Clz` and `Ctz` lower through the reusable x64 target, `machv_regalloc`, and `machv_emit` to real arithmetic machine code without using the legacy VCode emitter's local-label bit-scan sequence.
- 2026-07-04: Moved AArch64 direct-call stub and fixed `load imm64` patch-byte generation out of legacy `wasmoon/jit/jit_runtime.mbt` and into `wasmoon_jit`. The remaining legacy JIT runtime still calls the moved helpers while production executable loading is being migrated.
- 2026-07-04: Added `wasmoon_jit` coverage showing MilkIR branch block parameters lower through reusable target lowering, `machv_regalloc` edge-copy materialization, and `machv_emit` branch fixups without falling back to legacy VCode. This closes a CFG semantic gap in the reusable pipeline; production CLI execution still remains on the legacy VCode path until executable installation and full `.cwasm` metadata are migrated.
- 2026-07-04: Removed the unused `wasmoon_jit.plan_integration` AArch64-default compatibility wrapper. JIT integration planning now requires callers to choose an explicit `machv_emit.EmitTarget`; coverage checks both X64 and AArch64 object bytes through `plan_integration_for_target`.
- 2026-07-04: `CwasmArtifact` now records explicit target/name metadata and can produce a real moved `wasmoon_jit/cwasm.PrecompiledModule` plus serialized `.cwasm` bytes. `from_jit_plan` derives the `.cwasm` target from `JitIntegrationPlan.target`, and tests round-trip the serialized output through the moved reader.
- 2026-07-04: `wasmoon_jit` now preserves `machv_emit.CodeObject.frame_size` through `LinkedCode`, `CwasmArtifact`, and serialized `.cwasm` `CompiledEntry.frame_size`. Remaining `.cwasm` metadata gaps are scoped to per-function signatures, fixups, and GC stack maps.
- 2026-07-04: Added `wasmoon_jit.plan_entry_trampoline_for_target`, which builds scalar host-to-Wasm entry trampolines through MilkIR `LoadPtr`/`StorePtr`/`CallPtr`, then lowers through the reusable target, register allocation, and `machv_emit` pipeline. This replaces the core legacy `wasmoon/vcode/emit.emit_entry_trampoline` generation strategy for scalar signatures at the reusable module boundary; production `JITContext.call_multi_return` still needs to switch to the new planner and executable-code installation path. V128 trampolines are explicitly rejected with a `TODO(ISS-022)` error until MilkIR has vector lane extract/insert operations.
- 2026-07-04: Tried wiring scalar `JITContext.call_multi_return` to `wasmoon_jit.plan_entry_trampoline_for_target`, but native JIT execution regressed because the reusable MilkIR/MachV path does not yet materialize function-entry ABI parameter registers for the trampoline's C signature (`vmctx`, `values_vec`, `func_ptr`). The production path therefore remains on legacy `wasmoon/vcode/emit.emit_entry_trampoline`, with an explicit `TODO(ISS-022)` at the call site. Before this can be considered migrated, target lowering must assign or move entry params from the platform C ABI into MachV virtual values, and MilkIR must add vector lane extract/insert operations for V128 packing.
- 2026-07-04: Added the missing reusable entry-parameter materialization in `aarch64_target`/`x64_target` plus `machv_regalloc`, and changed target `CallPtr` lowering to copy Wasm results from return registers after the call. `machv_emit` now emits ABI prologue/epilogue preservation for MachV functions with declared signature params. A production wiring attempt passed the local `wasmoon/jit` execution tests after falling FP/V128 signatures back to legacy, but the full native test gate still hung, so `JITContext.call_multi_return` remains on legacy VCode trampoline generation until the reusable trampoline passes the full WAST/testsuite gate.
- 2026-07-05: Reintroduced the production wiring through `wasmoon_jit.plan_entry_trampoline_for_target` behind an explicit capability gate. The reusable MilkIR/MachV entry trampoline is used only for the currently proven scalar register-only `i64, i64 -> i64` signature; all other signatures intentionally fall back to the legacy entry trampoline with TODO(ISS-022) coverage for stack-passed entry params, overflow Wasm call args/results, reference-root handling, FP packing, and V128 lane extraction/insertion. This avoids the previous WAST hang while keeping a real production call site for the reusable module path. Validation passed with `python3 scripts/audit_module_boundaries.py && wasmtime run --dir ./issues::issues /Users/zhengyu/.ai/skills/markdown-issue-tracker/assets/derive-tracker.wasm issues --write-index && moon info && moon fmt && moon check --target native --warn-list +73 --diagnostic-limit=200 && moon test --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Moved the reusable entry-trampoline production capability gate from legacy `modules/wasmoon/jit` into `modules/wasmoon_jit` as `can_use_reusable_entry_trampoline`. Wasmoon now asks the JIT module whether a signature can use the reusable MilkIR/MachV trampoline instead of owning the policy locally. Added `wasmoon_jit` tests for the conservative true/false cases. Local validation passed with `moon test modules/wasmoon_jit modules/wasmoon/jit --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Moved hostcall import trampoline `values_vec` slot sizing and register-overflow layout planning from legacy `modules/wasmoon/vcode/emit` into `wasmoon_jit.plan_hostcall_import_trampoline_layout`. The legacy VCode emitter now consumes the Wasmoon JIT-owned plan and only maps it to old VCode instructions while the full MachV hostcall trampoline remains blocked on complete Wasm entry ABI and V128 lane packing support. Added `wasmoon_jit` layout coverage; local validation passed with `moon fmt && moon test modules/wasmoon_jit modules/wasmoon/vcode/emit --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Moved AArch64 direct-call fixup patching out of legacy `modules/wasmoon/jit` and into `wasmoon_jit` as `encode_aarch64_bl_imm26`, `patch_aarch64_call_veneer_target`, and `patch_aarch64_direct_call`. The old JIT loader now resolves target pointers from the function table or pseudo direct-call map, then delegates BL range checks, veneer literal patching, and branch patch writes to `wasmoon_jit`. Local validation passed with `moon test modules/wasmoon_jit modules/wasmoon/jit --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Moved function-address fixup target resolution and AArch64 load-imm patching into `wasmoon_jit` as `resolve_func_addr_fixup_target` and `patch_aarch64_func_addr_fixup`. The legacy JIT loader now only supplies the function table pointer, executable pointer, and `.cwasm` fixup record while `wasmoon_jit` owns the runtime-helper resolution and patch-byte write policy.
- 2026-07-05: Moved entry-trampoline signature hashing, legacy type-code projection, and V128 slot counting into `wasmoon_jit`. The legacy `JITContext` cache now asks `wasmoon_jit` for trampoline cache keys and fallback type-code arrays instead of owning duplicate trampoline planning policy.
- 2026-07-05: Moved JIT trap-code message mapping, pending-trap clearing, WASI exit sentinel checks, and WASI exit-code normalization into `wasmoon_jit`. The legacy `JITTrap` wrapper remains only as Wasmoon-facing error compatibility while native trap policy lives with the JIT runtime glue.
- 2026-07-05: Routed hostcall import trampoline function-address fixup patching through `wasmoon_jit.resolve_func_addr_fixup_target` and `wasmoon_jit.patch_aarch64_func_addr_fixup`. Both regular `.cwasm` function fixups and generated hostcall trampoline fixups now share the Wasmoon JIT-owned resolver/patcher instead of duplicating runtime-helper resolution in legacy `jit_runtime`.
- 2026-07-05: Moved direct-call fixup target resolution and pseudo direct-call ids into `wasmoon_jit` as `resolve_call_fixup_target`, `direct_call_idx_memory_fill_mem0`, and `direct_call_idx_memory_copy_mem0`. The legacy JIT loader now supplies the function table pointer plus installed direct-call stub map and delegates positive function-table lookups, negative pseudo-index lookup, and subsequent AArch64 patching to Wasmoon JIT-owned runtime glue. Local validation passed with `moon test modules/wasmoon_jit modules/wasmoon/jit --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Moved JIT context function-table sizing into `wasmoon_jit.required_context_function_count`. The legacy loader no longer owns the sparse `.cwasm` function-index scan or signature-table max policy; it asks Wasmoon JIT runtime glue how many function slots are required before allocating `JITContext`. Local validation passed with `moon test modules/wasmoon_jit modules/wasmoon/jit --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Moved import function-pointer resolution into `wasmoon_jit.resolve_import_function_ptr`. The legacy loader now delegates external-import precedence and built-in WASI/spectest trampoline fallback to the JIT integration module, leaving only installation and legacy hostcall-trampoline materialization in `wasmoon/jit`. Local validation passed with `moon test modules/wasmoon_jit modules/wasmoon/jit --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Moved import pointer classification into `wasmoon_jit` as `ImportFunctionResolution`, `classify_import_function_ptr`, and `resolve_import_function`. Negative external-import pointers are now decoded as hostcall function-address markers by the JIT module instead of by legacy `wasmoon/jit`; the old loader only consumes `DirectImportFunction`, `HostImportFunctionAddr`, or `UnsupportedImportFunction` results while legacy hostcall trampoline code generation remains to be retired. Local validation passed with `moon test modules/wasmoon_jit modules/wasmoon/jit --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Moved legacy trap-message-to-BRK/imm16 classification into `wasmoon_jit.legacy_trap_immediate_from_message`. Both AArch64 and AMD64 legacy VCode epilogues now consume the Wasmoon JIT-owned trap policy instead of carrying duplicate local message classifiers. Local validation passed with `moon test modules/wasmoon_jit modules/wasmoon/vcode/emit --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Moved structured native trap report message formatting into `wasmoon_jit.format_trap_report_message`. Legacy `JITModule` still gathers Wasmoon-specific debug dump data, but it no longer owns the `TrapReport` display policy after building the moved `wasmoon_jit.TrapReport` record. Local validation passed with `moon test modules/wasmoon_jit modules/wasmoon/jit --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Moved native signal-name formatting into `wasmoon_jit.format_signal_name`, keeping trap-details interpretation with the JIT runtime glue instead of legacy `JITModule`. Local validation passed with `moon test modules/wasmoon_jit modules/wasmoon/jit --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Moved JIT GC root argument classification into `wasmoon_jit.value_type_may_hold_gc_ref` and `wasmoon_jit.collect_gc_root_args`. Legacy `JITModule::call_with_context` now delegates frame-root selection to the JIT/GC runtime module before publishing roots to native GC state, and the old whitebox coverage calls the moved API directly. Local validation passed with `moon test modules/wasmoon_jit modules/wasmoon/jit --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Moved trap debug-dump formatting helpers into `wasmoon_jit.format_u64_hex` and `wasmoon_jit.sanitize_debug_filename`. Legacy `JITModule` still writes the Wasmoon-specific dump file, but no longer owns duplicate hex formatting or filename sanitization policy. Local validation passed with `moon test modules/wasmoon_jit modules/wasmoon/jit --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Moved production `.cwasm` entry assembly for emitted code into `wasmoon_jit.build_cwasm_entry_from_code`, including GC safepoint derivation and stackmap blob construction. The CLI JIT compile loop no longer imports or constructs legacy `wasmoon/vcode.CompiledFunction` just to wrap machine code as `.cwasm`; it hands normalized bytes, fixups, and safepoint arrays to `wasmoon_jit`. The remaining legacy `wasmoon/vcode.CompiledFunction` compatibility layer now delegates stackmap blob construction to `wasmoon_jit` instead of carrying a duplicate encoder. Local validation passed with `moon test modules/wasmoon_jit modules/wasmoon/vcode modules/wasmoon/cmd/wasmoon/commands --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Attempted to enable a conservative production function-body path for a pure `i64, i64 -> i64` Wasm function through `wasm_frontend -> MilkIR -> MachV -> machv_emit`, then install the emitted code as `.cwasm`. The AArch64 CLI smoke `wasmoon test` for an `i64.add` WAST trapped with SIGSEGV. The production compile loop must stay on legacy VCode until `wasmoon_jit` owns and validates a correct function-body ABI bridge, not only entry trampolines and metadata wrappers.
- 2026-07-05: Split reusable target lowering between generic platform-entry ABI (`lower_function`) and Wasm function-body ABI (`lower_wasm_body_function`), and surfaced the Wasm-body lowering selector through `wasmoon_jit.lower_wasm_body_milkir_for_target`. This makes the future production function-body path request the Wasmoon JIT ABI explicitly instead of accidentally reusing C/SystemV-style entry lowering. Clarification: the earlier AArch64 CLI smoke still traps with the legacy fallback, so it is tracked as a validation blocker rather than standalone proof that reusable body lowering alone is wrong. Local validation passed with `moon test modules/aarch64_target modules/x64_target modules/wasmoon_jit --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Added `wasmoon_jit.plan_wasm_body_milkir_integration_for_target`, which plans a MilkIR Wasm function body through target-specific Wasm-body ABI lowering, `machv_regalloc`, and `machv_emit` instead of using the generic platform-entry ABI. `wasmoon.plan_wasm_function_with_compiler_infra` and `preflight.validate_jit` now call this Wasm-body-specific entry, so Wasmoon-owned reusable planning paths consume the correct ABI boundary before production executable installation is migrated. Local validation passed with `moon fmt && moon test modules/wasmoon/preflight modules/wasmoon_jit --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: `wasmoon_jit.CwasmArtifact` now preserves per-function parameter/result counts via `CwasmFunctionRecord` and `from_jit_plan_with_signature`, and serializes those counts into moved `.cwasm` `CompiledEntry` records. This removes the signature metadata gap for reusable JIT plans; remaining artifact gaps are fixups and GC stack maps. Local validation passed with `moon info && moon fmt && moon test modules/wasmoon_jit --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Corrected Wasmoon reusable planning callers to use the default `wasm_frontend.EmbeddingEnvironment` with the hidden `Ptr` VMContext parameter when requesting Wasm-body ABI planning. `preflight.validate_jit` no longer lowers a two-argument Wasm signature and then asks `wasmoon_jit` to treat it as a VMContext-bearing body signature. Local validation passed with `moon fmt && moon test modules/wasmoon/preflight modules/wasmoon modules/wasmoon/wasm_frontend --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: `wasmoon.plan_wasm_function_with_compiler_infra` now normalizes a caller-provided embedding environment so Wasm-body planning always has a hidden VMContext parameter, even if the caller passed `hidden_context_type=None`. Added regression coverage for that public API behavior. Local validation passed with `moon fmt && moon test modules/wasmoon modules/wasmoon/wasm_frontend --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Wasmoon JIT now owns the legacy runtime-helper negative fixup id namespace through ordinal-based helpers, while the old VCode emitter only maps its libcall enums to those ordinals. This moves another piece of native runtime glue policy out of legacy emission without making `wasmoon_jit` depend on `wasmoon/vcode/instr`. Local validation passed with `moon check modules/wasmoon_jit modules/wasmoon/vcode/emit --target native --warn-list +73 --diagnostic-limit=200 && moon test modules/wasmoon_jit modules/wasmoon/vcode/emit --target native --warn-list +73 --diagnostic-limit=200`.

## Close Notes
