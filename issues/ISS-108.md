# ISS-108: Stop regalloc tests and docs from cementing internal APIs

## Metadata
- Type: task
- Status: closed
- Priority: 2
- Labels: regalloc, tests, docs, api, cleanup, agent
- Assignee: unassigned
- Created: 2026-07-09
- Updated: 2026-07-09
- External ref: none

## Description
`modules/regalloc/README.mbt.md` and blackbox tests currently demonstrate or depend on implementation-level APIs such as `build_live_ranges` and direct mutable field access. This makes internal helpers look public and prevents API visibility from being tightened without noisy test failures.

## Design
Rewrite README examples around the canonical public facade. Convert tests that intentionally exercise internals to whitebox tests and import private names with MoonBit's `for "wbtest"` mechanism. Keep blackbox tests focused on stable user-facing allocation behavior.

## Acceptance Criteria
- [x] README examples use the canonical public API and pass as `moon check` code blocks.
- [x] Internal helper tests are whitebox tests instead of requiring root public visibility.
- [x] Blackbox tests cover stable behavior through the public facade.
- [x] README tests and `moon test --target native --warn-list +73 --diagnostic-limit=400` pass.

## Relationships
- Depends on: ISS-104, ISS-105, ISS-106, ISS-107
- Parent: none
- Related: none
- Discovered from: ISS-104

## Notes
- 2026-07-09: Created from regalloc API cleanliness review.
- 2026-07-09: Started after ISS-107 narrowed the root model API.

## Close Notes
- 2026-07-09: Closed by keeping `modules/regalloc/README.mbt.md` focused on the canonical root allocation facade and documenting `Milky2018/regalloc/planning` as the expert package for lower-level planning algorithms. Internal live-range and planning coverage remains in whitebox/package-local tests rather than blackbox README examples. Verification: `moon check modules/regalloc --target native --warn-list +73 --diagnostic-limit=300`; `moon test modules/regalloc --target native --warn-list +73 --diagnostic-limit=300`; `moon check --target native --warn-list +73 --diagnostic-limit=400`; `moon test --target native --warn-list +73 --diagnostic-limit=400`; `python3 scripts/audit_module_boundaries.py`; `git diff --check`.
