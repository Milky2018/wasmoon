# ISS-070: Lower exact indirect type checks without a MachV Wasm pseudo-op

## Metadata
- Type: task
- Status: closed
- Priority: 0
- Labels: machv, instr, isa_target, typecheck, calls, architecture, migration, agent
- Assignee: unassigned
- Created: 2026-07-06
- Updated: 2026-07-06
- External ref: none

## Description
`TypeCheckIndirect(Int)` is a Wasm `call_indirect` exact type-check operation
encoded as a MachV instruction. It carries Wasm type-index semantics and emits a
specific compare-and-trap sequence.

## Design
Move exact indirect type-check lowering out of MachV instruction core. The
Wasm-facing lowering path should emit generic compare and conditional-trap
instructions that preserve the current trap behavior.

Do not change subtype-enabled indirect calls in this issue; they are handled by
ISS-071.

## Acceptance Criteria
- [x] `TypeCheckIndirect(Int)` is removed from MachV instruction core.
- [x] Exact indirect type checks lower to generic compare and trap instructions.
- [x] The trap condition and trap code behavior match the existing emitter behavior.
- [x] Relevant `call_indirect` exact-check WAST tests pass.
- [x] Public `.mbti` diffs are reviewed with `moon info --target native`.
- [x] `git diff --check` passes.
- [x] `moon check --target native --warn-list +73 --diagnostic-limit=200` passes.
- [x] `moon test --target native --warn-list +73 --diagnostic-limit=200` passes.

## Relationships
- Depends on: ISS-069
- Parent: ISS-062
- Related: ISS-065
- Discovered from: ISS-062

## Notes
- 2026-07-06: This should be a mechanical move of the existing compare/trap behavior, not a new type-check algorithm.

## Close Notes
- 2026-07-06: Removed MachV `TypeCheckIndirect(Int)` and its emitter/regalloc side-effect handling. Added generic flags-only `IntCmp(Bool)` beside `FpuCmp(Bool)`, then lowered exact indirect type checks to `LoadConst(expected)`, `IntCmp(false)`, and `TrapIf(Ne, 2)`. Kept `TypeCheckSubtypeIndirect` unchanged for ISS-071. Verified with `moon info --target native`, `moon check --target native --warn-list +73 --diagnostic-limit=200`, `moon test --target native --warn-list +73 --diagnostic-limit=200`, `./install.sh`, and targeted WAST runs for `call_indirect`, `return_call_indirect`, `func_ptrs`, `table`, `elem`, `ref_func`, `type-equivalence`, and `memory64/call_indirect64`.
