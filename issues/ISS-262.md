# ISS-262: Place cold live ranges outside hot loops

## Metadata
- Type: bug
- Status: closed
- Priority: 1
- Labels: regalloc, performance, loop, aarch64, scrypt, agent
- Assignee: unassigned
- Created: 2026-07-24
- Updated: 2026-07-24
- External ref: none

## Description

The production allocator chooses the wrong values to spill in the
`pwhash_scrypt_ll` Salsa20/8 kernel. `func_42` has sixteen loop-carried state
words and several address indices that are used before and after the arithmetic
loop. Wasmoon keeps many of the cold indices in physical registers and assigns
hot state values to stack slots. The static arithmetic loop therefore contains
19 reloads and 8 spills; Wasmtime spills dormant indices outside the loop and
keeps all Salsa state in registers, producing no stack access in the arithmetic
body.

The problem is a placement and splitting defect, not insufficient loop
weighting in the abstract. The allocator must distinguish a value that is live
through a hot region but unused inside it from values used on every iteration.

## Design

Extend the target-neutral regalloc planning/allocation path so a cold
live-through segment can be split and parked outside a hot loop region. Prefer
evicting dormant address/index ranges over frequently used loop-carried state.
Use CFG and use-position information already owned by `regalloc`; do not encode
Salsa operations or AArch64 registers in the policy.

The target is not “never spill.” Address indices may live in stack slots between
the load and writeback phases. The requirement is that the repeated arithmetic
region retains its hot state in registers.

## Acceptance Criteria

- [x] A focused regalloc test models cold values spanning a loop and hot values
      used on every iteration, and locks the intended split/spill decision.
- [x] `func_42` has no allocator-generated spill or reload inside the
      `add`/`ror`/`eor` arithmetic loop.
- [x] Cold address/index values may be saved before the loop and reloaded after
      it without extending hot live ranges.
- [x] The change remains target-neutral and reserves no physical register.
- [x] Regalloc, AArch64 target, native, and WAST correctness suites pass.

## Relationships
- Depends on: none
- Parent: ISS-261
- Related: ISS-220, ISS-258, ISS-263
- Discovered from: ISS-261

## Notes
- 2026-07-24: Current `func_42` arithmetic-loop counts are 126 instructions and
  27 stack accesses for Wasmoon versus 99 instructions and zero stack accesses
  for Wasmtime.
- 2026-07-24: Implementation started with a target-neutral regalloc fix:
  loop-weighted use costs, corrected conflict comparison, and bounded
  loop-boundary splitting with cold gaps assigned to spill homes.
- 2026-07-24: The final allocation keeps hot loop-carried values resident
  through conflict-free call-free bridge blocks, parks conflicting cold
  live-through values in spill homes, and emits transitions only on the exit
  edge. `func_42` now has zero allocator spill/reload edits in instructions
  i43 through i140 and zero stack moves on the continuation backedge.

## Close Notes

Closed after the focused allocation snapshot, all 66 regalloc tests, all 202
AArch64 target tests, all 98 x64 target tests, all 1,344 native tests, and all
258 interpreter plus 258 JIT WAST files passed. A three-run local
`pwhash_scrypt_ll` sample improved from the retained 47.20-second pre-fix run
to a 10.27-second median.
