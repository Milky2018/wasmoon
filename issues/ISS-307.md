# ISS-307: Preserve hostcall suspension and trap activations

## Metadata
- Type: feature
- Status: closed
- Priority: 0
- Labels: jit, continuation, hostcall, trap, ffi, agent
- Assignee: codex
- Created: 2026-07-28
- Updated: 2026-07-28
- External ref: none

## Description
`wasmoon_jit_hostcall` currently turns every nonzero callback status into a
`siglongjmp`, destroying the active Wasm frames. Trap state is also held in
thread-local singletons whose entry frames may have already returned when a
continuation resumes.

## Design
Introduce a typed internal suspension status. Let the MoonBit callback return
fully before yielding the native fiber. Keep `sigsetjmp`, trap metadata, and
active stack bounds in the fiber activation, with TLS pointing only to the
currently running activation.

## Acceptance Criteria
- [x] Suspension preserves the instruction after the hostcall without replay.
- [x] Trap after resume reports the correct function and source location.
- [x] Nested activations do not overwrite each other's trap state.
- [x] No MoonBit callback frame remains parked on a native fiber stack.

## Relationships
- Depends on: ISS-306
- Parent: ISS-298
- Related: ISS-170
- Discovered from: ISS-298

## Notes
- 2026-07-28: Initial issue.
- 2026-07-28: Claimed after the architecture-specific fiber primitive landed.
- 2026-07-28: Replaced the singleton jump buffer with a TLS stack of
  activation-owned trap, stack-bound, exception, and GC-scope state. A parked
  activation is detached from TLS and its context before another continuation
  may run.
- 2026-07-28: Hostcall callbacks now return a typed outcome. The C bridge
  performs the fiber yield only after the MoonBit callback has returned.

## Close Notes
- Added a native-only JIT continuation entry that pins its context and value
  buffer without retaining a MoonBit entry callback frame on the fiber.
- Regression tests cover no-replay continuation, trap metadata after resume,
  nested and interleaved same-context activations, and a structured fiber guard
  fault.
- Validation: 154 `Milky2018/wasmoon_jit` tests and 521
  `Milky2018/wasmoon/testsuite` tests passed on macOS AArch64. The pinned
  async-0.3 suite remains at the same baseline result as the parent commit:
  23/24 files and 152/153 commands, with the pre-existing
  `async-calls-sync.wast` re-entry failure.
