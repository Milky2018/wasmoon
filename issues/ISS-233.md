# ISS-233: Define the Cwasm artifact and compatibility manifest

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: wasmoon-jit, artifact, cwasm, manifest, code-object, api, agent
- Assignee: codex
- Created: 2026-07-22
- Updated: 2026-07-22
- External ref: none

## Description

Cwasm v8 serializes per-function code plus lossy integer fixups and a single cancellation bit. It cannot reconstruct the verified `UnlinkedCodeObject` seam or express the strict compatibility contract selected by ISS-190.

## Design

Introduce the target-portable `Milky2018/wasmoon_jit/artifact` package and define ordinary-data records for module identity, compilation policy, the complete compatibility manifest, function identities and signatures, and every `UnlinkedCodeObject` field. Persist symbolic identities only; never persist resolved addresses, linked images, compiler IR, or executable memory.

## Acceptance Criteria
- [x] The manifest covers format, target/OS ABI/endianness/pointer width, CPU features, JIT ABI, codegen revision, source/semantic digest, and compilation policy.
- [x] Artifact code records preserve relocations, traps, sources, safepoints, stack maps, unwind, signatures, and stable function identity without resolved addresses.
- [x] Unknown targets, symbols, policy fields, and manifest fields are structurally representable as decode errors rather than fallback values.
- [x] Public artifact types remain ordinary target-portable data and do not import native FFI or installers.

## Relationships
- Depends on: none
- Parent: ISS-202
- Related: ISS-188, ISS-190, ISS-191
- Discovered from: ISS-202

## Notes
- 2026-07-22: Current `CwasmArtifact::from_jit_plan` links before serialization and discards the resolved addresses, contradicting the selected unlinked artifact seam.
- 2026-07-22: The production loader reads only import identities, compiled
  functions, target features, and code metadata from the v8 model. The final
  schema will not carry its unused memory, global, table, element, or data
  copies; source-module and semantic digests bind code to the validated module.

## Close Notes

- 2026-07-22: Added `Milky2018/wasmoon_jit/artifact` with the breaking v9
  manifest and ordinary artifact records. Closed wire enums have no unknown
  fallback; symbolic identities are preserved verbatim for later JIT ABI
  whitelist validation.
- 2026-07-22: A full-object black-box snapshot covers the manifest, import and
  function identities, signatures, symbolic external relocation, source and
  trap sites, GC/cancellation safepoint and stack map, unwind directive, code,
  entry offset, and frame size. Validation passed with 2298 native tests,
  warning-73 checks, and the reusable-module boundary audit.
