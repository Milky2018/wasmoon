# ISS-008: Add machv_regalloc adapter

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: machv_regalloc, regalloc, adapter, agent
- Assignee: unassigned
- Created: 2026-07-04
- Updated: 2026-07-04
- External ref: none

## Description
Create `modules/machv_regalloc` as the adapter between MachV functions and the pure `regalloc` module.

## Design
Project MachV functions into the abstract `regalloc` program view, run allocation, and apply decisions back to MachV by rewriting operands and inserting moves, reloads, spills, and stack-frame edits. Keep allocator policy generic, and keep MachV-specific mutation in this adapter.

## Acceptance Criteria
- [x] `machv_regalloc` can allocate a MachV function using `regalloc`.
- [x] `regalloc` remains independent of MachV concrete instruction data structures.
- [x] Existing VCode regalloc behavior is preserved for current correctness tests.
- [x] Known incomplete adapter behavior includes scoped `TODO(machv_regalloc)` comments.

## Relationships
- Depends on: ISS-006, ISS-007
- Parent: ISS-001
- Related: none
- Discovered from: none

## Notes
- 2026-07-04: This issue owns projection and rewrite mechanics, not allocator algorithm design.

## Close Notes
Closed after adding the MachV-to-regalloc projection adapter, `allocate_function`, and a first `allocate_and_apply` implementation that materializes spill slots on MachV functions. Existing VCode regalloc remains untouched, preserving current correctness behavior while the reusable adapter is introduced. Validation passed: `moon fmt`; `moon test --target native --warn-list +73 --diagnostic-limit=200` (1945 passed); `moon info`.
