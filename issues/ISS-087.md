# ISS-087: Move Wasmoon debug and GC context offsets out of machv_emit

## Metadata
- Type: task
- Status: closed
- Priority: 0
- Labels: machv_emit, wasmoon_jit, gc, debug, reusable-infra, abstraction-leak, agent
- Assignee: Codex
- Created: 2026-07-07
- Updated: 2026-07-07
- External ref: none

## Description
`machv_emit` still directly reads and writes debug-current-function and GC allocation cursor/limit offsets from `EmbeddingContextLayout`. Those are Wasmoon runtime fields, not generic emission concepts.

## Design
Move Wasmoon-specific debug/GC offset expansion into Wasmoon-owned lowering or represent the accesses as generic context-slot reads/writes before emission. The generic emitter may emit ordinary loads/stores and traps, but it must not name or interpret debug/GC runtime fields.

## Acceptance Criteria
- [x] `machv_emit` no longer references debug-current-function or GC allocation field names.
- [x] Existing debug and GC behavior is preserved through Wasmoon-owned slot mapping or pre-lowered instructions.
- [x] `machv_emit` APIs remain generic and do not require Wasmoon VMContext field schema.
- [x] GC/debug-related tests and WAST gates pass.

## Relationships
- Depends on: ISS-084
- Parent: ISS-083
- Related: ISS-079
- Discovered from: ISS-083

## Notes
- 2026-07-07: Current offenders include `modules/machv_emit/prelude.mbt`, `mem_epilogue.mbt`, `instruction.mbt`, and `instruction_amd64.mbt`.

## Close Notes

- 2026-07-07: Added generic capability slots for active function index and
  allocation cursor/limit in `EmbeddingContextLayout`, with Wasmoon JIT mapping
  those capabilities to its concrete VMContext fields. Updated `machv_emit` to
  retrieve offsets through generic slot helpers instead of direct
  debug-current-function or GC allocation field names. Verified with
  `moon info`, `moon fmt`, `moon check --target native --warn-list +73
  --diagnostic-limit=200`, `moon test --target native --warn-list +73
  --diagnostic-limit=200`, `python3 scripts/audit_module_boundaries.py`,
  `git diff --check`, and `./install.sh && python3 scripts/run_all_wast.py
  --rec` (interpreter 258/258, JIT 258/258).
