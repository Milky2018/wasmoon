# ISS-019: Move real instruction selection and ABI policy into ISA targets

## Metadata
- Type: task
- Status: in_progress
- Priority: 0
- Labels: x64_target, aarch64_target, lowering, abi, migration, agent
- Assignee: unassigned
- Created: 2026-07-04
- Updated: 2026-07-04
- External ref: none

## Description
Replace the textual-opcode target skeletons with real MilkIR-to-MachV lowering by migrating the instruction-selection, ABI, and target-policy code currently under `modules/wasmoon/vcode/lower`, `vcode/isa`, and `vcode/abi`.

## Design
Keep target-specific lowering in `x64_target` and `aarch64_target`. Shared helpers may be extracted only after duplication is real and stable; do not introduce `backend`, `isa_backend`, or `backend_common` prematurely. Target modules own ABI policy, register placement, target constraints, legalization, call lowering, and instruction selection.

## Acceptance Criteria
- [ ] `x64_target` lowers the supported MilkIR subset into real MachV instructions and constraints without textual placeholder opcodes.
- [ ] `aarch64_target` lowers the supported MilkIR subset into real MachV instructions and constraints without textual placeholder opcodes.
- [ ] ABI/calling-convention tests from old `vcode/abi`, `vcode/isa`, and `vcode/lower` are ported or tracked.
- [ ] Target modules do not depend on Wasmoon runtime/JIT packages.
- [ ] JIT/interpreter correctness gates remain green for migrated lowering.

## Relationships
- Depends on: ISS-016, ISS-018
- Parent: ISS-014
- Related: ISS-021
- Discovered from: ISS-014

## Notes
- 2026-07-04: This is not satisfied by current `Target(String)` pseudo-op lowering.
- 2026-07-04: In progress. Added structured MachV opcodes for constants, integer arithmetic/comparison, float arithmetic, select, calls, and traps. `x64_target` and `aarch64_target` now lower that supported MilkIR subset to structured MachV opcodes instead of textual `Target("*.iadd")` placeholders. Remaining work: migrate real memory/legalization/call ABI lowering from old `vcode/lower` and port old ABI/lowering tests.
- 2026-07-04: MachV terminators now preserve block arguments on conditional branches, and both `x64_target` and `aarch64_target` lower MilkIR return/jump/branch/brz/brnz/br_table/trap exits into structured MachV terminators instead of appending branch/return pseudo-instructions. Remaining work: copy-first migration of old VCode instruction selection, memory/legalization, and ABI lowering into the target modules, then remove the old Wasmoon-owned duplicates.
- 2026-07-04: Copy-first migration moved the pure division-by-constant magic-number helper from `modules/wasmoon/vcode/lower/divmagic` to `modules/machv/divmagic`; old `vcode/lower` now imports the MachV helper directly. Remaining work: move the actual numeric lowering that consumes these helpers out of Wasmoon-owned VCode lowering.
- 2026-07-04: AArch64 register-role mapping, AAPCS64 register lists, call-clobber sets, and regalloc machine-env construction now live in `modules/aarch64_target`. Old `modules/wasmoon/vcode/isa/aarch64` and `modules/wasmoon/vcode/abi` route those helpers through `aarch64_target`, leaving VMContext offsets and Wasmoon stack layout in `vcode/abi`.
- 2026-07-04: Expanded MachV structured opcodes to cover MilkIR integer arithmetic, bitwise operations, shifts/rotates, comparisons, float arithmetic/comparisons, conversions, memory placeholders, stack-address placeholders, indirect calls, call pointers, and custom ops. `x64_target` and `aarch64_target` now map every MilkIR opcode to a structured MachV opcode instead of falling back to textual `Target(String)` names.
- 2026-07-04: Migrated the first real raw pointer memory-lowering slice into MachV and the target modules. MachV `LoadMemory`, `StoreMemory`, `LoadPtr`, and `StorePtr` now carry `MemType` plus immediate offset, and both `x64_target` and `aarch64_target` fold MilkIR constant pointer offsets into those structured memory opcodes while leaving non-constant offsets as explicit TODO-backed operands. Added target tests for `load_ptr`/`store_ptr` immediate offsets. Full native gate passed with 1984 tests. Remaining work: migrate register-offset and narrow-memory forms, target legality, and call ABI lowering from old VCode lowering.

## Close Notes
