# ISS-527: Support the Microsoft C compiler and validate it in Windows CI

## Metadata
- Type: feature
- Status: closed
- Priority: 1
- Labels: windows, runtime, jit, abi, ci
- Assignee: codex
- Created: 2026-09-15
- Updated: 2026-09-15
- External ref: https://github.com/Milky2018/wasmoon/actions/runs/34943251256

## Description
Support building the complete native Windows AMD64 runtime with Microsoft cl.exe, including interpreter and JIT execution. The existing Windows build relies on Clang's SysV ABI attribute and assembler.

## Design
Keep guest calling conventions explicit at host boundaries. Compile native C with cl.exe and provide compatible assembly/build integration without silently substituting Clang for C compilation. Retain the existing Clang gate and run the full Windows acceptance suites independently for MSVC.

## Acceptance Criteria
- [x] cl.exe builds the complete native runtime and native tests.
- [x] Guest/host ABI crossings preserve arguments, results and nonvolatile registers.
- [x] Independent MSVC CI passes native tests and both engines' core, component, misc, WASIp1 and readiness suites.
- [x] Existing Clang, Linux and macOS gates pass.
- [x] Windows documentation describes prerequisites and reproducible build commands.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-525
- Discovered from: ISS-525

## Notes
- 2026-09-15: Started implementation at the user's request. CI acceptance is required before closure.
- 2026-09-15: MSVC CI 34940714463 passed all 39 native probes, including actual execution of all 125 MASM guest bridges and Microsoft AddressSanitizer. Runtime C and MASM compilation passed; generated CLI C exceeded the MSVC parser depth in the WAT instruction dispatcher. Split its 505 unchanged instruction arms into scalar/SIMD families. Local validation passed 2452 native tests and both core engines (258 WAST files and 62563 assertions each). Full MSVC acceptance remains pending.
- 2026-09-15: MSVC CI 34941888897 passed every external corpus after fixing `_M_X64` detection in bound callable generation. Native tests exposed signaling-NaN quieting in interpreter sign operations and nine test-only guest entries missing ABI bridges. Replaced scalar/SIMD sign arithmetic with bit operations, expanded NaN regression assertions, and included test entries in bridge generation and native bridge probes. Local native tests remain 2452/2452; full CI verification remains pending.

## Close Notes

- 2026-09-15: Completed Microsoft C/MASM integration with explicit SysV-to-Win64 guest bridges, portable C11 atomics, matching FFI declarations, and MSVC-compatible context/trampoline assembly. Split the WAT dispatcher to stay within the compiler's parser depth and fixed signaling-NaN sign operations and test-only ABI crossings exposed by actual execution.
- Acceptance CI [34943251256](https://github.com/Milky2018/wasmoon/actions/runs/34943251256) at `c88857e4a6f57b73030d8aff2c84f20448dfff55` passed all four jobs: Windows MSVC, Windows Clang, Linux AMD64 and macOS ARM64. Both Windows native reports contain all 2452 tests across 44 packages, including 167 JIT tests, with no missing executions.
- Each Windows compiler passed both core engines (258 files and 62563 assertions per engine), stable/async/future component suites (845/153/387 commands per engine), misc (692 assertion-bearing and 72 script-only passes), WASIp1 (116 explicit-rights and four smoke passes), and 12 readiness guest executions. The previously agreed hostcall-fuel exclusion remains unchanged.
- All 39 Windows native C/AddressSanitizer/ABI probes passed for each compiler; the ABI probe executes all 134 generated bridges (125 runtime helpers and nine test entries). Local `moon info`, `moon fmt`, `moon check --target native --warn-list +73`, `moon test --target native` (2452 passed), Python runner tests, and bridge regeneration checks passed.
- Build prerequisites and the `. ./scripts/msvc/setup.ps1` command are documented in `docs/windows.md`. No follow-up is required for this scope.
