# ISS-315: Return structured Component Async canonical traps

## Metadata
- Type: bug
- Status: closed
- Priority: 0
- Labels: component-model, async, canonical-abi, traps, conformance, agent
- Assignee: codex
- Created: 2026-07-28
- Updated: 2026-07-28
- External ref: none

## Description
Several invalid Component Async operations are detected only by later guest
assertions, producing a generic core `unreachable` trap. The official suite
requires the canonical operation itself to reject deadlock, operations on
completed future/stream endpoints, and transfers still owned by a waitable
set.

## Design
Validate these state transitions at the canonical intrinsic boundary and
raise typed `ComponentRuntimeError` values with stable spec-facing messages.
Do not normalize unrelated core traps into the expected text.

## Acceptance Criteria
- [x] `async/deadlock.wast` passes.
- [x] `async/trap-if-done.wast` passes.
- [x] `async/trap-if-transfer-in-waitable-set.wast` passes.
- [x] Trap classification is identical in interpreter and native JIT modes.

## Relationships
- Depends on: ISS-314
- Parent: ISS-298
- Related: ISS-310
- Discovered from: ISS-310

## Notes
- 2026-07-28: Split from the pinned async 0.3 conformance run at
  `d346e5a335b772909ac4c8072f56d7ed7ea49927`.

## Close Notes
- 2026-07-28: After ISS-316 made `--no-jit` select the actual interpreter,
  all listed canonical-trap tests pass there without changes to trap
  classification.
- 2026-07-28: The JIT path previously raised a guest `unreachable` before the
  canonical operation because ISS-317 rejected hostcall slots on the native
  fiber stack. With that boundary fixed, the pinned async-0.3 snapshot passes
  24/24 files and 153/153 commands in both modes with the same structured
  canonical errors. No unrelated core trap normalization was added.
