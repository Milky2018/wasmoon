# ISS-444: Store allocator hot state in a reusable dense context

## Metadata
- Type: task
- Status: in_progress
- Priority: 0
- Labels: regalloc, data-layout, reference-counting, compile-time, performance, aarch64, x64, agent
- Assignee: codex
- Created: 2026-08-28
- Updated: 2026-08-31
- External ref: none

## Description

AEGIS-128L spends 62,377 us in register allocation. Function 56 alone spends
26,269 us, including 14,709 us in bundle allocation. Its deterministic loop
performs 10,526 register probes and 603,391 occupied-segment visits but only
4,620 newly observed conflicts, 52 evictions, and 64 splits. This is sustained
hot-path traversal, not runaway queue or backtracking churn.

The retained ISS-371 change kept reference-counted `AllocationSegment` records
out of scan-only comparisons without changing decisions or visit counts. That
single representation change reduced aggregate bundle time by about 11
percent, directly demonstrating that object and reference-count traffic is a
material remaining cost.

## Design

Introduce a serial-compilation-owned allocation context that is reset for each
function and owns dense primitive arrays for hot range, segment, bundle, owner,
endpoint, mark, and allocation facts. Use stable integer IDs in probe and queue
paths. Reuse capacities for conflict sets, work queues, mark arrays, temporary
range lists, and spillset pools across functions compiled by one serial module
job.

Use selective value representation only for small immutable facts after
checking copy and ABI semantics. Do not mechanically mark mutable
owner-bearing `AllocationSegment` or `AllocationBundle` records as value types;
prefer dense index or structure-of-arrays storage for them.

Preserve the current merged traversal and intrusive occupancy index unless a
measured replacement wins in wall time. Prior binary, galloping, and more
aggressive reseek experiments reduced visits but increased total compilation
time.

## Acceptance Criteria
- [x] Register probes and allocation queues use dense IDs and primitive indexed facts in their common paths without materializing reference-counted segment or bundle records.
- [x] Scratch collections and temporary storage are cleared and reused, including across functions in one serial module compilation.
- [x] Allocation decisions, deterministic loop counters, spills, reloads, moves, and generated code remain unchanged unless an explicitly documented code-quality improvement is separately accepted.
- [x] Across at least seven cache-isolated AEGIS-128L captures, aggregate bundle allocation has a median at or below 15 ms and function 56 complete register allocation has a median at or below 15 ms.
- [ ] Function 56 remains at the 16,036-byte control, and bounded BLAKE2b and Salsa20 controls do not regress materially; no full algorithms corpus is required for the performance comparison.
- [x] Regalloc, MachV adapter, AArch64, x64, strict allocation-validation, formatting, warning-73, generated-interface, and module-boundary gates pass.

## Relationships
- Depends on: ISS-443
- Parent: ISS-371
- Related: ISS-408, ISS-410, ISS-416
- Discovered from: ISS-371

## Notes
- 2026-08-28: Wasmtime/regalloc2 is useful here as a data-layout reference: it centralizes dense vectors, cached conflict state, reusable scratch collections, and bump-backed small lists. This issue does not require copying its allocation policy.
- 2026-08-28: Work started after ISS-443. The current seven-run AEGIS median
  is 51,725 us for aggregate regalloc, 25,328 us for bundle allocation, and
  22,088 us for function 56 regalloc. The first bounded slice removes the
  queue's retained `Array[AllocationBundle]`: immutable priority and weight
  facts are captured in dense queue entries at enqueue time, while mutable
  bundle ownership remains in the allocator context.
- 2026-08-28: The queue slice is retained as a small measured win and ownership
  cleanup, not presented as the main ISS-444 result. Queue comparisons no longer
  dereference reference-counted bundles. Seven cache-isolated AEGIS runs reduce
  median bundle allocation from 25,328 to 25,214 us and aggregate regalloc from
  51,725 to 51,502 us. All loop counters and code-size vectors are unchanged.
- 2026-08-28: A direct integer cursor in the occupancy scan was rejected after
  a paired build comparison. Despite removing the one-element `Array[Int]` and
  `Int?` facade, it increased median AEGIS module compilation from 119,696 to
  125,668 us and function 56 regalloc from 22,341 to 23,742 us. The experiment
  was fully reverted rather than retaining a locally simpler but slower path.
- 2026-08-28: A temporary classification probe found that 254,556 of function
  56's 259,364 overlapping occupied-segment visits immediately repeated the
  same bundle owner. The occupancy index now maintains the number of contiguous
  runs for each allocated owner in O(1) on insertion and removal. Once a probe
  records a conflicting owner whose segments form one run, it jumps directly
  past that run. Seven cache-isolated AEGIS captures reduce occupied visits from
  603,391 to 97,020 while preserving 1,679 queue pops, 10,526 register probes,
  4,620 conflicts, 52 evictions, 64 splits, 65 second-chance attempts, every
  spill/reload/move vector, the 16,036-byte function 56, and 59,428 total bytes.
  Against the paired HEAD medians, aggregate bundle allocation falls from
  25,223 to 21,554 us (-14.5%), aggregate regalloc from 51,690 to 47,904 us
  (-7.3%), function 56 bundle allocation from 12,668 to 9,879 us (-22.0%), and
  function 56 regalloc from 22,341 to 19,385 us (-13.2%). Raw retained captures
  are in `target/aegis-iss444-owner-run.UgCzPk`; the paired control was built
  from `97d00caf`, the immediately preceding `dev` commit.
- 2026-08-28: Clobber probes no longer hash `PhysicalReg` tuples or repeatedly
  compare reference-backed `ProgramPoint` values through the block-order map.
  The clobber index is keyed directly by allocatable-register ID and stores
  cached block-order/instruction coordinates shared with the occupancy index.
  Across seven cache-isolated captures, median aggregate bundle allocation
  falls from 21,554 to 19,563 us (-9.2%), aggregate regalloc from 47,904 to
  45,936 us (-4.1%), function 56 bundle allocation from 9,879 to 9,346 us
  (-5.4%), and function 56 regalloc from 19,385 to 18,845 us (-2.8%). Function
  9 bundle allocation also falls from 3,889 to 3,309 us. Allocation decisions,
  spill/reload/move vectors, and every function's code size remain unchanged.
  Raw captures are in `target/aegis-iss444-dense-clobber.GFFW0e`.
- 2026-08-28: Four follow-up experiments were rejected and fully reverted.
  Removing `segment_ids.copy()` without eliminating the plan's alias made
  MoonBit's shared-array sort path slower; even clearing the plan slot before
  sorting raised median aggregate bundle formation from 3,226 to 3,527 us and
  function 56 regalloc from 18,845 to 20,551 us. Dense integer caches for rare
  register hints added work for the common no-hint case. A thresholded binary
  jump reduced function 56 occupied visits from 97,020 to 69,485 but increased
  bundle allocation from 9,346 to 9,672 us and complete regalloc from 18,845
  to 19,560 us. Reusing two operand scratch arrays produced no repeatable
  function-56 or total-regalloc win. These results reinforce that the next
  useful slice must remove a representation or whole repeated traversal, not
  merely replace cache-friendly linear work with helpers, maps, or searches.
- 2026-08-29: The allocator no longer materializes one reference-counted
  `AllocationBundle` per bundle or copies every initial segment-membership
  array. Bundle formation now seals membership in live-range-end order, and a
  single `AllocationContext` owns membership plus dense priority, weight,
  constraint, spill-set, and state tables addressed by integer bundle IDs.
  Split and spill bundles append to the same authoritative table. Across seven
  cache-isolated AEGIS captures, median aggregate bundle allocation falls from
  19,563 to 18,985 us, aggregate regalloc from 45,936 to 45,817 us, function 56
  bundle allocation from 9,346 to 9,055 us, and complete function 56 regalloc
  from 18,845 to 18,833 us. Formation moves from about 3,226 to 3,679 us because
  ordering is now sealed there, so the structural deletion rather than phase
  relabeling is the retained result. Every capture preserves all allocation
  counters, spill/reload/move vectors, the 16,036-byte function 56, and 59,428
  total code bytes. Raw captures are in `target/aegis-iss444-context.BhsVXW`.
  Native and strict-validation tests both pass 2,682/2,682, warning-73 and
  generated-interface checks pass, and the module-boundary audit passes.
- 2026-08-29: Serial compilation now owns an explicit reusable session from
  Wasmoon's `NativeCompiler` through each target and MachV adapter down to
  regalloc. The session retains dense bundle facts, occupancy-index storage,
  owner and conflict marks, probe and eviction sets, register-order scratch,
  and a structure-of-arrays queue heap; one-shot entry points remain thin
  adapters that create an ephemeral session. No global or thread-local cache is
  involved. A large-small-large regression verifies that stale state cannot
  leak across calls. Relative to the preceding context-only milestone, seven
  cache-isolated AEGIS captures reduce median aggregate regalloc from 45,817 to
  45,597 us and bundle allocation from 18,985 to 18,824 us. Function 56 bundle
  allocation moves from 9,055 to 8,978 us, while its full regalloc is effectively
  flat at 18,833 versus 18,880 us. Every allocation and code-size vector remains
  unchanged. Native and strict-validation tests both pass 2,683/2,683, warning
  73 and generated-interface checks pass, and the module-boundary audit passes.
  Raw retained captures are in `target/aegis-iss444-session-queue.umY5Hf`.
- 2026-08-29: Reusing the nested clobber-index arrays was rejected and reverted.
  Two independent seven-run groups raised aggregate regalloc to 47,915-48,052
  us and function 56 regalloc to 19,791-20,405 us, consistent with an
  unfavorable nested-array reference-count/COW shape. Those arrays remain
  function-local; reuse is limited to flat storage that measured neutral or
  better.
- 2026-08-29: The next bounded slice removes four sources of repeated work.
  Production bundle-plan and allocation self-checks now follow `RegallocConfig`
  instead of running twice in normal JIT mode; strict validation still executes
  them. Candidate register lookup scans the target's small fixed register list
  instead of constructing a per-function tuple-keyed map. Liveness uses a dense
  bitset when the complete block-by-value domain is bounded, avoiding maps for
  the large AEGIS functions. Operand assignment merges tie construction,
  constraint reduction, and resident-segment recording into existing passes.
  Direct dense-word union avoids per-word representation dispatch. Across
  seven cache-isolated AEGIS captures, median aggregate regalloc is 43,496 us,
  bundle allocation is 18,095 us, and function 56 complete regalloc is 17,737
  us, down from 45,597, 18,824, and 18,880 us respectively at the prior
  milestone. Aggregate live-range construction falls from 5,790 to 4,663 us
  and operand assignment from 7,444 to 7,227 us. Every allocation counter,
  spill/reload/move vector, per-function code size, the 16,036-byte function
  56, and 59,428 total bytes remain unchanged. Raw captures are in
  `target/aegis-iss444-dense-liveness-direct.QKTlii`.
- 2026-08-29: Clobber points are emitted in program order, so production no
  longer sorts every per-register point list after construction; custom block
  orders retain the fallback sort. A session-owned bounded dense physical-
  register index then replaces the separate tuple-keyed maps and repeated
  linear searches in clobber construction, bundle hint selection, and operand
  assignment. Register ids above the bounded ISA range use a non-allocating
  linear fallback. Seven cache-isolated AEGIS captures reduce median aggregate
  regalloc from 43,496 to 41,118 us, aggregate bundle allocation from 18,095 to
  17,068 us, and function 56 complete regalloc from 17,737 to 16,781 us.
  Function 56 bundle allocation is 8,259 us and aggregate operand assignment
  falls from 7,227 to 5,929 us. Allocation counters and all code vectors remain
  identical. Raw captures are in
  `target/aegis-iss444-physical-register-index.CpNM5K`.
- 2026-08-29: Production liveness now tells segment construction that ranges
  and uses already satisfy program order, removing two per-range sorts while
  preserving the conservative sorting path for synthetic and whitebox inputs.
  Segment preference counters allocate only when a preference actually
  occurs. Bundle formation also delays span materialization and fixed-register
  contract scans until a union-find root first participates in an affinity
  merge. Seven cache-isolated AEGIS captures reduce median aggregate regalloc
  from 41,118 to 39,446 us and function 56 complete regalloc from 16,781 to
  16,147 us. Aggregate segment construction is 1,627 us and bundle formation
  is 2,462 us; function 56 records 706 and 1,304 us respectively. Aggregate
  bundle allocation is effectively flat at 17,261 us, so ISS-444 remains open
  for the probe path. Allocation counters and every code vector remain
  unchanged. Raw captures are in
  `target/aegis-iss444-lazy-fixed-contracts.DlwcSw`.
- 2026-08-29: `ProgramPoint` and `ProgramRange` are now native value types.
  They are immutable coordinate facts with no identity semantics, and keeping
  them inline removes reference-count traffic from the first-chance occupancy
  probe without duplicating allocator state. Across seven cache-isolated AEGIS
  captures, median aggregate regalloc falls from 39,446 to 36,941 us,
  aggregate bundle allocation from 17,261 to 16,487 us, and function 56
  complete regalloc from 16,147 to 14,698 us. Function 56 bundle allocation is
  7,825 us and live-range construction is 1,826 us. Every allocation loop
  counter and code-size vector remains identical, including the 16,036-byte
  function 56 and 59,428 total bytes. Raw captures are in
  `target/aegis-iss444-valtype.VfI2x8`.
- 2026-08-29: `VirtualReg` and `PhysicalReg` are likewise immutable numeric
  identities rather than heap objects. Representing them as native values
  removes reference-count traffic from hints, allocation states, operand
  assignment, and live-range construction. Relative to the coordinate-value
  milestone, seven cache-isolated AEGIS captures reduce median aggregate
  regalloc from 36,941 to 35,634 us and function 56 complete regalloc from
  14,698 to 14,060 us. Aggregate bundle allocation moves from 16,487 to 16,317
  us, showing that the benefit is deliberately pipeline-wide rather than a
  substitute for the remaining occupancy-probe work. Allocation counters and
  every code-size vector remain identical. Raw captures are in
  `target/aegis-iss444-reg-valtype.5zPoA6`.
- 2026-08-29: A branch-counting probe showed that 67,260 of function 56's
  97,020 occupied-segment visits merely advanced through ordered bundle
  segments that ended before the current occupied range. After four linear
  advances, the probe now lower-bounds the remaining contiguous segment-ID
  array by cached endpoint coordinates. The threshold keeps short bundles on
  the sequential path and avoids the tree reseek regression rejected earlier.
  Across seven cache-isolated AEGIS captures, median occupied visits fall to
  53,897, aggregate regalloc from 35,634 to 35,366 us, aggregate bundle
  allocation from 16,317 to 16,138 us, and function 56 complete regalloc from
  14,060 to 13,911 us. Function 56 bundle allocation is 7,526 us. Allocation
  decisions, loop counters other than the deliberately reduced visit count,
  and every code-size vector remain identical. A threshold of two reduced
  visits further but did not improve full regalloc. Retained captures are in
  `target/aegis-iss444-bundle-lower-bound.7zvFaW`.
- 2026-08-29: Small hot control facts now stay inline. Probe policies use a
  two-word value representation, clobber intersections return an inline
  optional `ProgramPoint`, and split selection carries an explicit presence
  bit instead of repeatedly constructing payload-bearing options. Physical-
  register hints retained by queues, spill sets, and value preferences are
  allocatable-register indexes with `-1` as the internal absence sentinel.
  Three successive nine-pair comparisons reduce median aggregate regalloc by
  495, 478, and 355 us and aggregate bundle allocation by 146, 580, and 491 us
  respectively. Raw captures are in
  `target/aegis-iss444-policy-pairs.LuPIw9`,
  `target/aegis-iss444-point-pairs.0k0Iu8`, and
  `target/aegis-iss444-index-hint-pairs.NWC6CS`.
- 2026-08-29: Occupancy and clobber indexes now encode the ordered block and
  signed instruction of each program point into one order-preserving `Int64`.
  This halves the endpoint-coordinate arrays and changes hot intersection and
  range comparisons from two-field lexicographic helpers to direct integer
  comparisons. Against the integer-hint milestone, nine alternating cold-cache
  pairs reduce median aggregate regalloc from 33,559 to 32,253 us, aggregate
  bundle allocation from 14,618 to 13,182 us, function 56 regalloc from 13,259
  to 12,769 us, and function 56 bundle allocation from 7,062 to 6,501 us. The
  paired deltas are -1,263, -1,475, -527, and -615 us respectively. All nine
  captures preserve 53,897 function-56 occupied visits, every allocation and
  code vector, the 16,036-byte function 56, and 59,428 total bytes. Raw
  captures are in `target/aegis-iss444-packed-point-pairs.jumeqQ`.
- 2026-08-29: Two tempting value-representation changes were rejected. Making
  the reference-bearing allocation-state enum a value type increased median
  aggregate regalloc by 342 us and bundle allocation by 270 us. Returning a
  value queue-pop result after the hint-index conversion removed 1,679 result
  allocations in function 56 but produced no stable full-regalloc benefit
  (paired median +12 us), so its less explicit precondition was not retained.
  Raw rejected captures are in `target/aegis-iss444-state-pairs.BsPwQB` and
  `target/aegis-iss444-direct-pop-pairs.uYBTeq`.
- 2026-08-29: The retained changes pass 66/66 focused regalloc tests and both
  the normal and `MACHV_REGALLOC_VALIDATION=1` native suites at 2,685/2,685.
  Formatting, generated interfaces, warning 73, the module-boundary audit, and
  diff hygiene pass. No WAST or algorithms corpus was run; the bounded
  BLAKE2b/Salsa20 acceptance control remains deliberately unchecked.
- 2026-08-31: Three availability/probe experiments were rejected. A program-point
  segment tree reduced probes from 66,493 to 28,281 and occupied visits from
  131,048 to 75,168, but its recursive queries increased one AEGIS bundle-
  allocation capture from 12,438 to 21,621 us. A conservative 16-point bucket
  index preserved allocation decisions only after falling back whenever its
  coarse answer could affect register order; it removed just 834 probes and
  increased bundle allocation to 14,219 us. A two-stage probe reduced occupied
  visits from 131,048 to 122,525 and improved local bundle allocation by a
  paired median of 447 us, but increased register probes by 30 percent, changed
  statistics semantics, and left complete module compilation statistically
  flat at a paired median of +232 us. Its implementation and whitebox test were
  reverted because the end-to-end result did not justify the extra scan or
  maintenance surface. Raw two-stage captures remain in
  `target/aegis-iss444-two-phase-probe.Jn0QWl`. No experiment is retained, and
  no algorithms corpus was run.
- 2026-08-31: Production liveness now coalesces fragments that meet exactly at
  adjacent layout-block boundaries. Cross-block segments use dense program
  offsets for spill density and support ordinary gap splitting, so the compact
  representation does not disable eviction recovery or distort long-range
  priority. The existing allocator snapshots remain unchanged. Across seven
  cold-cache AEGIS-128L runs, median aggregate regalloc falls from 28,339 to
  22,234 us (-21.5%), function 56 regalloc from 11,812 to 6,455 us (-45.4%),
  and occupied-segment scans from 131,048 to 78,290 (-40.3%). The allocation
  result is an explicitly accepted code-quality improvement: total code falls
  from 57,396 to 57,324 bytes, spills from 350 to 342, and reloads from 1,023
  to 991; register moves increase from 83 to 85. Median module compilation
  moves from 78,223 to 77,464 us (-1.0%). Captures are in
  `target/aegis-live-range-coalesce-{baseline,current}.*`; no algorithms corpus
  was run.

## Close Notes
