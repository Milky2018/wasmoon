# ISS-529: Verify developer-mode symlinks and preserve Windows privilege errors

## Metadata
- Type: bug
- Status: closed
- Priority: 1
- Labels: windows, wasi, runtime
- Assignee: codex
- Created: 2026-09-15
- Updated: 2026-09-15
- External ref: https://github.com/Milky2018/wasmoon/actions/runs/34956755258

## Description
Verify capability-relative symlink creation with and without Windows Developer Mode under a token without the symlink privilege. Earlier assumptions that direct reparse creation always requires the privilege need runtime validation.

## Design
Keep handle-relative reparse creation. Initial CI showed creation already succeeds without the symlink privilege in Developer Mode, without entering the proposed path fallback. Remove that unnecessary fallback, verify the effective token's privilege list and use an independent raw FSCTL_SET_REPARSE_POINT control. Test renamed/replaced ancestors and concurrent parent renames through the public primitive. Preserve precise cleanup errors.

## Acceptance Criteria
- [x] Developer Mode permits creation using a token without the symlink privilege.
- [x] Without Developer Mode or privilege, creation fails with a permission error and leaves no placeholder.
- [x] Concurrent ancestor/parent replacement cannot redirect creation outside the held directory.
- [x] Existing privilege handling, relative targets, duplicates, and renamed parents retain their behavior.
- [x] Clang/MSVC CI and full native gates pass; document tested boundaries.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-525, ISS-528
- Discovered from: ISS-525

## Notes
- 2026-09-15: CI 34954943298 passed both compiler permission matrices, including four P1 guest combinations per compiler. Full P1 testing caught Windows ACCESS_DENIED for existing destinations with trailing slashes. The adapter now confirms an actual destination collision before preserving the upstream target-error precedence, while always retaining ERROR_PRIVILEGE_NOT_HELD.
- 2026-09-15: The P1 end-to-end denial test exposed an absolute-path error translation bug: a failed target-attribute lookup overwrote every CreateSymbolicLinkW error, including privilege denial. Limit the existing target-before-destination-conflict handling to actual name collisions. Added native absolute-path and both-engine WASIp1 regressions. P1 uses this absolute-path adapter; component filesystem adapters use the held-parent primitive.
- 2026-09-15: CI 34953397831 passed the enabled/disabled Developer Mode behavior test but disproved the assumption that a path fallback is needed: its locking hook was never reached. Removed the unneeded implementation; added effective-token checks and an independent OS control before final acceptance.
- 2026-09-15: cap-primitives 3.4.5 denies FILE_SHARE_DELETE on directory opens before converting handles to paths. This informed the initial fallback proposal; direct OS experiments made that fallback unnecessary.

## Close Notes

- Direct FSCTL_SET_REPARSE_POINT controls and explicit effective-token privilege enumeration demonstrated that Windows Server 2025 (10.0.26100) already permits handle-relative creation in Developer Mode without SeCreateSymbolicLinkPrivilege. Removed the proposed path fallback; the final implementation retains held-parent mutations and introduces no path-locking subsystem.
- Fixed the absolute-path adapter so missing-target lookup errors cannot hide ERROR_PRIVILEGE_NOT_HELD. Preserved upstream error precedence for existing destinations and trailing-slash destinations. Failed reparse creation now reports a placeholder-cleanup failure instead of silently discarding it.
- Added native tests for the independent OS control, token restoration, missing targets, collisions, ancestor replacement and concurrent parent renames. Added WASIp1 JIT/interpreter guests whose process token has the privilege removed; Developer Mode on/off yields success/EPERM respectively, with readlink verification and no failed-operation residue. CI restores Developer Mode configuration after the matrix.
- Acceptance CI 34956755258 at `3b37b74feb10610411a8e693871c389dede01c0a` passed Windows MSVC, Windows Clang, Linux AMD64 and macOS ARM64. Each platform/compiler passed all 2452 native tests; both Windows reports executed all 44 packages without missing tests.
- Both Windows compilers passed the privilege matrices, all core/component/misc suites, 116 explicit-rights P1 cases, four stdio smoke cases, and all four developer-mode/engine guest combinations. The two established hostcall-fuel exclusions remain unchanged. Existing native and external trailing-slash regressions pass without modifying upstream fixtures or expected results.
- Local moon info, moon fmt, moon check --target native --warn-list +73, moon test --target native (2452/2452), and Python tests (146 total, 49 Windows-only skips) passed. The new guest success path also passed locally in both engines.
