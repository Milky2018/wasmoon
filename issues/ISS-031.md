# ISS-031: Restore production-style JIT unit-test harness

## Metadata
- Type: task
- Status: closed
- Priority: 3
- Labels: tests, jit, wasi, wasmoon, wasmoon_jit
- Assignee: unassigned
- Created: 2026-07-05
- Updated: 2026-07-05
- External ref: none

## Description
The old `compare_jit_interp` and WASI JIT tests manually constructed partial JIT runtime state. After deleting legacy fallbacks, those helpers were converted to interpreter-backed compatibility shims to avoid duplicating private runtime integration logic. Unit-level JIT execution coverage should be restored through the production runtime path.

## Design
Build the test harness around the same Store/Instance and JIT setup used by the CLI/WAST path. It should install hostcall and interpcall dispatchers, support captured WASI stdio/preopens, and exercise production-loaded JIT code without local copies of runtime initialization.

## Acceptance Criteria
- [x] `compare_jit_interp` executes production-loaded JIT code for supported cases.
- [x] WASI JIT tests execute production-loaded JIT code with captured stdout/stderr, stdin callbacks, and preopens.
- [x] Unsupported signatures, bodies, or incomplete native helper paths use the production fallback path, not a test-only interpreter shortcut.
- [x] The harness does not duplicate VMContext layout or legacy setup logic.
- [x] `moon test --target native --warn-list +73 --diagnostic-limit=200` passes.

## Relationships
- Depends on: ISS-028, ISS-029, ISS-033, ISS-034, ISS-035, ISS-036, ISS-037
- Parent: ISS-026
- Related: ISS-025, ISS-030
- Discovered from: ISS-026

## Notes
- 2026-07-05: Created from the post-legacy review. Production correctness is currently covered by the WAST JIT runner; this issue restores finer-grained MoonBit unit coverage.
- 2026-07-05: `compare_jit_interp` now uses the production-loaded JIT setup for supported non-WASI cases. The WASI JIT harness migration exposed a native import-call hang and is split to ISS-033 before this issue can close.
- 2026-07-05: Unsupported-body interpcall fallback exposed a hang in `br_table_as_block_value`; this is split to ISS-034 before this issue can close.
- 2026-07-05: Native `MemoryInit/DataDrop` segment helpers execute but trap with out-of-bounds under the production testsuite harness; this is split to ISS-035 before this issue can close.
- 2026-07-05: Native memory64 `memory.grow` hangs under the production testsuite harness; this is split to ISS-036 before this issue can close.
- 2026-07-05: Native table64 `table.grow` hangs under the production testsuite harness; this is split to ISS-037 before this issue can close.
- 2026-07-05: After ISS-033, ISS-034, ISS-035, ISS-036, and ISS-037, the remaining testsuite compatibility shortcuts were removed. The MoonBit unit harness now follows the same production JIT module load, runtime state initialization, and fallback trampoline path as CLI/WAST execution.

## Close Notes
- Restored `compare_jit_interp` to production-loaded JIT execution and removed the unsupported-function interpreter shortcut.
- Restored WASI JIT tests to production-loaded JIT execution with captured stdout/stderr, stdin callbacks, preopens, normal WASI linker registration, hostcall/interpcall dispatcher installation, and store-to-JIT memory/global/table initialization.
- Verified that unsupported reusable-frontend bodies are represented by production fallback trampoline entries from `compile_module_to_jit`, matching the old main-branch JIT behavior instead of using a testsuite-only interpreter bypass.
