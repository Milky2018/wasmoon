# ISS-522: Resolve the Linux call_ref conformance failure

## Metadata
- Type: bug
- Status: in_progress
- Priority: 1
- Labels: correctness, conformance, runtime
- Assignee: codex
- Created: 2026-09-10
- Updated: 2026-09-10
- External ref: https://github.com/Milky2018/wasmoon/actions/runs/34439681495

## Description
Linux AMD64 CI at 6772d400 reports 30/31 passing assertions in the unmodified core call_ref.wast script. The interpreter passes all 31. The previous runner discarded assertion details, so reproduce with an x64 binary and retain useful failure evidence before deciding the fix.

## Acceptance Criteria
- [x] Identify the failed assertion and validate its behavior independently.
- [x] Fix any confirmed runtime defect without weakening upstream assertions.
- [ ] Native Linux AMD64 and macOS ARM64 core gates pass.

## Relationships
- Depends on: none
- Parent: ISS-477
- Related: ISS-519, ISS-520, ISS-521
- Discovered from: ISS-521

## Close Notes

- At 84801f0c, the final x64 Rosetta binary passed call_ref.wast in 100 fresh processes (31 assertions each; /tmp/iss519-x64-call-ref-stability.log). This does not identify the old Linux failure. Native CI run 34444271389 includes the runner fix that retains actual assertion failure text.

- CI 34447109701 recovered the exact failure: spec/call_ref.wast line 165 expects unreachable, but receives NativeFiberFailure("UnexpectedYield(-1)"). It is a synchronous entry unexpectedly suspending, not a numerical call_ref result mismatch. alloc_context_internal uses malloc and manually initializes fields but omits scheduling_budget. A deterministic poisoned-allocation constructor test is being added before replacing field-by-field zeroing with whole-context zero initialization.

- Deterministic allocator control: a test compiler wrapper preinitializes malloc storage in jit_context.c to 0x01. The new default-state regression failed with scheduling_budget=16843009 before the fix and passed with zero afterwards (/tmp/iss522-poison-red.log and /tmp/iss522-poison-green.log). The constructor now calloc-initializes the entire context, explicitly initializes its atomic size, and assigns only allocated resources and nonzero sentinels. A repeated fresh-context regression exercises reuse after a context with scheduling enabled.

- Pre-CI validation: all 2,450 native tests passed; 18 continuation tests plus the new default-state test passed under ASan (leak detection disabled); full misc passed all 764 executions (tmp/iss522-all-misc/summary.json). The pinned Wasmtime 668016 control and the current local JIT both passed the complete call_ref.wast script. Native Linux acceptance remains pending.
