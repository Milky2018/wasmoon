# ISS-436: Preserve table out-of-bounds trap classification in the JIT

## Metadata
- Type: bug
- Status: open
- Priority: 1
- Labels: jit, traps, table, wasm-smith, differential-testing, aarch64, x64, agent
- Assignee: unassigned
- Created: 2026-08-28
- Updated: 2026-08-28
- External ref: none

## Description

The versioned Wasm-Smith differential corpus deterministically finds one trap
classification mismatch at seed `193200201`, case 70. The interpreter reports
`out of bounds table access`, while the AArch64 JIT reports `out of bounds
memory access` with `brk_imm=1`. Both executions trap, but the structured trap
reason is observably different.

The same JIT diagnosis reproduces at `d5e692d6`, `7516a11b`, and the current
tree, so this is not a regression from ISS-433 or ISS-434. The fixed corpus
runner correctly records 99 passes and one failure; callers must preserve its
non-zero exit status rather than masking it with a later shell command.

## Design

Trace table bounds checks from Wasm frontend trap construction through target
lowering, machine trap immediates, native signal transport, and public runtime
error mapping. Assign or preserve a distinct structured trap code for table
out-of-bounds without changing guarded-memory fault handling. Keep the fix in
the shared trap representation where possible and use target-specific encoding
only at the final machine-instruction boundary.

## Acceptance Criteria
- [ ] A minimized public CLI reproduction reports table out of bounds in interpreter and JIT modes.
- [ ] AArch64 and x64 encode and transport table out-of-bounds distinctly from memory out-of-bounds.
- [ ] Existing explicit and hardware memory-fault paths continue to report memory out of bounds.
- [ ] Structured trap metadata and cached artifact compatibility are updated if the machine trap encoding changes.
- [ ] The versioned 100-case Wasm-Smith differential corpus passes 100/100.
- [ ] Strict native, complete WAST, Component Model, and module-boundary gates pass.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-193, ISS-324, ISS-431, ISS-432
- Discovered from: ISS-433

## Notes
- 2026-08-28: Reproduce with `python3 scripts/run_fixed_smith_gate.py --out <directory>` using the checked-in manifest. The implementation must reduce deterministic case 70 into a checked-in black-box regression rather than depend on the runner's temporary artifact.

## Close Notes
