# ISS-195: Implement the target-neutral MachV core

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: machv, ir, verifier, architecture, agent
- Assignee: codex
- Created: 2026-07-16
- Updated: 2026-07-16
- External ref: none

## Description

Implement the final target-neutral MachV semantic IR, including construction, printing, verification, and mandatory cleanup, without routing production compilation through it yet.

## Design

- Represent semantic machine operations, block parameters, calls, effects, traps, safepoints, and abstract values without target opcodes, physical registers, encodings, or ABI facts.
- Reject foreign ownership, malformed SSA/CFG, invalid signatures, and illegal operation contracts at construction or verification boundaries.
- Keep the public surface deep and minimal; target and compatibility concerns remain outside the module.

## Acceptance Criteria

- [x] The semantic MachV IR, builder, printer, verifier, and mandatory cleanup pipeline implement the contracts decided by ISS-181 through ISS-191.
- [x] Blackbox tests snapshot complete functions and cover ownership, SSA dominance, CFG edge arguments, types, signatures, effects, traps, calls, and safepoints.
- [x] Negative compile-time or dependency tests show that target opcodes, physical registers, encodings, and ABI-specific facts cannot enter semantic MachV.
- [x] The semantic module has no dependency on `machv_legacy`, target packages, regalloc, emitters, or Wasmoon product packages.
- [x] Production JIT behavior remains on the unchanged legacy path.
- [x] The common ISS-192 quality gates pass.

## Relationships
- Depends on: ISS-194
- Parent: ISS-180
- Related: none
- Discovered from: ISS-192

## Notes

- This is the new semantic source of truth, but production cutover belongs to ISS-196.
- 2026-07-16: Claimed after ISS-194 closed. Implementation will follow the contracts recorded by ISS-181 through ISS-191, start from black-box verifier and printer tests, and leave the production JIT on the mechanically isolated legacy backend.
- 2026-07-16: Implemented in `ac59e2e0`. The final module owns opaque function-scoped values, blocks, and instructions; exact target-neutral types and operations; typed CFG edges and block parameters; structured calls, traps, effects, source locations, GC and cancellation safepoints; deterministic verification; stable printing; and mandatory alias normalization plus effect-aware DCE.
- 2026-07-16: Construction errors remain builder-local, so invalid mutations are rejected before changing canonical storage while `Function::verify()` depends only on the current IR rather than construction history.
- 2026-07-16: Boundary audits prove that the public API contains no target opcode, physical-register, encoding, ABI, custom-extension, or legacy escape hatch, and that the module imports no legacy backend, target, allocator, emitter, or Wasmoon product package.

## Close Notes

- Closed after `moon info`, `moon fmt`, `python3 scripts/audit_module_boundaries.py`, `python3 scripts/audit_semantic_machv_api.py`, `moon check --target native --warn-list +73`, and `MACHV_REGALLOC_VALIDATION=1 moon test --target native` passed; the native suite reported 1960/1960 tests.
- The MachV package additionally passed 35/35 tests on native, wasm-gc, and JS. Representative production smoke tests passed with JIT enabled for `spec/const.wast` (376), `spec/memory64/call_indirect64.wast` (1), and `spec/simd/simd_f32x4_arith.wast` (1819), plus interpreter-only `spec/const.wast` (376).
- Production packages still have no import of `Milky2018/machv`; the unchanged JIT remains on `machv_legacy` until ISS-196 performs the semantic producer cutover.
