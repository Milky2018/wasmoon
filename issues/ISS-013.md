# ISS-013: Validate first-stage split and document remaining gaps

## Metadata
- Type: task
- Status: closed
- Priority: 2
- Labels: validation, docs, migration, agent
- Assignee: unassigned
- Created: 2026-07-04
- Updated: 2026-07-04
- External ref: none

## Description
Run the correctness gates and document remaining intentional gaps after the first-stage module split.

## Design
Use correctness-oriented gates such as `moon check`, `moon test`, focused JIT/interpreter regression tests, and relevant WAST runners. Performance gates are out of scope. Verify that incomplete compiler-infrastructure areas are marked with scoped TODO comments and that module boundaries match `CONTEXT.md`.

## Acceptance Criteria
- [x] `moon check` passes for the workspace.
- [x] `moon test` passes or each failure has a follow-up issue.
- [x] Relevant Wasm correctness runners pass or each failure has a follow-up issue.
- [x] Scoped TODO comments exist for known incomplete MilkIR, MachV, regalloc, target, and emitter areas.
- [x] `CONTEXT.md` and issue relationships match the implemented first-stage architecture.

## Relationships
- Depends on: ISS-012
- Parent: ISS-001
- Related: none
- Discovered from: none

## Notes
- 2026-07-04: TODO comments are required for known gaps but are not themselves a correctness gate.

## Close Notes

Closed after final first-stage validation. Gates passed: `moon fmt`; `moon info`; `moon check --target native --warn-list +73 --diagnostic-limit=200`; `moon test --target native --warn-list +73 --diagnostic-limit=200` (1956 passed); `./install.sh`; `python3 scripts/run_all_wast.py --rec` (Interpreter 258/258 files, 62563 passed; JIT 258/258 files, 62563 passed); `python3 scripts/run_component_wast.py --dir component-spec/values --rec` (2/2 files, 60 passed); `python3 scripts/run_component_wast.py --dir component-spec/names --rec` (1/1 files, 3 passed). Scoped TODO comments exist for MilkIR, Wasm frontend, MachV, regalloc, MachV regalloc, x64 target, AArch64 target, MachV emitter, and Wasmoon JIT. `CONTEXT.md` matches the implemented module names and ownership boundaries.
