# ISS-030: Close native frontend lowering gaps

## Metadata
- Type: task
- Status: closed
- Priority: 2
- Labels: wasm_frontend, milkir, lowering, reusable-infra, correctness
- Assignee: unassigned
- Created: 2026-07-05
- Updated: 2026-07-05
- External ref: none

## Description
The native Wasm-to-MilkIR frontend no longer falls back to legacy IR, but several operators and structured-control-flow cases remain incomplete. Unsupported cases currently rely on production interpcall fallback, which is correct for Wasmoon but not enough for a broadly reusable compiler frontend.

## Design
Audit the remaining `TODO(ISS-026)` and `UnsupportedNativeLowering` cases. Implement reusable MilkIR lowering for high-value missing operators and control-flow constructs, and keep unsupported cases explicit with capability reporting. Avoid introducing Wasmoon-specific semantics into `milkir`.

## Acceptance Criteria
- [x] Remaining structured `if`, label-stack branch, and branch-value cases are either lowered correctly or explicitly rejected with clear capability diagnostics.
- [x] Memory64 and runtime-helper ABI limitations are aligned with the reusable embedding ABI from ISS-029.
- [x] Operator coverage is expanded without reintroducing legacy IR/VCode.
- [x] WAST JIT correctness remains green.
- [x] `moon check --target native --warn-list +73 --diagnostic-limit=200` passes.

## Relationships
- Depends on: ISS-029
- Parent: ISS-026
- Related: ISS-025, ISS-028
- Discovered from: ISS-026

## Notes
- 2026-07-05: Created to make the native frontend useful as reusable infrastructure instead of only a Wasmoon fast path plus interpreter fallback.
- 2026-07-05: Started implementation pass after closing ISS-028/ISS-032. Scope is to remove or make explicit the remaining native frontend gaps without reintroducing legacy IR/VCode dependencies.
- 2026-07-05: `UnsupportedNativeLowering` now carries a capability reason. The native lowerer reports structured branch/label-stack gaps and memory64 bulk-memory helper ABI gaps explicitly, while `try_lower_native_function` keeps its Option-style probe API.
- 2026-07-05: Expanded native operator coverage for reusable memory lowering by adding `v128.load` and `v128.store` through existing MilkIR `V128` and target `MemV128` support.

## Close Notes
Closed on 2026-07-05. Native frontend unsupported cases now produce capability diagnostics for the ISS-030 structured-control and memory64 runtime-helper gaps, and operator coverage expanded with `v128.load/store` without reintroducing legacy IR/VCode. Validation: `moon check modules/wasmoon/wasm_frontend --target native --warn-list +73 --diagnostic-limit=200`; `moon test modules/wasmoon/wasm_frontend --target native --warn-list +73 --diagnostic-limit=200`; full native and JIT WAST gates are recorded in the landing commit.
