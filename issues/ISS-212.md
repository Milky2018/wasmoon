# ISS-212: Improve current MachV compile-time performance

## Metadata
- Type: bug
- Status: open
- Priority: 2
- Labels: machv, performance, compile-time, diagnostics, aarch64, amd64, agent
- Assignee: agent
- Created: 2026-07-20
- Updated: 2026-07-22
- External ref: CI run 29716402331

## Description

The current MachV pipeline still has actionable compile-time hotspots, especially large-CFG target lowering and fixed costs on small functions. The retired backend is useful historical evidence but is no longer a release or migration authority.

## Design

Use current-version phase metrics, profilers, and scaling tests to identify shared fixed costs and workload-specific complexity. Preserve the final semantic MachV, verifier, Target VCode, allocation, and code-object seams; do not reintroduce legacy code or a fixed-legacy release threshold.

## Acceptance Criteria

- [ ] Current-version diagnostics separate frontend, lowering, verification, regalloc, and emission costs.
- [ ] Large-CFG scaling has a deterministic regression test and no demonstrated superlinear target-pipeline hotspot.
- [ ] Representative AArch64 and AMD64 compile workloads have retained current-version trend reports.
- [ ] Correctness and code-quality gates remain green.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-199, ISS-200, ISS-201
- Discovered from: ISS-193

## Notes
- 2026-07-20: This issue excludes the large-CFG complexity defect tracked by ISS-211 and code-size growth tracked by ISS-213.
- 2026-07-20: After the regalloc and target-pipeline fixes, the 21-pair AArch64 corpus compile ratio is 0.922647 with a 0.931365 upper 95% bound, so the aggregate limit passes. Per-workload failures remain for `float_exprs.wast` (upper bound 1.109874), `ref.wast` (1.575283), `simd_f32x4_arith.wast` (1.142093), `matmul.wat` (1.244980), and statistically inconclusive `register_pressure.wast` (upper bound 1.114665). These are no longer caused by the closed large-CFG scalability defect.
- 2026-07-21: The two-pair call-transfer cutover report at `/private/tmp/machv-call-transfer-20260721/perf-report.json` has an aggregate compile ratio of 0.896389 with a 0.922132 upper bound, but several individual workloads remain over or inconclusive against 1.10. This preserves the existing open acceptance work; no threshold was weakened.
- 2026-07-21: The final post-correctness report at `/private/tmp/machv-perf-final-20260721-1236/perf-report.json` has a 0.921206 compile corpus ratio and 0.926689 upper 95% bound. Per-workload limits still fail for `float_exprs.wast` (1.132051), `ref.wast` (1.584894), `simd_f32x4_arith.wast` (1.266350), and `large_cfg.wast` (1.108184), so this issue remains open.
- 2026-07-22: Reopened because deleting the fixed-legacy runner did not resolve the measured per-workload regressions or satisfy the recorded confidence-bound acceptance criteria.
- 2026-07-22: After restoring the runner, a complete two-pair AArch64 smoke measured a compile corpus point ratio of `1.065560` with upper 95% ratio `1.096506`; multiple individual workloads remained over or inconclusive. This is diagnostic evidence, not the formal 9/21-pair acceptance run, and the issue remains open.
- 2026-07-22: The nine-pair AArch64 report confirms a compile corpus ratio of `1.055745` with upper 95% ratio `1.068714`. `large_cfg.wast` remains the dominant stable regression at `1.785804`; const, integer, float, reference, SIMD, and FNV-1a workloads also exceed their point limits, while indirect return calls and matmul remain inconclusive. The issue remains open.
- 2026-07-22: Converted to non-blocking current-version performance work after the explicit ISS-193 policy decision retired fixed legacy as an acceptance authority. Historical paired measurements remain diagnostic input only.

## Close Notes

Closed as obsolete rather than as a parity claim. The compared legacy compiler is no longer supported and is not an acceptance baseline; actionable compile-time complexity defects remain independently testable and tracked without a legacy ratio gate.
