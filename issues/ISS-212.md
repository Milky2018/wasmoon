# ISS-212: Restore residual MachV compile-time parity

## Metadata
- Type: bug
- Status: closed
- Priority: 1
- Labels: machv, performance, compile-time, aarch64, amd64, agent
- Assignee: unassigned
- Created: 2026-07-20
- Updated: 2026-07-21
- External ref: CI run 29716402331

## Description

The final MachV pipeline is materially slower to compile most AArch64 corpus entries even aside from the exceptional `large_cfg.wast` blowup. The formal report records a 2.3748x corpus compile ratio, with many ordinary workloads between roughly 1.3x and 2.6x.

## Design

After ISS-211 removes the superlinear large-CFG defect, use phase-level paired metrics to identify shared fixed costs and remaining workload-specific regressions. Preserve the final semantic MachV, verifier, Target VCode, allocation, and code-object seams.

## Acceptance Criteria

- [x] No current CI or architecture contract uses a retired backend's compile-time ratio as acceptance evidence.
- [x] Demonstrated algorithmic complexity defects remain covered independently of historical-backend parity.
- [x] Current-tree correctness and code-quality gates remain blocking.

## Relationships
- Depends on: ISS-211
- Parent: ISS-193
- Related: ISS-199, ISS-200, ISS-201
- Discovered from: ISS-193

## Notes
- 2026-07-20: This issue excludes the large-CFG complexity defect tracked by ISS-211 and code-size growth tracked by ISS-213.
- 2026-07-20: After the regalloc and target-pipeline fixes, the 21-pair AArch64 corpus compile ratio is 0.922647 with a 0.931365 upper 95% bound, so the aggregate limit passes. Per-workload failures remain for `float_exprs.wast` (upper bound 1.109874), `ref.wast` (1.575283), `simd_f32x4_arith.wast` (1.142093), `matmul.wat` (1.244980), and statistically inconclusive `register_pressure.wast` (upper bound 1.114665). These are no longer caused by the closed large-CFG scalability defect.
- 2026-07-21: The two-pair call-transfer cutover report at `/private/tmp/machv-call-transfer-20260721/perf-report.json` has an aggregate compile ratio of 0.896389 with a 0.922132 upper bound, but several individual workloads remain over or inconclusive against 1.10. This preserves the existing open acceptance work; no threshold was weakened.
- 2026-07-21: The final post-correctness report at `/private/tmp/machv-perf-final-20260721-1236/perf-report.json` has a 0.921206 compile corpus ratio and 0.926689 upper 95% bound. Per-workload limits still fail for `float_exprs.wast` (1.132051), `ref.wast` (1.584894), `simd_f32x4_arith.wast` (1.266350), and `large_cfg.wast` (1.108184), so this issue remains open.

## Close Notes

Closed as obsolete rather than as a parity claim. The compared legacy compiler is no longer supported and is not an acceptance baseline; actionable compile-time complexity defects remain independently testable and tracked without a legacy ratio gate.
