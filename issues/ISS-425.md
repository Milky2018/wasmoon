# ISS-425: Model reserved fixed-operand registers in regalloc validation

## Metadata
- Type: bug
- Status: in_progress
- Priority: 0
- Labels: backend, x64, regalloc, validation, agent
- Assignee: codex
- Created: 2026-08-13
- Updated: 2026-08-13
- External ref: none

## Description
Strict register-allocation validation rejects the x64 signed division,
remainder, and multiply-high JIT tests after the emergency-move refactor. The
lowered instructions require their second input in R11, but R11 is intentionally
neither allocatable nor an allocator edit/spill scratch after ISS-423.

The default pipeline emits working code because plan verification is optional.
With `MACHV_REGALLOC_VALIDATION=1`, the verifier reports R11-backed values as
illegal allocation locations.

## Design
Represent registers reserved for fixed instruction operands separately from
allocatable registers and allocator edit/spill scratches. The allocator may
honor an explicit fixed constraint in such a register, while it must not choose
that register for a value home, an unconstrained operand, or an inserted edit.
Pass the x64 instruction-reserved registers through the MachV adapter without
granting them transfer-scratch authority.

## Acceptance Criteria
- [ ] The allocation environment distinguishes fixed-operand-only registers from allocator edit/spill scratches.
- [ ] The plan verifier accepts an explicit matching fixed constraint in a reserved operand register and rejects unconstrained use there.
- [ ] X64 keeps R11 outside allocatable, spill-scratch, and transfer-scratch roles.
- [ ] The three strict-validation JIT regressions pass.
- [ ] The full native suite passes with `MACHV_REGALLOC_VALIDATION=1`.
- [ ] Formatting, interfaces, native checks, and module-boundary audit pass.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-419, ISS-423, ISS-424
- Discovered from: ISS-423

## Notes
- 2026-08-13: Reproduced 3/2613 failures. All three lower an explicit R11 fixed input that the post-refactor allocator environment no longer declares legal.

## Close Notes

