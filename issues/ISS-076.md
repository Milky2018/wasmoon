# ISS-076: Split Wasm dialect operations out of MilkIR core

## Metadata
- Type: task
- Status: open
- Priority: 0
- Labels: milkir, wasm_frontend, reusable-infra, abstraction-leak, agent
- Assignee: unassigned
- Created: 2026-07-07
- Updated: 2026-07-07
- External ref: none

## Description
MilkIR is intended to be a reusable Cranelift-like SSA IR, but its core package still owns Wasm-shaped operations and builder helpers. Examples include `Wasm(WasmOpcode)`, Wasm memory/table/GC/reference/call opcodes, `IRBuilder` methods such as `memory_grow`, `table_fill`, `struct_new`, and Wasm-specific validation/optimization behavior.

This leaks Wasmoon frontend and runtime semantics into a generic IR module. Downstream compiler authors should be able to use MilkIR without inheriting WebAssembly-specific concepts.

## Design
Move Wasm semantic extensions out of the MilkIR core surface. Preserve behavior by first copying the current Wasm extension logic into a Wasmoon-owned or explicitly Wasm-dialect package, then mechanically retargeting the Wasmoon frontend/lowering path to use that package.

Non-goals: do not rewrite Wasm lowering logic from scratch, do not remove functionality, and do not weaken existing WAST/JIT correctness gates.

## Acceptance Criteria
- [ ] `Milky2018/milkir` no longer exposes a generic `Wasm(WasmOpcode)` opcode variant in its core IR.
- [ ] Wasm-specific builder helpers are no longer public API of the generic MilkIR core package.
- [ ] Wasm frontend translation still has a behavior-preserving way to represent Wasm-only operations before final lowering.
- [ ] MilkIR validation and optimization passes either become generic or delegate Wasm-specific logic to an explicit Wasm dialect/adapter.
- [ ] Existing Wasmoon JIT and interpreter correctness gates still pass.
- [ ] README and module metadata describe the new ownership boundary.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-065, ISS-077
- Discovered from: none

## Notes
- 2026-07-07: Created after architecture review found MilkIR still carrying WebAssembly semantic operations despite being documented as generic compiler infrastructure.

## Close Notes

