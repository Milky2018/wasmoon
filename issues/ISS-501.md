# ISS-501: Bound large-string transcoding work before host materialization

## Metadata
- Type: bug
- Status: closed
- Priority: 1
- Labels: correctness, conformance, runtime
- Assignee: codex
- Created: 2026-09-09
- Updated: 2026-09-09
- External ref: none

## Description
Both engines time out inside the component adapter on big-strings.wast at command 158. The upstream fixture intentionally fails realloc before massive transcoding; reproduce and preserve the required allocation/trap ordering.

Evidence: `tmp/iss496-high-memory/summary.json` (unmodified upstream inputs).

## Acceptance Criteria
- [x] Reproduce with independent expected results and contrast controls.
- [x] Fix the runtime boundary without changing upstream inputs or masking failures.
- [x] Focused and external regressions pass on both engines.

## Relationships
- Depends on: none
- Parent: ISS-478
- Related: ISS-469
- Discovered from: ISS-478

## Close Notes

A small independent ordering probe proves the root cause without a large allocation: an invalid one-byte UTF-8 source and a trapping destination realloc produce unreachable in pinned Wasmtime, but invalid UTF-8 in current Wasmoon. Valid-byte and out-of-bounds controls pass. Evidence: /tmp/iss501-ordering.wast, /tmp/iss501-before-jit.log, /tmp/iss501-oracle-ordering-final.log. Use borrowed string spans only for immediate synchronous guest-to-guest arguments; asynchronous payload snapshots and host values remain owned.

The synchronous lowering path now carries a checked borrowed span only into an immediate non-suspending guest call. String lowering streams one scalar at a time after allocation, preserves compact-prefix expansion and final shrinking, and validates size arithmetic before realloc. Embedding boundaries materialize strings; pending asynchronous payloads retain independent owned snapshots. Independent UTF-8/UTF-16 ordering controls pass pinned Wasmtime, including the compact pass that grows before decoding non-Latin1 input.

Validation: 2391 native tests and all 78 component runtime ASan tests passed. Core/runtime WAST, stable/async/future/local component suites, and explicit-rights P1 passed. The complete high-memory-inclusive misc sweep is 574 pass, 68 script-only, 112 fail, 10 unsupported, zero timeout/deferred/harness-error. All high-memory scripts now pass. Remaining failures are WAST threads and typed continuations, tracked separately. Evidence: /tmp/iss501-gate-results.json, tmp/iss501-all-misc/summary.json, /tmp/iss501-asan.log, /tmp/iss501-oracle-order-complete.log.
