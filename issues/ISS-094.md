# ISS-094: Introduce a user-facing MilkIR-to-MachV lowering configuration

## Metadata
- Type: task
- Status: closed
- Priority: 2
- Labels: milkir_machv, milkir, machv, api, reusable-infra, usability, agent
- Assignee: unassigned
- Created: 2026-07-09
- Updated: 2026-07-09
- External ref: none

## Description
`Milky2018/milkir_machv/lower.lower_function` is the main path from MilkIR to
MachV, but its public signature has many optional parameters for ISA, ABI
settings, embedding roles, context layout, call convention, helper symbols,
trap payloads, extension lowering, import counts, and IR optimization.

That is powerful, but it is not a friendly interface for ordinary users who
want to lower a verified MilkIR function with an explicit target.

## Design
Add a small user-facing configuration object or facade that groups lowering
options into coherent concepts. Keep advanced hooks available, but do not force
every caller to understand the full internal lowering context.

The design should preserve the existing explicit-ISA rule: reusable lowering
must not detect the host architecture itself.

## Acceptance Criteria
- [x] A documented user-facing lowering configuration or facade exists.
- [x] README examples use the new lower-level configuration path.
- [x] Advanced hooks such as extension lowering and helper-symbol maps remain available without dominating the simple case.
- [x] Existing Wasmoon JIT integration still passes explicit ABI/context data.
- [x] `moon info`, native checks, and relevant README tests pass.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-075, ISS-077, ISS-080
- Discovered from: none

## Notes
- 2026-07-09: Created after reviewing `milkir_machv/lower` public API. The current API is capable but too parameter-heavy as the first interface users see.
- 2026-07-09: Started with an explicit-ISA `LoweringConfig` facade that keeps advanced hooks grouped behind methods.

## Close Notes
- 2026-07-09: Closed by introducing `LoweringConfig` and `lower_function_with_config` in `Milky2018/milkir_machv/lower`. The config keeps explicit ISA selection in the simple path and groups embedding ABI, helper symbol maps, trap payload resolver, extension lowerer, import count, and IR optimization policy behind fluent methods. Existing `lower_function` remains as an advanced compatibility facade and existing Wasmoon/Wasm integration call sites continue to pass explicit ABI/context data. Verification: `moon check --target native --warn-list +73 --diagnostic-limit=400`; `moon test --target native --warn-list +73 --diagnostic-limit=400`; `python3 scripts/audit_module_boundaries.py`; `git diff --check`.
