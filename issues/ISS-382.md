# ISS-382: Delete the consumer-less e-graph use-def map

## Metadata
- Type: chore
- Status: open
- Priority: 3
- Labels: milkir, egraph, cleanup, agent
- Assignee: unassigned
- Created: 2026-08-03
- Updated: 2026-08-03
- External ref: none

## Description
`EGraph.uses` records use-def edges "for worklist scheduling"
(`modules/milkir/egraph/egraph.mbt:524`), and `rebuild` reconstructs the
whole map from scratch (`egraph.mbt:932`). The map has **zero readers** —
the worklist scheduling it was built for never landed. Every `add` and
every rebuild pays hash-map maintenance for a structure nothing consumes.

## Design
Delete the field and both maintenance sites. If ISS-381's incremental
rebuild later needs parent pointers, reintroduce them together with their
consumer in the same change — speculative infrastructure without a reader
does not stay.

## Acceptance Criteria
- [ ] `EGraph.uses` and all writes to it are removed.
- [ ] E-graph suites stay green; no behavioral change.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-381
- Discovered from: none

## Notes
- 2026-08-03: Found during an e-graph audit. Note the tension with
  ISS-381, which may want parent pointers — the rule is that the structure
  arrives with its consumer, not before.

## Close Notes
