# ISS-017: Port IR optimizations and egraph passes to MilkIR

## Metadata
- Type: task
- Status: in_progress
- Priority: 1
- Labels: milkir, optimization, egraph, migration, agent
- Assignee: unassigned
- Created: 2026-07-04
- Updated: 2026-07-04
- External ref: none

## Description
Move the reusable optimization pipeline now under `modules/wasmoon/ir/opt*.mbt` and `modules/wasmoon/ir/egraph` onto MilkIR, without baking WebAssembly semantics into generic optimization passes.

## Design
Separate generic algebraic, CFG, CSE/GVN, rematerialization, and loop optimizations from Wasm-specific legality conditions. Keep optimization APIs usable by non-Wasmoon frontends. If a rule depends on WebAssembly numeric, reference, trap, or runtime behavior, isolate that condition behind metadata or a Wasm-specific rule set.

## Acceptance Criteria
- [x] MilkIR exposes an optimization driver or pass API with ported tests from the old IR optimization suite.
- [x] Generic optimization passes do not import `wasm_core` or Wasmoon runtime/JIT packages.
- [ ] WebAssembly-specific rewrite legality is isolated in frontend/runtime-specific adapters or explicitly documented follow-up issues.
- [x] Old IR optimization tests are ported, replaced, or tracked as follow-up issues.
- [x] `moon test --target native --warn-list +73 --diagnostic-limit=200` passes for affected packages.

## Relationships
- Depends on: ISS-015
- Parent: ISS-014
- Related: ISS-016
- Discovered from: ISS-014

## Notes
- 2026-07-04: This is intentionally separate from core MilkIR so the base IR can stabilize before optimization migration.
- 2026-07-04: Copy-first migration started for the reusable egraph substrate. The old `modules/wasmoon/ir/egraph` package, including algebraic/bitwise/float/vector/extend/select/remat rewrite rules and their whitebox tests, moved to `modules/milkir/egraph`. The moved package no longer imports `wasm_core` or Wasmoon packages; legacy `wasmoon/ir` now imports `Milky2018/milkir/egraph` instead of owning a duplicate egraph implementation. Remaining work: port the old function-level optimization driver and CFG/CSE/remat passes onto MilkIR values/blocks, and isolate Wasm-specific legality rules before closing this issue.
- 2026-07-04: Ported a conservative function-level block-parameter cleanup into MilkIR as `eliminate_dead_block_params`. The pass removes block params with no real instruction/terminator uses and rewrites incoming `Jump`/`Branch` edge arguments, while keeping transitively forwarded params for a later dataflow pass. Added MilkIR verifier-backed tests; `moon test modules/milkir --target native --warn-list +73 --diagnostic-limit=200` passed with 16 tests.
- 2026-07-04: Ported generic unreachable-block elimination into MilkIR as `eliminate_unreachable_blocks` and added it to the O2/O3 optimization pipeline after DCE/block-param cleanup. Added verifier-backed tests; `moon test modules/milkir --target native --warn-list +73 --diagnostic-limit=200` passed with 17 tests.
- 2026-07-04: Ported constant branch simplification into MilkIR as `simplify_branches` and wired it into O2/O3 before block-param/unreachable cleanup. The pass simplifies constant `Brz`, `Brnz`, and `BrTable` terminators to direct jumps; it includes a TODO for frontend-specific large i64 `br_table` index legality. Local MilkIR gates passed with 19 tests.
- 2026-07-04: Ported jump threading into MilkIR as `thread_jumps` and wired it into O2/O3 before unreachable-block cleanup. The pass bypasses instruction-empty jump blocks, including safe block-param argument rewriting for `Jump`/`Branch` edges and no-arg threading for `Brz`/`Brnz`/`BrTable`. Local MilkIR gates passed with 21 tests.
- 2026-07-04: MilkIR now has a native optimization entry point (`OptLevel`, `OptResult`, `instruction_count`, `fold_constants`, `optimize_with_level`) plus ported constant-folding tests. The pass is intentionally conservative: it folds basic non-trapping integer/float/comparison expressions and leaves div/rem/conversions to a TODO until MilkIR carries explicit trapping/overflow legality metadata. `moon check --target native --warn-list +73 --diagnostic-limit=200` and `moon test --target native --warn-list +73 --diagnostic-limit=200` passed with 1975 tests.
- 2026-07-04: Added MilkIR `eliminate_dead_code` and wired it into O2/O3 after constant folding. The pass only deletes unused pure instructions and explicitly keeps loads/calls/stores/traps/custom ops until MilkIR has alias, volatility, and trap metadata. Full native gate passed with 1977 tests.
- 2026-07-04: Ported the pure-expression CSE/GVN slice from the old IR optimizer into MilkIR as `eliminate_common_subexpressions` and wired it into O2/O3 between constant folding and DCE. The pass reuses expressions along the dominator tree, preserves sibling-branch correctness, and deliberately excludes loads, calls, div/rem, float-to-int conversion, traps, stores, and custom ops until MilkIR has alias/trap/overflow legality metadata. Added ported tests for local pure CSE, dominating-block CSE, and duplicate loads that must not be CSE'd. Full native gate passed with 1980 tests.

## Close Notes
