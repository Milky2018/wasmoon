# ISS-126: Complete MilkIR verifier instruction contracts

## Metadata
- Type: bug
- Status: closed
- Priority: 1
- Labels: milkir, verifier, ssa, correctness, tests, agent
- Assignee: codex
- Created: 2026-07-10
- Updated: 2026-07-10
- External ref: none

## Description
MilkIR now verifies definition uniqueness, dominance, use types, CFG targets, block-edge arguments, and return signatures, but instruction verification remains incomplete. Many opcode families validate only operand arity or fall through without checking operand types, result counts, or result types. `CallIndirect`, `CallPtr`, SIMD operations, and untyped `Custom` instructions can therefore pass verification with malformed contracts. `FunctionBuilder::finalize()` also returns an invalid function without validation.

## Design
Define complete, conservative instruction contracts at the MilkIR boundary and make the builder finalization boundary reject invalid IR. Keep structural SSA validation independent from opcode validation, avoid a permissive catch-all opcode branch, and require custom or dialect operations to provide a verifiable contract or be rejected. Preserve the existing dependency boundary: reusable MilkIR must not import Wasmoon product packages.

## Acceptance Criteria
- [x] Scalar, comparison, select, conversion, memory, SIMD, and control-related instruction families validate operand and result arity and types.
- [x] `CallIndirect` validates operands and results against its `Signature`; `CallPtr` validates its declared argument and result counts.
- [x] `Custom` and extension instructions cannot bypass verification without an explicit contract.
- [x] `FunctionBuilder::finalize()` cannot return structurally invalid, unverified IR.
- [x] Every test in `modules/milkir/verify_contract_wbtest.mbt` passes without weakening its assertions.
- [x] Existing verifier, native, and wasm-gc test gates remain green.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-119
- Discovered from: ISS-120

## Notes
- 2026-07-10: Added a red contract suite. Current result is 11 tests: 3 existing structural guarantees pass and 8 missing instruction/finalization contracts fail deterministically.
- 2026-07-10: Claimed for implementation after the user approved moving from red to green.
- 2026-07-10: Code review found and fixed overly broad integer contracts, incomplete memory contracts, negative `CallPtr` counts, and duplicated SIMD lane checks.

## Close Notes
- Added exhaustive opcode contract verification without a permissive catch-all branch, including scalar, conversion, memory, SIMD, call, custom, and extension operations.
- Added explicit signatures to direct calls, custom operations, and extension operations; `FunctionBuilder::finalize()` now verifies before returning.
- Expanded the verifier contract suite from 13 to 18 tests after review. Final gates: MilkIR 92/92, wasm frontend 52/52, native 1772/1772, wasm-gc 462/462, and native with `MACHV_REGALLOC_VALIDATION=1` 1772/1772.
