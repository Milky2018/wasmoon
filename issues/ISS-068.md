# ISS-068: Replace Wasm function indices in MachV call and address opcodes

## Metadata
- Type: task
- Status: closed
- Priority: 0
- Labels: machv, instr, wasmoon_jit, relocation, calls, architecture, migration, agent
- Assignee: unassigned
- Created: 2026-07-06
- Updated: 2026-07-06
- External ref: none

## Description
MachV currently represents direct Wasm targets with raw integer payloads in
instructions such as `LoadFuncAddr(Int)` and `CallDirect(Int, ...)`. The integer
is a Wasm function index, so MachV instruction core still knows a frontend-owned
identifier.

## Design
Introduce a generic code-symbol or code-address reference for MachV direct
function targets. Wasmoon-owned lowering maps Wasm function indices to those
generic references, and Wasmoon JIT/native glue maps the emitted relocation or
fixup metadata back to the current runtime patching representation.

Preserve existing generated behavior by keeping the current patching format
where necessary at the Wasmoon artifact boundary. The MachV opcode payload
itself must stop being a raw Wasm function index.

## Acceptance Criteria
- [x] `LoadFuncAddr(Int)` is replaced by a generic code-address or code-symbol opcode.
- [x] `CallDirect(Int, ...)` no longer carries a raw Wasm function index.
- [x] Wasm function-index to code-symbol/fixup mapping is owned by Wasmoon JIT or Wasm lowering code.
- [x] Existing direct-call and tail-call fixup behavior is preserved.
- [x] Relevant direct call, return_call, and function-reference WAST tests pass.
- [x] Public `.mbti` diffs are reviewed with `moon info --target native`.
- [x] `git diff --check` passes.
- [x] `moon check --target native --warn-list +73 --diagnostic-limit=200` passes.
- [x] `moon test --target native --warn-list +73 --diagnostic-limit=200` passes.

## Relationships
- Depends on: ISS-067
- Parent: ISS-062
- Related: ISS-040, ISS-063
- Discovered from: ISS-062

## Notes
- 2026-07-06: Keep runtime patching behavior intact; this issue only moves the Wasm index out of MachV instruction payloads.

## Close Notes
- 2026-07-06: Added generic `CodeSymbol` and `RelocTarget` to MachV instruction core, replaced `LoadFuncAddr(Int)` with `LoadCodeAddr(CodeSymbol)`, and changed `CallDirect` to carry `CodeSymbol` instead of a raw function index. Wasm lowering now maps function indices to `CodeSymbol("wasm.func", idx)`, while Wasmoon JIT maps those symbols back to cwasm fixup indices at the artifact boundary. Verified with `moon info --target native`, `moon check --target native --warn-list +73 --diagnostic-limit=200`, `moon test --target native --warn-list +73 --diagnostic-limit=200`, and `./wasmoon test spec/call.wast && ./wasmoon test spec/return_call.wast && ./wasmoon test spec/ref_func.wast && ./wasmoon test spec/func_ptrs.wast`.
