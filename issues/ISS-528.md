# ISS-528: Wait for exclusive Windows input without readiness polling

## Metadata
- Type: feature
- Status: closed
- Priority: 1
- Labels: windows, wasi, runtime, readiness
- Assignee: codex
- Created: 2026-09-15
- Updated: 2026-09-15
- External ref: https://github.com/Milky2018/wasmoon/actions/runs/34949733449

## Description
Replace periodic readiness checks for exclusive CLI stdin with bounded input buffering and completion notifications. Preserve the native Unix readiness path and the non-consuming contract for shared host descriptors.

## Design
Explicitly claim exclusive input at the CLI boundary. Windows synchronous pipe and console reads use a dedicated worker, one bounded buffer shared by descriptor aliases, and a socket notification that can participate in the same WSAPoll wait as network sockets. Polling and reading observe the same buffer, EOF and error state. Close joins the worker before releasing resources. The async reactor uses the same interruptible descriptor wait. Shared input and synchronous pipe output retain their compatibility observation path. Unix input remains directly kernel-backed.

## Acceptance Criteria
- [x] Explicit exclusive input policy is shared across platforms; embedding defaults remain shared.
- [x] Windows exclusive input waits on completion without periodic readiness scanning, including mixed socket and reactor waits.
- [x] Partial reads, duplicates, EOF, nonblocking reads and close/cancellation preserve the input contract.
- [x] Native Windows tests cover managed pipes and consoles, idle waits, aliases and cancellation.
- [x] Windows MSVC/Clang, Linux and macOS CI and both-engine readiness regressions pass.
- [x] Documentation states the ownership and compatibility contracts.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-525, ISS-527
- Discovered from: ISS-527

## Notes
- 2026-09-15: A repeated Clang run observed four scans at an 80 ms console timeout. Unified poll and reactor deadlines on QueryPerformanceCounter instead of coarse GetTickCount64; retained the scan bound. Explicit ownership now rejects synchronous duplex pipes, whose shared host blocking mode would otherwise change nonblocking write semantics; added a native rejection regression.
- 2026-09-15: CI 34947936864 passed all Windows external suites and 45 native probes, including ASan; Linux passed. MSVC native tests exposed a test-helper contract regression: the shared test-pipe factory is also used to redirect CRT stdin, so it must remain a real CRT pipe. Restored that factory while retaining socket notifications for production resolver completion and reactor wakeups.
- 2026-09-15: Initial Windows MSVC/Clang probes passed exclusive pipe readiness, bounded scanning, mixed socket waits, aliases, EOF and cancellation. The console test incorrectly assumed CRLF in unprocessed line mode; changed it to compare exact bytes against a direct ReadFile control in the same console mode. Full acceptance remains pending.
- 2026-09-15: Started after the user approved the ownership/buffering design and cross-platform interface review.

## Close Notes

- Implemented explicit exclusive-input ownership at the CLI boundary and an opt-in host API. Windows read-only synchronous pipes and console input use demand-driven reads into a shared 4096-byte buffer, with socket completion notifications for mixed waits. Runtime aliases share bytes, EOF and errors; last-alias close and explicit release join cancelled native work.
- Unified Windows reactor wakeups, resolver notifications and descriptor waits, and use QueryPerformanceCounter for deadline calculations. Linux/macOS retain native kernel readiness. Shared Windows input and synchronous pipe output retain the documented compatibility polling path; synchronous duplex input claims return ENOTSUP rather than changing write semantics.
- [Acceptance CI 34949733449](https://github.com/Milky2018/wasmoon/actions/runs/34949733449) at `ec9e6addcc5bc421fc5de2ef02a5a516519393ab` passed Windows MSVC, Windows Clang, Linux AMD64 and macOS ARM64. Each platform/compiler passed 2452 native tests. Both Windows package reports contain all 44 packages and no missing test executions; the WASI component package passed 42/42.
- Both Windows compilers passed 46 native probes, including bounded idle scan counts, console bytes compared with direct ReadFile, mixed socket/input wakeups, partial reads, aliases, EOF, nonblocking reads, repeated cancellation, duplex rejection and ASan lifetime checks. Both-engine readiness guests passed 12/12 on Windows and locally on macOS.
- Both Windows compilers passed the complete core/component/misc/WASIp1 gates: 258 core files and 62563 assertions per engine; component suites with 845/153/387 commands per engine; 692 misc assertion-bearing passes plus 72 script-only passes; 116 explicit-rights P1 passes plus four stdio smoke passes. The previously agreed hostcall-fuel exclusion is unchanged.
- Local moon info/fmt/check (native, warning 73), all 2452 native tests, 143 Python runner tests (46 Windows-only skips on macOS), and the compiler module boundary audit passed. Ownership and compatibility contracts are documented in docs/windows.md. No follow-up is required for this scope.
