# ISS-528: Wait for exclusive Windows input without readiness polling

## Metadata
- Type: feature
- Status: in_progress
- Priority: 1
- Labels: windows, wasi, runtime, readiness
- Assignee: codex
- Created: 2026-09-15
- Updated: 2026-09-15
- External ref: none

## Description
Replace periodic readiness checks for exclusive CLI stdin with bounded input buffering and completion notifications. Preserve the native Unix readiness path and the non-consuming contract for shared host descriptors.

## Design
Explicitly claim exclusive input at the CLI boundary. Windows synchronous pipe and console reads use a dedicated worker, one bounded buffer shared by descriptor aliases, and a socket notification that can participate in the same WSAPoll wait as network sockets. Polling and reading observe the same buffer, EOF and error state. Close joins the worker before releasing resources. The async reactor uses the same interruptible descriptor wait. Shared input and synchronous pipe output retain their compatibility observation path. Unix input remains directly kernel-backed.

## Acceptance Criteria
- [ ] Explicit exclusive input policy is shared across platforms; embedding defaults remain shared.
- [ ] Windows exclusive input waits on completion without periodic readiness scanning, including mixed socket and reactor waits.
- [ ] Partial reads, duplicates, EOF, nonblocking reads and close/cancellation preserve the input contract.
- [ ] Native Windows tests cover managed pipes and consoles, idle waits, aliases and cancellation.
- [ ] Windows MSVC/Clang, Linux and macOS CI and both-engine readiness regressions pass.
- [ ] Documentation states the ownership and compatibility contracts.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-525, ISS-527
- Discovered from: ISS-527

## Notes
- 2026-09-15: A repeated Clang run observed four scans at an 80 ms console timeout. Unified poll and reactor deadlines on QueryPerformanceCounter instead of coarse GetTickCount64; retained the scan bound. Explicit ownership now rejects synchronous duplex pipes, whose shared host blocking mode would otherwise change nonblocking write semantics; added a native rejection regression.
- 2026-09-15: CI 34947936864 passed all Windows external suites and 45 native probes, including ASan; Linux passed. MSVC native tests exposed a test-helper contract regression: the shared test-pipe factory is also used to redirect CRT stdin, so it must remain a real CRT pipe. Restored that factory while retaining socket notifications for production resolver completion and reactor wakeups.
- 2026-09-15: Initial Windows MSVC/Clang probes passed exclusive pipe readiness, bounded scanning, mixed socket waits, aliases, EOF and cancellation. The console test incorrectly assumed CRLF in unprocessed line mode; changed it to compare exact bytes against a direct ReadFile control in the same console mode. Full acceptance remains pending.
- 2026-09-15: Started after the user approved the ownership/buffering design and cross-platform interface review.

## Close Notes
