# ISS-278: Integrate component commands and WASI conformance gates

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: wasi, component-model, cli, conformance, ci
- Assignee: codex
- Created: 2026-07-27
- Updated: 2026-07-27
- External ref: none

## Description
Expose a real component command execution path and gate the host against pinned
Preview 2 and Preview 3 component workloads.

## Design
Extend the existing component CLI without conflating validation with
execution. The run path configures arguments, environment, preopens, standard
I/O, clocks, random providers, and network policy, instantiates the command
world, invokes `run`, and converts structured exit status to the CLI status.

## Acceptance Criteria
- [x] The CLI runs binary components and preserves the existing parse/validate
      commands.
- [x] Preview 2 stable and Preview 3 async suites are separate versioned lanes.
- [x] Tests cover arguments, environment, exit, stdio, clocks, random,
      filesystem, polling, and loopback sockets.
- [x] CI cannot pass with an empty or skipped WASI component corpus.
- [x] User-facing documentation states supported snapshots and capability
      defaults.

## Relationships
- Depends on: ISS-273, ISS-274, ISS-275, ISS-276, ISS-277
- Parent: ISS-271
- Related: ISS-268, ISS-269, ISS-270
- Discovered from: ISS-271

## Notes
- 2026-07-27: Added `wasmoon component --run` for pinned Preview 2 and
  Preview 3 command exports. The command validates before instantiation,
  inherits standard I/O, preserves configured arguments and environment, and
  grants filesystem/network authority only through explicit `--dir` and
  `--network` options. Added synchronous and async command fixtures plus CLI
  documentation.

## Close Notes
- Closed after ISS-275 and ISS-277 completed, both pinned command variants ran
  through the CLI, capability defaults were documented, and the full MoonBit
  suite passed.
