# ISS-023: Retire legacy wasmoon IR and VCode implementation paths

## Metadata
- Type: task
- Status: open
- Priority: 1
- Labels: wasmoon, cleanup, migration, legacy, agent
- Assignee: unassigned
- Created: 2026-07-04
- Updated: 2026-07-05
- External ref: none

## Description
Remove or reduce the old `modules/wasmoon/ir`, `modules/wasmoon/vcode`, `modules/wasmoon/cwasm`, and old JIT integration paths after the real implementations have moved into reusable modules.

## Design
Wasmoon should remain the product runtime, CLI, interpreter, validator, parser integration, and host integration package. It should consume compiler infrastructure modules instead of carrying duplicate internal compiler implementations. Temporary compatibility shims are allowed only when they are thin, documented, and scheduled for deletion.

## Acceptance Criteria
- [ ] `modules/wasmoon/ir` and `modules/wasmoon/vcode` are deleted, reduced to compatibility shims, or explicitly justified by follow-up issues.
- [ ] Wasmoon CLI, testsuite, explorer, and JIT paths use `milkir`, `machv`, targets, `regalloc`, `machv_regalloc`, `machv_emit`, and `wasmoon_jit` as the implementation path.
- [ ] No compiler-infrastructure module imports `Milky2018/wasmoon` or legacy `Milky2018/wasmoon/ir` / `Milky2018/wasmoon/vcode` packages.
- [ ] User-facing CLI behavior and correctness tests remain stable.
- [ ] Any remaining legacy code has a concrete issue ID and deletion condition.

## Relationships
- Depends on: ISS-017, ISS-022
- Parent: ISS-014
- Related: ISS-024
- Discovered from: ISS-014

## Notes
- 2026-07-04: This issue prevents ending the migration with duplicate real and scaffolded compiler stacks.
- 2026-07-04: `wasmoon explore` gained text stages for the reusable compiler infrastructure: `milkir`, `opt-milkir`, `machv`, and `machv-mc`. These stages lower through `wasm_frontend`, optimize MilkIR, lower into MachV via `wasmoon_jit`, and emit via `machv_emit`, so the CLI can inspect the new path without replacing legacy `ir`/`vcode` compatibility stages yet. Smoke validation passed with `moon run modules/wasmoon/cmd/wasmoon -- explore examples/add.wat --stage milkir --stage machv-mc --func 0`.
- 2026-07-04: Removed the unused legacy `JITEngine`/`FunctionEntry`/`CompilationStatus` layer from `modules/wasmoon/jit`. It was only exercised by its own whitebox tests and kept a public API dependency on legacy `wasmoon/vcode.CompiledFunction` and `JITRuntime` without participating in the production JIT path. Full native `moon info`, `moon fmt`, `moon check`, and `moon test` passed with 2069 tests after removal.
- 2026-07-04: Added explicit `TODO(ISS-023)` on `wasm_frontend.lower_function` because it still obtains MilkIR through the legacy Wasmoon IR translator bridge. Added explicit `TODO(ISS-022)` on the CLI production JIT compiler loop because it cannot safely switch to the new MilkIR/MachV path until that path emits complete `.cwasm` metadata for imports, direct calls, GC stackmaps, exceptions, and dump-on-trap diagnostics. Full native `moon info`, `moon fmt`, `moon check`, and `moon test` passed with 2070 tests.
- 2026-07-05: Updated `wasmoon explore` reusable stages to use the default `wasm_frontend` embedding environment with hidden `Ptr` VMContext and the Wasm-body `wasmoon_jit` lowering/planning entries for `machv` and `machv-mc`. The CLI inspection path now reflects the same Wasm-body ABI boundary used by reusable runtime/preflight planning instead of the generic platform-entry ABI. Smoke validation passed with `moon fmt && moon run modules/wasmoon/cmd/wasmoon -- explore examples/add.wat --stage milkir --stage machv-mc --func 0`.
- 2026-07-05: Moved the duplicated little-endian `Bytes` to `Int64` helper into reusable `wasm_core/types.bytes_to_int64_le` and updated the legacy VCode emitter, WAST runner/JIT support, CLI run command, and testsuite helpers to import it instead of carrying private copies. Targeted native warning gates passed for `wasm_core/types`, `wasmoon/wast`, `wasmoon/testsuite`, `wasmoon/vcode/emit`, and `wasmoon/cmd/wasmoon/commands`.
- 2026-07-05: Moved the duplicated `Int64` pair to Wasm `v128` little-endian byte encoding helper into reusable `wasm_core/types.int64_pair_to_v128_le` and updated CLI result conversion, WAST JIT support, WAST runner result conversion, and testsuite comparison helpers to import it. Targeted native warning gates passed for `wasm_core/types`, `wasmoon/wast`, `wasmoon/testsuite`, and `wasmoon/cmd/wasmoon/commands`.
- 2026-07-05: Added a conservative native MilkIR fast path in `wasm_frontend` for linear scalar Wasm bodies covering parameters, locals, i32/i64/f32/f64 constants, i32/i64 add/sub/mul, f32/f64 add/sub/mul, drop, explicit return, implicit return, and unreachable. Unsupported operators, control flow, calls, memory, references, SIMD, and unreachable tails still fall back to the legacy `wasmoon/ir` translator bridge under `TODO(ISS-023)`. Local validation passed with `moon check modules/wasmoon/wasm_frontend --target native --warn-list +73 --diagnostic-limit=200 && moon test modules/wasmoon/wasm_frontend --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Expanded the native `wasm_frontend` linear fast path to cover `local.tee`, `select`, integer div/rem, bitwise ops, shifts, rotates, clz/ctz/popcnt, integer comparisons, float div/min/max, float unary abs/neg/sqrt, and float comparisons. These straight-line scalar functions now construct MilkIR directly instead of routing through legacy `wasmoon/ir`; control flow, memory, calls, references, SIMD, conversions, and unreachable-tail modelling remain explicit `TODO(ISS-023)` gaps. Local validation passed with `moon check modules/wasmoon/wasm_frontend --target native --warn-list +73 --diagnostic-limit=200 && moon test modules/wasmoon/wasm_frontend --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Expanded the native `wasm_frontend` linear fast path to cover float ceil/floor/trunc/nearest, integer reduction/extension/sign-extension, float promotion/demotion, trapping and saturating numeric conversions, and reinterpret/bitcast. These cases now emit reusable MilkIR opcodes directly instead of using the legacy `wasmoon/ir` bridge. Remaining fallback areas include control-flow beyond the separate structured lowering path, calls, memory, references, SIMD, `copysign`, and unreachable-tail modelling. Local validation passed with `moon check modules/wasmoon/wasm_frontend --target native --warn-list +73 --diagnostic-limit=200 && moon test modules/wasmoon/wasm_frontend --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Copy-first migrated direct `call` lowering and guard-page `memory32`/memory-0 scalar load/store shape into the native `wasm_frontend` linear fast path. Direct calls now emit `milkir.Call("wasm.func.N")` from Wasm function signatures, and simple memory accesses emit the same low-level `wasmoon.LoadMemBase` custom plus `load_ptr`/`store_ptr` sequence previously reached only through the legacy IR bridge. Non-zero memories, memory64, narrow memory accesses, SIMD memory, and full bounds-check lowering remain explicit `TODO(ISS-023)` fallback areas. Local validation passed with `moon check modules/wasmoon/wasm_frontend --target native --warn-list +73 --diagnostic-limit=200 && moon test modules/wasmoon/wasm_frontend --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Copy-first migrated `global.get`/`global.set` lowering into the native `wasm_frontend` linear fast path using the legacy VMContext globals pointer offset and 16-byte global stride. The new path emits MilkIR `load_ptr`/`store_ptr` directly for imported and module globals; the VMContext layout constants are marked `TODO(ISS-023)` until the embedding ABI is described outside the legacy FuncEnvironment. Local validation passed with `moon check modules/wasmoon/wasm_frontend --target native --warn-list +73 --diagnostic-limit=200 && moon test modules/wasmoon/wasm_frontend --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Copy-first migrated `table.size`, `table.get`, and `table.set` low-level shapes into native `wasm_frontend`. `table.size/get` now emit MilkIR VMContext table loads, table.get bounds branch/trap, and table entry loads directly; `table.set` preserves the old `TableFill` runtime-helper shape as `Custom("wasmoon.TableFill")` with an explicit TODO to lower it to a reusable runtime call. VMContext table layout constants remain TODO-scoped until the embedding ABI owns them. Local validation passed with `moon check modules/wasmoon/wasm_frontend --target native --warn-list +73 --diagnostic-limit=200 && moon test modules/wasmoon/wasm_frontend --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Copy-first migrated bulk memory/table runtime-helper boundaries into native `wasm_frontend` as MilkIR custom op shapes: `memory.size`, `memory.grow`, `memory.fill`, `memory.copy`, `memory.init`, `data.drop`, `table.grow`, `table.fill`, `table.copy`, `table.init`, and `elem.drop`. These no longer need the legacy IR bridge just to preserve helper-call boundaries, but each custom op is marked TODO-scoped for replacement by reusable runtime-call lowering in `wasmoon_jit`/targets. Local validation passed with `moon check modules/wasmoon/wasm_frontend --target native --warn-list +73 --diagnostic-limit=200 && moon test modules/wasmoon/wasm_frontend --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Copy-first migrated scalar reference instructions into native `wasm_frontend`: `ref.null`, `ref.is_null`, `ref.func`, `ref.as_non_null`, and `ref.eq`. Null checks now emit MilkIR constants, compares, and branch/trap blocks directly; `ref.func` preserves the old `GetFuncRef` helper boundary as `Custom("wasmoon.GetFuncRef")` with a TODO for reusable runtime-call/relocation lowering. Local validation passed with `moon check modules/wasmoon/wasm_frontend --target native --warn-list +73 --diagnostic-limit=200 && moon test modules/wasmoon/wasm_frontend --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Expanded native `wasm_frontend` coverage for `nop` and F32/F64 local default initialization. Local default constants are now emitted after the entry block is selected, fixing a native fast-path gap where default local values were created without being attached to a MilkIR block. Local validation passed with `moon check modules/wasmoon/wasm_frontend --target native --warn-list +73 --diagnostic-limit=200 && moon test modules/wasmoon/wasm_frontend --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Added native `wasm_frontend` flattening for branchless `block` bodies, including nested branchless blocks. Branching constructs (`br`, `br_if`, `br_table`, `loop`, `if`, `try_table`, and br-on-null variants) still intentionally fall back until label stack effects are modelled directly. Local validation passed with `moon check modules/wasmoon/wasm_frontend --target native --warn-list +73 --diagnostic-limit=200 && moon test modules/wasmoon/wasm_frontend --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Native `wasm_frontend` now skips dead linear tails after `return` or `unreachable` instead of falling back to the legacy IR translator. Local validation passed with `moon check modules/wasmoon/wasm_frontend --target native --warn-list +73 --diagnostic-limit=200 && moon test modules/wasmoon/wasm_frontend --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Added native lowering for pure single-result `if` expressions as MilkIR `select` when both branches are single side-effect-free expressions (`local.get`, constants, or `ref.null`). General structured `if` control flow still falls back until block parameters and label stack effects are modelled directly. Local validation passed with `moon check modules/wasmoon/wasm_frontend --target native --warn-list +73 --diagnostic-limit=200 && moon test modules/wasmoon/wasm_frontend --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Expanded the pure `if` fast path from single-instruction arms to safe scalar expression bodies. The native path now handles branch arms containing local reads, constants, non-trapping integer/float arithmetic, bit operations, comparisons, extensions, saturating conversions, bitcasts, nested selects, and drops, then emits a MilkIR `select` without going through legacy `wasmoon/ir`. Potentially trapping or side-effecting arm operations remain rejected with the existing `TODO(ISS-023)` fallback. Local validation passed with `moon test modules/wasmoon/wasm_frontend --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Native `wasm_frontend` now lowers scalar narrow memory operations for memory0/memory32 (`i32.load8/16_s/u`, `i64.load8/16/32_s/u`, `i32.store8/16`, and `i64.store8/16/32`) into reusable MilkIR `LoadPtrNarrow`/`StorePtrNarrow` operations instead of falling back to legacy `wasmoon/ir`. Non-zero memories, memory64, SIMD memory, and full bounds-check expansion remain explicit `TODO(ISS-023)` gaps. Local validation passed with `moon test modules/milkir modules/machv modules/aarch64_target modules/x64_target modules/machv_emit modules/wasmoon/wasm_frontend modules/wasmoon_jit --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Native `wasm_frontend` now lowers `f32.copysign` and `f64.copysign` directly into reusable MilkIR bitcasts plus integer mask/merge operations, matching the legacy translator's bit-level lowering without importing `wasmoon/ir`. Local validation passed with `moon test modules/wasmoon/wasm_frontend --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Native `wasm_frontend` now lowers `i64.mul_wide_u` and `i64.mul_wide_s` directly into reusable MilkIR `Umulh`/`Smulh` high-half multiply operations instead of using the legacy translator bridge. Local validation passed with `moon test modules/wasmoon/wasm_frontend --target native --warn-list +73 --diagnostic-limit=200`.

## Close Notes
