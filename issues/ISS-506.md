# ISS-506: Honor the component implements feature switch

## Metadata
- Type: feature
- Status: closed
- Priority: 1
- Labels: correctness, conformance, runtime
- Assignee: codex
- Created: 2026-09-09
- Updated: 2026-09-09
- External ref: none

## Description
The misc implements-disabled script is excluded because validation cannot disable the proposal. Add a real validator setting and propagate it through embedding, CLI validation/execution, and conformance scripts. Preserve the upstream input and remove its exclusion only after both modes execute it.

## Acceptance Criteria
- [x] The same component validates when enabled and is rejected with the expected feature error when disabled.
- [x] Imports, exports, nested component and instance type attributes obey the setting.
- [x] CLI and external test configuration reach the real validator; both external modes pass.
- [x] Existing native and component regressions remain green.

## Relationships
- Depends on: none
- Parent: ISS-478
- Related: ISS-485
- Discovered from: ISS-478

## Close Notes

Validation: 2393 native tests and 90 Python tests passed. Enabled/disabled native CLI validation and invocation controls use identical bytes; enabled invocation returns 42, disabled validation reports the cm-implements feature error. Component-test controls also reject the disabled input in both engines. All stable/async/future/local component suites pass. The complete misc sweep reports 576 pass, 68 script-only, 112 fail, eight unsupported, zero timeout/deferred/harness-error; both unchanged implements-disabled executions pass. Evidence: /tmp/iss506-cli-controls.json, /tmp/iss506-script-controls.json, /tmp/iss506-gate-results.json, tmp/iss506-all-misc/summary.json, /tmp/iss506-oracle.log.
