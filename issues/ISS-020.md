# ISS-020: Migrate real register allocation into regalloc and machv_regalloc

## Metadata
- Type: task
- Status: in_progress
- Priority: 0
- Labels: regalloc, machv_regalloc, migration, agent
- Assignee: unassigned
- Created: 2026-07-04
- Updated: 2026-07-05
- External ref: none

## Description
Move the production register allocation implementation now under `modules/wasmoon/vcode/regalloc` into the reusable `regalloc` algorithm module plus the MachV-specific `machv_regalloc` adapter.

## Design
Keep `regalloc` purely algorithmic over an abstract program model. Move live ranges, bundles, backtracking, constraints, spill decisions, reload coalescing, stack layout policy that is algorithm-owned, and allocation checking into reusable APIs where possible. Keep MachV projection, operand rewriting, reload/spill insertion, and MachV stack-frame edits in `machv_regalloc`.

## Acceptance Criteria
- [ ] `modules/regalloc` contains the real allocator algorithm rather than the deterministic baseline placeholder.
- [ ] `modules/regalloc` does not import MachV, Wasmoon, VCode, Wasm, JIT, or target packages.
- [ ] `modules/machv_regalloc` applies allocations back to MachV, including operand rewriting, reload/spill insertion, and stack-slot materialization.
- [ ] Old `vcode/regalloc` tests are ported or tracked; allocation checker behavior is preserved.
- [ ] Existing JIT correctness tests pass after Wasmoon consumes the migrated allocator.

## Relationships
- Depends on: ISS-018
- Parent: ISS-014
- Related: ISS-019, ISS-021
- Discovered from: ISS-014

## Notes
- 2026-07-04: This issue directly addresses the requirement that regalloc be pure algorithm, not dependent on a concrete machine IR.
- 2026-07-04: In progress. `modules/regalloc` now uses abstract live intervals and linear-scan allocation instead of the previous deterministic first-N-register baseline. It remains dependency-free and independent of MachV/Wasmoon. Remaining work is in `machv_regalloc`: operand rewriting, reload/spill insertion, and stack-slot materialization beyond reporting spill slots.
- 2026-07-04: `machv_regalloc.allocate_and_apply` now rewrites allocated virtual operands to physical MachV operands and still materializes stack slots for spilled assignments. Remaining work: insert explicit reload/spill moves around instructions with spilled operands once MachV load/store pseudos are fully owned by the generic emitter path.
- 2026-07-04: `machv_regalloc.allocate_and_apply` now inserts MachV `Load(StackSlot)` and `Store(StackSlot)` pseudos around instructions with spilled operands, reserves scratch registers when possible, rewrites spilled operands through those scratch registers, and has a regression test that verifies reload/store insertion and removal of virtual operands. Remaining work: migrate the old VCode backtracking allocator, checker behavior, reload coalescing, rematerialization, and stress tests into reusable `regalloc` plus MachV adapter APIs before closing.
- 2026-07-04: Copy-first migration started. The pure `UnionFind` helper used by the old backtracking allocator moved from `modules/wasmoon/vcode/regalloc/unionfind` to `modules/regalloc/unionfind`, and `modules/wasmoon/vcode/regalloc` now imports the new module directly. The old Wasmoon-owned unionfind package was removed so there is no duplicate implementation.
- 2026-07-04: `modules/regalloc` now owns a generic live-range model (`ProgramPoint`, `ProgramRange`, `UsePosition`, `OperandConstraint`, `LiveRange`, `LiveRangeSet`) and the allocator consumes `build_live_ranges(program)` instead of a private placeholder interval scan. The linear scan now evicts a longer active interval for a shorter current interval when that reduces spill pressure. Added tests for live-range construction and eviction behavior. Full native gate passed with 1982 tests. Remaining work: adapt the old VCode backtracking allocator/checker/reload-coalescing/rematerialization code to this pure model, then remove the Wasmoon-owned implementations.
- 2026-07-04: Threaded fixed-register constraints through the pure regalloc model. `Operand` now carries an `OperandConstraint`, `build_live_ranges` records it on use positions, the allocator prefers fixed registers and can evict longer active ranges occupying the required fixed register, and `machv_regalloc` projects MachV `Instruction.constraints` into regalloc operands. Added pure regalloc and MachV adapter tests. Full native gate passed with 1986 tests.
- 2026-07-04: Added a pure `regalloc.verify_allocation` pass with typed `VerifyError`s for unassigned vregs, fixed-register mismatches, register-class mismatches, and overlapping live ranges assigned to the same location. This ports the reusable overlap/fixed-constraint part of the old VCode checker without depending on MachV, VCode, Wasmoon, or target packages. Full native gate passed with 1989 tests.
- 2026-07-04: `machv_regalloc.allocate_function` and `allocate_and_apply` now run the pure regalloc verifier before returning or rewriting MachV operands, so projected MachV constraints cannot be silently ignored. Added an adapter regression test for an unsatisfied fixed-register constraint. Full native gate passed with 1992 tests.
- 2026-07-04: Copy-first migrated the parallel-move resolver out of legacy `wasmoon/vcode/regalloc` into pure `modules/regalloc`. The new `regalloc.resolve_parallel_moves` works over `Location`, `PhysicalReg`, and explicit scratch-register inputs only; legacy VCode now adapts its `RegMove` values to the pure resolver. Added pure tests for cycle breaking and stack-to-stack moves. Full native gate passed with 1998 tests.
- 2026-07-04: Migrated the approximate loop-depth spill-weight heuristic into pure `modules/regalloc` as `compute_approx_loop_depths(successors)`. Legacy VCode regalloc now only projects terminators into successor lists and delegates the actual CFG heuristic to the reusable regalloc module. Local regalloc/VCode regalloc gates passed with 60 tests.
- 2026-07-04: Migrated spill-slot reservation policy into pure `modules/regalloc` as `reserve_spill_slot`. Legacy VCode regalloc now adapts register classes and delegates 8-byte slot accounting plus 16-byte vector alignment to the reusable module. Local regalloc/VCode regalloc gates passed with 61 tests.
- 2026-07-04: Copy-first migrated the generic stack-frame slot layout from legacy `wasmoon/vcode/regalloc` into pure `modules/regalloc` as `StackFrame`, `StackSlot`, and `StackSlotKind`. Legacy VCode regalloc now wraps the reusable frame and only adapts MachV/VCode register classes plus AArch64 FP/LR pseudo prologue/epilogue generation. Local regalloc/VCode regalloc gates passed with 64 tests.
- 2026-07-04: Migrated the Cranelift/regalloc2-style spill-weight formula into pure `modules/regalloc` as `compute_spill_weight(uses, loop_depths)`. Legacy VCode bundle code now only projects its use positions into the reusable model before delegating the weight calculation. Local regalloc/VCode regalloc gates passed with 66 tests.
- 2026-07-04: `x64_target.allocatable_regs()` and `aarch64_target.allocatable_regs()` now expose machine-env-backed int, float, and vector registers to `machv_regalloc` instead of the earlier integer-only placeholder lists. This lets reusable regalloc allocate F64 MilkIR values through the MachV adapter instead of forcing them into unsupported spill reload/store paths.
- 2026-07-04: `machv_regalloc` now preserves MachV Float32 vs Float64 register classes when rewriting allocated operands back from the pure regalloc `Float` class. Both x64 and AArch64 target register universes now include Float32 physical registers as well as Float64, so reusable regalloc can allocate scalar float-width conversion instructions without class erasure.
- 2026-07-04: While migrating x64 integer division, found a real remaining regalloc modeling gap: the pure allocator did not yet model tied use-def operands or instruction clobbers during live-range construction. `x64_target` now legalizes division with explicit temporary moves and a TODO-backed fake RDX def so the reusable JIT pipeline is correct without depending on hidden emitter scratch registers. Closing this issue still requires first-class tied physical-def/temp support instead of these target-side workarounds.
- 2026-07-04: The `Load(StackSlot)`/`Store(StackSlot)` pseudos inserted by `machv_regalloc` now have reusable emitter coverage in `machv_emit`, with a `wasmoon_jit` regression that forces x64 spills through `machv_regalloc.allocate_and_apply` and then emits the resulting stack traffic.
- 2026-07-04: `machv_regalloc` vector spills now materialize 16-byte, 16-aligned MachV stack slots, and `machv_emit.StackFrameLayout::build_with_slots` lays out real spill slots by size/alignment instead of assuming every slot is an 8-byte index. Remaining regalloc work is still first-class tied/clobber/temp modeling plus the old backtracking/checker/reload-coalescing/rematerialization migration.
- 2026-07-04: Pure `regalloc` now treats instruction clobbers as allocation constraints: live ranges that are live across a clobber point avoid that physical register, and `verify_allocation` rejects hand-built allocations that place such a range in a clobbered register. `machv_regalloc` projects MachV clobbers into the pure model and has adapter coverage for this behavior. Remaining work is tied physical-def operands, per-instruction temps, and the larger old backtracking/checker/reload-coalescing/rematerialization migration.
- 2026-07-04: Pure `regalloc` now models tied operands through dependency-free operand `tie_id`s. The allocator can co-locate tied use-def ranges at a single program point, the verifier rejects tied operands assigned to different locations while allowing their one-point overlap, and `machv_regalloc` projects MachV operand ties into the pure model before applying allocations back to MachV operands. Local regalloc/MachV adapter gate passed with 33 tests. Remaining work is per-instruction temps plus the larger old backtracking/checker/reload-coalescing/rematerialization migration.
- 2026-07-04: Pure `regalloc` now computes block-level live-in/live-out sets from CFG successors and extends live ranges across successor blocks. `machv_regalloc` projects MachV successors and terminator uses, including return values and branch/block arguments, into the pure model before allocation. Local regalloc/MachV adapter gate passed with 37 tests. Remaining work is per-instruction temps plus the larger old backtracking/checker/reload-coalescing/rematerialization migration.
- 2026-07-04: The legacy `wasmoon/vcode/regalloc` checker now routes pure assignment validation through `@core_regalloc.verify_allocation` by projecting VCode through the MachV bridge and converting `RegAllocResult` into the core allocation model. The old checker no longer owns a duplicate physical-register overlap scan; it remains as a Wasmoon/ISA ABI wrapper for allocatable, scratch, and call-clobber policy. Local legacy regalloc gate passed with 47 tests.
- 2026-07-04: Pure `regalloc` now allows fixed-register constraints to allocate a register outside the ordinary allocatable pool when the value is not live across a clobber of that register. This matches ABI scratch/register-argument constraints such as CallPtr callee pointers in x64 r11 and AArch64 x17 without making those scratch registers globally allocatable.
- 2026-07-04: `machv_regalloc.allocate_and_apply` now applies allocation results to MachV terminator operands as well as ordinary instruction operands. Return values, branch conditions, branch-compare inputs, br_table indices, and block-argument arrays are rewritten from virtual to physical registers; spilled terminator uses get reloads inserted immediately before the terminator. A scoped TODO remains for reserving distinct terminator reload temporaries when multiple spilled same-class edge operands are live together.
- 2026-07-04: MachV target lowering now preserves MilkIR block parameters, and `machv_regalloc` projects those parameters as block-entry definitions in the pure regalloc program. After allocation, `machv_regalloc.allocate_and_apply` materializes block-argument edge transfers as MachV `Move`/`Load`/`Store` pseudos; conditional branches with edge arguments are split through generated edge-copy blocks so copies only execute on the taken edge.
- 2026-07-04: `machv_regalloc` now resolves edge-copy parallel moves per exact MachV class, preserving Float32 vs Float64 when adapting from pure regalloc's collapsed `Float` class back to MachV operands.
- 2026-07-04: Spill-to-spill block-argument edge copies now route through the pure `regalloc.resolve_parallel_moves` scratch expansion instead of becoming a silent no-op in `machv_regalloc`. Vector edge copies pass vector scratch registers to the pure resolver, and unresolved spill-to-spill copies report `ScratchRegisterUnavailable` through the regalloc error surface.
- 2026-07-04: `machv_regalloc` now reserves scratch registers per instruction/terminator while rewriting spilled operands, so multiple spilled operands of the same register class no longer reload into the same temporary. Spill-mode allocation conservatively keeps the first register in each regalloc class allocatable and reserves the remaining registers as scratch; unresolved cases report `ScratchRegisterUnavailable` through the pure regalloc error surface. Added white-box coverage for distinct scratch reservation and vector scratch selection.
- 2026-07-05: Copy-first migrated the block-local reload-interval discovery algorithm into pure `modules/regalloc` as `compute_reload_intervals`. The legacy VCode reload-coalescing path now only projects spilled VCode uses into `ReloadUseInfo` and keeps ISA-specific physical-register candidate selection locally. Local validation passed with `moon test modules/regalloc modules/wasmoon/vcode/regalloc --target native --warn-list +73 --diagnostic-limit=200`. Remaining work is still the larger backtracking allocator/bundle-coalescing migration and removing the Wasmoon-owned allocator implementation.
- 2026-07-05: Migrated the long-distance rematerialization clone-selection policy into pure `modules/regalloc` as `plan_long_distance_rematerializations`. Legacy VCode rematerialization now only recognizes cheap cloneable VCode opcodes, projects rematerializable definitions and use positions into the pure model, and consumes the reusable plan when cloning. Local validation passed with `moon test modules/regalloc modules/wasmoon/vcode/regalloc --target native --warn-list +73 --diagnostic-limit=200`. Remaining rematerialization work is the cross-block local-clone planner plus final deletion of the Wasmoon-owned allocator implementation.
- 2026-07-05: Migrated the cross-block rematerialization local-clone planner into pure `modules/regalloc` as `plan_cross_block_rematerializations`. Legacy VCode rematerialization now delegates both same-block long-distance clone selection and cross-block once-per-use-block clone selection to the reusable algorithm module; it only projects VCode defs/uses and builds cloned VCode instructions. Local validation passed with `moon test modules/regalloc modules/wasmoon/vcode/regalloc --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Migrated spill-slot compaction/coloring into pure `modules/regalloc` as `compact_spill_slots`. The reusable algorithm now owns non-overlap slot reuse, vector two-slot alignment, and no-share anti-coalescing edges; legacy VCode backtracking only projects live ranges/block-arg edge constraints into the pure model and applies the returned slot remap to spill maps and edit streams. Local validation passed with `moon test modules/regalloc modules/wasmoon/vcode/regalloc --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Migrated the backtracking allocator worklist heap into pure `modules/regalloc` as `PriorityWorkQueue`. Legacy VCode backtracking now delegates priority ordering, stable insertion-order tie-breaks, and heap push/pop mechanics to the reusable queue while keeping VCode-specific register hints in a side table. Local validation passed with `moon test modules/regalloc modules/wasmoon/vcode/regalloc --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Migrated fixed-register hint selection into pure `modules/regalloc` as `choose_fixed_reg_hint`. Legacy VCode backtracking now only projects available registers and fixed-reg use constraints into the reusable model; the frequency count and low-index tie-break policy live in the pure algorithm module. Local validation passed with `moon test modules/regalloc modules/wasmoon/vcode/regalloc --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Migrated the backtracking allocator conflict scan/cutoff logic into pure `modules/regalloc` as `scan_conflicts_with_cutoff`. Legacy VCode now projects ordered spans plus occupant bundle weight/pinned metadata into the reusable scanner; conflict de-duplication, first-conflict detection, pinned/heavier rejection, and max-cost cutoff live in the pure module. The old per-query conflict epoch fields were deleted. Local validation passed with `moon test modules/regalloc modules/wasmoon/vcode/regalloc --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Migrated register probe-order planning into pure `modules/regalloc` as `plan_register_probe_order`. Legacy VCode `try_allocate` and `try_evict` now delegate phased caller-saved/callee-saved scanning, deterministic offset rotation, scan-limit handling, and optional hint skipping to the reusable module while retaining only ABI register-set projection. Local validation passed with `moon test modules/regalloc modules/wasmoon/vcode/regalloc --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Migrated backtracking eviction policy helpers into pure `modules/regalloc`: `eviction_cutoff`, `should_take_eviction_candidate`, and `should_take_split_candidate`. Legacy VCode `try_evict` now delegates best-cost cutoff selection, hint-register tie-breaking, and later-split tie-breaking to reusable policy functions while retaining only the side effects that remove conflicting bundles and enqueue retries. Local validation passed with `moon test modules/regalloc modules/wasmoon/vcode/regalloc --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Migrated the backtracking bundle-coalescing planner into pure `modules/regalloc` as `plan_bundle_merges`. The reusable planner owns the old union direction, fixed-register compatibility checks, sorted-span overlap scan, 200-check complexity guard, and merged-span cache; legacy VCode now only projects live-range spans/fixed constraints/block-argument edges into the pure model and rebuilds its local `BundleSet` wrapper from the returned plan. Local validation passed with `moon test modules/regalloc modules/wasmoon/vcode/regalloc --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Migrated the backtracking bundle split decision into pure `modules/regalloc` as `plan_bundle_split`. The reusable planner now owns split-cap fallback, whole-bundle spill fallback, and before/after range partitioning around a split point; legacy VCode keeps only spill-bundle allocation, queue bookkeeping, and local `Bundle` construction. Local validation passed with `moon test modules/regalloc modules/wasmoon/vcode/regalloc --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Migrated backtracking allocation-loop policy into pure `modules/regalloc`: `allocation_iteration_limit`, `should_force_spill_after_retry`, and `should_force_spill_for_class_and_call`. Legacy VCode now supplies bundle state and executes spill actions, while retry-limit, iteration-limit, and vector-across-call spill decisions live in the reusable algorithm module. Local validation passed with `moon test modules/regalloc modules/wasmoon/vcode/regalloc --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Removed the old `wasmoon/vcode/regalloc` whitebox test dependency on `regalloc/unionfind`; the moved pure `UnionFind` implementation is now tested only in `modules/regalloc/unionfind`, so the legacy VCode package no longer carries duplicated pure-algorithm coverage for that helper. Local validation passed with `moon test modules/regalloc/unionfind modules/wasmoon/vcode/regalloc --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Migrated fixed-register constraint edit planning out of legacy `BacktrackingAllocator::generate_result` into pure `modules/regalloc` as `plan_fixed_constraint_edits`. The reusable planner now owns before-use reload/move generation, after-def store/move generation, physical-use mismatch moves, and adapter-preserved class tags; legacy VCode only projects instruction constraints plus allocation/spill locations and converts returned generic moves back to `RegMove`. Local validation passed with `moon test modules/regalloc modules/wasmoon/vcode/regalloc --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Routed the legacy non-backtracking `process_constraints` path through the same pure `regalloc.plan_fixed_constraint_edits` API, so fixed-register edit generation is no longer duplicated between `process_constraints` and the backtracking result builder. Also deleted the old no-op `BacktrackingAllocator::generate_moves` split-bundle placeholder because its result was ignored and real spill/reload edits are generated by the allocation application path. Local validation passed with `moon test modules/regalloc modules/wasmoon/vcode/regalloc --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Migrated output-build temporary register pool ordering into pure `modules/regalloc` as `plan_temp_register_pool`. The reusable planner owns scratch-first ordering, high-index-before-low-index candidate preference, used-register filtering, and duplicate suppression; legacy VCode output construction now only projects ISA machine-env register arrays into integer ids for the planner. Local validation passed with `moon test modules/regalloc modules/wasmoon/vcode/regalloc --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Migrated output-build temporary register choice into pure `modules/regalloc` as `choose_temp_register`. The reusable selector now owns circular temp-pool probing, fallback register stealing, reserved-register filtering, and preserve-spill-slot reservation using the existing spill-slot policy; legacy VCode only converts used epoch marks and ISA candidate lists, then applies the returned preg/save-slot/next-spill-count to `Output`. Local validation passed with `moon test modules/regalloc modules/wasmoon/vcode/regalloc --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Migrated the block-local active reload cache out of legacy `output_build` into pure `modules/regalloc` as `ActiveReloadCache`. Legacy VCode now keeps only class restoration and edit emission; slot/epoch/preg-id tracking, clearing stale reloads after spilled defs, and out-of-range slot tolerance live in the reusable module. Local validation passed with `moon test modules/regalloc modules/wasmoon/vcode/regalloc --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Migrated ordered-span lower-bound and fast conflict-probe scanning into pure `modules/regalloc` as `lower_bound_ordered_span_by_end` and `has_ordered_span_conflict`. Legacy VCode backtracking now projects local occupied spans into reusable ordered spans before checking whether a candidate register is free, removing another hot-path overlap algorithm from the Wasmoon-owned allocator. Local validation passed with `moon test modules/regalloc modules/wasmoon/vcode/regalloc --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Migrated block point-base planning into pure `modules/regalloc` as `compute_block_point_bases`. Legacy VCode backtracking now supplies block order and per-block instruction counts, while reusable code owns the dense program-point base calculation used by bundle ordering and conflict scans. Local validation passed with `moon test modules/regalloc modules/wasmoon/vcode/regalloc --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Migrated call-crossing available-register selection into pure `modules/regalloc` as `select_available_regs_for_bundle`. Legacy VCode backtracking now only projects ABI register sets into abstract physical registers and maps the selected registers back to ABI classes, preserving the existing Float32/Float64 behavior while moving the callee-saved/vector-across-call policy out of Wasmoon-owned allocator logic. Local validation passed with `moon test modules/regalloc modules/wasmoon/vcode/regalloc --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Migrated caller-saved register derivation into pure `modules/regalloc` as `select_caller_saved_regs`, plus the reusable `max_physical_reg_id` helper. Legacy VCode allocator construction now delegates callee-saved filtering to the pure module and only maps ABI register classes at the boundary. Local validation passed with `moon test modules/regalloc modules/wasmoon/vcode/regalloc --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Migrated parameter preassignment policy into pure `modules/regalloc` as `plan_param_preassignment` and `ParamPreassignDecision`. Legacy VCode still resolves ABI incoming registers and call-crossing callee-saved conflicts, but strict precoloring, special pinned params, hint-only params, and call-crossing defer decisions now live in the reusable allocator policy layer. Local validation passed with `moon test modules/regalloc modules/wasmoon/vcode/regalloc --target native --warn-list +73 --diagnostic-limit=200`.
- 2026-07-05: Migrated allocated-span table maintenance into pure `modules/regalloc` as `AllocatedProgramSpan`, `insert_allocated_spans`, and `remove_allocated_spans`. Legacy VCode backtracking now converts ordered spans at the package boundary while reusable code owns sorted insertion and owner-based removal for physical-register occupancy tables. Local validation passed with `moon test modules/regalloc modules/wasmoon/vcode/regalloc --target native --warn-list +73 --diagnostic-limit=200`.

## Close Notes
