# ISS-194: Isolate the union backend as machv_legacy

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: migration, machv, legacy, architecture, agent
- Assignee: codex
- Created: 2026-07-16
- Updated: 2026-07-16
- External ref: none

## Description

Mechanically relocate the current union-style MachV backend to an unpublished `machv_legacy` module and reserve the official `machv` package locations for the final target-neutral architecture. This milestone changes ownership and dependency names, not backend behavior.

## Design

- Preserve the known-good lowering, allocation, emission, and JIT logic mechanically.
- Permit `machv_legacy` imports only from the old migration pipeline and the internal comparison seam.
- Do not publish, document, or expose `machv_legacy` as a supported API.
- Keep the repository buildable and make the entire relocation one coherent revert unit.

## Acceptance Criteria

- [x] The current union representation and backend are moved mechanically to unpublished `machv_legacy` packages without semantic rewrites.
- [x] Existing lowering, regalloc, emission, and JIT consumers compile against the relocated packages with unchanged production behavior.
- [x] Final target-neutral `machv` package locations exist and do not import legacy, target, ABI, emitter, or Wasmoon-owned packages.
- [x] A dependency audit or explicit allowlist proves that no new package can adopt `machv_legacy`.
- [x] Key printer and machine-code snapshots, native tests, and WAST tests show no new failures.
- [x] The common ISS-192 quality gates pass and generated-interface changes contain only the intended mechanical break.

## Relationships
- Depends on: ISS-192, ISS-193
- Parent: ISS-180
- Related: none
- Discovered from: ISS-192

## Notes

- This issue is blocked until ISS-193 fixes the quantified acceptance and performance gates.
- `machv_legacy` has a mandatory deletion milestone in ISS-201.
- 2026-07-16: Claimed after ISS-193 closed. The fixed pre-migration implementation commit is `af3fa2d99598554baab7614e0b08584ab5f8d9da`; migration will mechanically preserve its union IR and backend behavior. The reviewed target and workload manifests live under `docs/perf/machv-migration/`.
- 2026-07-16: Relocated the union backend to `Milky2018/machv_legacy`, redirected the existing lowering, allocation, emission, and JIT pipeline through explicit package aliases, and reserved `Milky2018/machv` version 0.3.0 as an independent target-neutral module.
- 2026-07-16: Extended `scripts/audit_module_boundaries.py` with an exact, shrinking allowlist for legacy consumers. The audit rejects new consumers, stale exceptions, and any dependency from the final `machv` module to legacy, target, ABI, emitter, or Wasmoon-owned packages.

## Close Notes

- Implementation commit: `46c4608dc6c704827ca1099a119093ad0c9424b2`.
- Verified `python3 scripts/audit_module_boundaries.py`, `moon info`, `moon fmt`, `moon check --target native --warn-list +73`, `git diff --check`, and `MACHV_REGALLOC_VALIDATION=1 moon test --target native` with 1925/1925 tests passing.
- Built and installed the release CLI with `moon build --target native --release && ./install.sh`.
- Confirmed byte-for-byte identical `./wasmoon explore examples/benchmark.wat --stage machv mc` output before and after the relocation.
- Confirmed JIT WAST coverage with `spec/const.wast` (376), `spec/int_exprs.wast` (89), `spec/float_exprs.wast` (819), `spec/simd/simd_f32x4_arith.wast` (1819), and `spec/memory64/memory_copy64.wast` (4402), all with zero failures; `examples/benchmark.wasm` also completed.
- Reviewed generated interfaces mechanically after normalizing the package rename; legacy and dependent interfaces matched the fixed pre-migration implementation.
- Follow-up: ISS-195 implements the semantic MachV surface; ISS-201 must delete the complete legacy module and its audit exceptions.
