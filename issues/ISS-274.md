# ISS-274: Implement Preview 2 streams and polling

## Metadata
- Type: feature
- Status: closed
- Priority: 1
- Labels: wasi, preview2, io, streams, poll
- Assignee: codex
- Created: 2026-07-27
- Updated: 2026-07-27
- External ref: none

## Description
Implement `wasi:io/error`, `wasi:io/streams`, and `wasi:io/poll`, including
standard input/output/error stream resources and readiness subscriptions.

## Design
Represent streams and pollables as typed host resources in the shared component
resource table. Reuse the platform readiness backends where their semantics
match, but keep Preview 2 blocking, nonblocking, splice, flush, and error
contracts at the component-host boundary.

## Acceptance Criteria
- [x] Input/output stream methods implement exact partial-progress and error
      contracts.
- [x] Pollables compose clocks and standard streams without busy waiting;
      filesystem and socket poll sources are covered by ISS-275 and ISS-276.
- [x] Standard streams respect inherited, captured, and closed configurations.
- [x] Resource drop closes owned handles exactly once.
- [x] Component-level stream and poll tests pass locally and are included in
      the macOS/Linux CI jobs.

## Relationships
- Depends on: ISS-272, ISS-279
- Parent: ISS-271
- Related: ISS-174, ISS-175, ISS-177, ISS-178, ISS-179
- Discovered from: ISS-271

## Notes
- 2026-07-27: Added shared Preview 2 error, poll, streams, stdio, terminal, and
  monotonic-clock subscription resources. Captured stdio and deadline polling
  have focused tests; platform I/O error propagation and full readiness
  coverage remain open.
- 2026-07-27: All registered stream, poll, stdio, and terminal contracts now
  consume the generated Preview 2 type space. Resource destruction and exact
  partial-progress behavior remain open.
- 2026-07-27: Added embedding-owned resource destructors to the canonical
  resource-drop path. Repeated stdio getters now receive independent owned
  wrappers, so dropping one handle releases exactly its backing entry without
  invalidating another handle.
- 2026-07-27: Native output readiness now uses the platform poll backend,
  blocking writes enforce the 4096-byte contract, short/error writes become
  `last-operation-failed`, and splice performs its required readiness/permit
  sequence.

## Close Notes
- 2026-07-27: Closed after generated-contract registration, captured and
  inherited stream behavior, multi-source deadline/fd polling, canonical host
  resource destruction, splice, blocking-size, and full repository regressions
  passed. Filesystem and socket poll-source adapters remain in ISS-275/276.
