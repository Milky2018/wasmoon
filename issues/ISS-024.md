# ISS-024: Validate real compiler-infrastructure migration

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: validation, gates, migration, agent
- Assignee: unassigned
- Created: 2026-07-04
- Updated: 2026-07-04
- External ref: none

## Description
Run final correctness gates and dependency-boundary audits after the real migration has moved implementation code into reusable modules.

## Design
This is the validation gate for the real migration, not for scaffolding. It should verify module dependency direction, absence of legacy implementation paths, issue tracker state, generated interfaces, and Wasm correctness. Performance gates remain out of scope unless a correctness failure requires performance-sensitive investigation.

## Acceptance Criteria
- [x] `moon fmt`, `moon info`, and `moon check --target native --warn-list +73 --diagnostic-limit=200` pass.
- [x] `moon test --target native --warn-list +73 --diagnostic-limit=200` passes.
- [x] `./install.sh` succeeds and the CLI smoke path works.
- [x] `python3 scripts/run_all_wast.py --rec` passes for interpreter and JIT modes or failures have explicit follow-up issues.
- [x] Component WAST subset runners listed in AGENTS pass or failures have explicit follow-up issues.
- [x] Dependency audit confirms reusable modules do not depend on Wasmoon runtime or legacy compiler packages.
- [x] Issue tracker has no unresolved real-migration issues except explicitly deferred follow-ups.

## Relationships
- Depends on: ISS-023
- Parent: ISS-014
- Related: none
- Discovered from: ISS-014

## Notes
- 2026-07-04: Do not close this issue while only the scaffolded modules are validated.
- 2026-07-04: Added `scripts/audit_module_boundaries.py` as a repeatable dependency-boundary gate. It scans reusable compiler-infrastructure modules (`wasm_core`, `milkir`, `machv`, `regalloc`, `machv_regalloc`, `machv_emit`, `aarch64_target`, and `x64_target`) and fails if they import or reference Wasmoon runtime or legacy `wasmoon/ir`/`wasmoon/vcode` packages. Current result: `python3 scripts/audit_module_boundaries.py` passed.
- 2026-07-05: Validation checkpoint after the runtime-helper migration: `moon fmt`, `moon info`, `scripts/audit_module_boundaries.py`, `moon check --target native --warn-list +73 --diagnostic-limit=200`, and `moon test --target native --warn-list +73 --diagnostic-limit=200` passed with 2293 native tests. `./install.sh` succeeded, `./wasmoon wat examples/simple_add.wat` parsed 48 lines, and `./wasmoon run examples/simple_add.wat` exited successfully. Full WAST correctness passed in both modes: `python3 scripts/run_all_wast.py --rec --only-interp --dump-failures --timeout-sec 20` passed 258/258 files with 62563 assertions, and `python3 scripts/run_all_wast.py --rec --only-jit --dump-failures --timeout-sec 20` passed 258/258 files with 62563 assertions. Component subset gates passed: `python3 scripts/run_component_wast.py --dir component-spec/values --rec --dump-failures` passed 2/2 files with 60 commands, and `python3 scripts/run_component_wast.py --dir component-spec/names --rec --dump-failures` passed 1/1 file with 3 commands. Do not close yet: ISS-020, ISS-021, ISS-022, and ISS-023 remain unresolved migration issues.
- 2026-07-05: Validation checkpoint after moving WASI context initialization into `wasmoon_jit`: `moon fmt`, `moon info`, issue-index regeneration, `scripts/audit_module_boundaries.py`, `moon check --target native --warn-list +73 --diagnostic-limit=200`, and `moon test --target native --warn-list +73 --diagnostic-limit=200` passed with 2294 native tests. `./install.sh` succeeded. Full WAST correctness passed in both modes: `python3 scripts/run_all_wast.py --rec --only-interp --dump-failures --timeout-sec 20` passed 258/258 files with 62563 assertions, and `python3 scripts/run_all_wast.py --rec --only-jit --dump-failures --timeout-sec 20` passed 258/258 files with 62563 assertions. Do not close yet: ISS-022 and ISS-023 remain unresolved migration issues.
- 2026-07-05: Validation checkpoint after moving direct-call stub installation into `wasmoon_jit`: `moon fmt`, `moon info`, issue-index regeneration, `scripts/audit_module_boundaries.py`, `moon check --target native --warn-list +73 --diagnostic-limit=200`, and `moon test --target native --warn-list +73 --diagnostic-limit=200` passed with 2295 native tests. `./install.sh` succeeded, and `python3 scripts/run_all_wast.py --rec --only-jit --dump-failures --timeout-sec 20` passed 258/258 files with 62563 assertions. Do not close yet: ISS-022 and ISS-023 remain unresolved migration issues.
- 2026-07-05: Validation checkpoint after moving `jit_runtime.mbt` context/GC/exception/segment FFI calls behind `wasmoon_jit`: `moon fmt`, `moon info`, issue-index regeneration, `scripts/audit_module_boundaries.py`, `moon check --target native --warn-list +73 --diagnostic-limit=200`, and `moon test --target native --warn-list +73 --diagnostic-limit=200` passed with 2295 native tests. `./install.sh` succeeded, and `python3 scripts/run_all_wast.py --rec --only-jit --dump-failures --timeout-sec 20` passed 258/258 files with 62563 assertions. Do not close yet: ISS-022 and ISS-023 remain unresolved migration issues.
- 2026-07-05: Validation checkpoint after wrapping the temporary legacy `JITContext` native handle in `wasmoon_jit.NativeJITContext`: `moon fmt`, `moon info`, issue-index regeneration, `scripts/audit_module_boundaries.py`, `moon check --target native --warn-list +73 --diagnostic-limit=200`, and `moon test --target native --warn-list +73 --diagnostic-limit=200` passed with 2295 native tests. `./install.sh` succeeded, and `python3 scripts/run_all_wast.py --rec --only-jit --dump-failures --timeout-sec 20` passed 258/258 files with 62563 assertions. Do not close yet: ISS-022 and ISS-023 remain unresolved migration issues.

## Close Notes
- 2026-07-05: Closed after final correctness validation of the real migration. Passed: `moon fmt`; `moon info`; issue-index regeneration; `python3 scripts/audit_module_boundaries.py`; `moon check --target native --warn-list +73 --diagnostic-limit=200`; `moon test --target native --warn-list +73 --diagnostic-limit=200` with 2295 tests; `./install.sh`; `python3 scripts/run_all_wast.py --rec --only-interp --dump-failures --timeout-sec 20` with 258/258 files and 62563 assertions; `python3 scripts/run_all_wast.py --rec --only-jit --dump-failures --timeout-sec 20` with 258/258 files and 62563 assertions; `python3 scripts/run_component_wast.py --dir component-spec/values --rec --dump-failures` with 2/2 files and 60 commands; `python3 scripts/run_component_wast.py --dir component-spec/names --rec --dump-failures` with 1/1 file and 3 commands. Only ISS-025 remains, explicitly deferred for deleting the quarantined `wasmoon_jit/legacy_*` fallback internals after native MilkIR/MachV coverage is complete.
