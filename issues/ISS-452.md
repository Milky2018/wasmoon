# ISS-452: Unify the VCode and regalloc allocation vocabulary

## Metadata
- Type: task
- Status: closed
- Priority: 0
- Labels: machv, vcode, regalloc, architecture, compile-time, performance, aarch64, x64, agent
- Assignee: codex
- Created: 2026-08-29
- Updated: 2026-08-30
- External ref: none

## Description

VCode and regalloc independently represent register classes, physical
registers, operand roles, operand constraints, and allocation locations.
`machv_regalloc` must therefore translate every operand read and every
allocation result even though both sides describe the same target-neutral
machine-allocation facts.

ISS-450 prototypes proved that this is not a cosmetic adapter cost. Direct
indexed input repeated the conversion in each allocation phase and regressed
AEGIS-128L regalloc from 32,016 us to 34,267 us after local caching. An event
sink reduced final plan translation but double-wrote decisions through dynamic
callbacks and regressed regalloc from the retained 30,701 us result to 34,772
us. Removing the adapter safely requires one canonical value vocabulary below
both packages.

## Design

Define target-neutral allocation value types in a dependency layer shared by
VCode and regalloc. Keep VCode owner-tagged `Value`, `Block`, and `Instruction`
handles at the IR boundary, but store already-normalized allocation operands
and result locations in canonical dense value types. Model register classes as
target-defined identities rather than baking the current duplicate enum names
into the shared contract.

Migrate one concept at a time with exact allocation/code-vector tests. Delete
the corresponding conversion only after both producers and consumers use the
canonical type. Do not introduce a VCode-to-regalloc dependency, dynamic event
callbacks in allocator hot loops, or a second cached production representation.

## Acceptance Criteria
- [x] VCode and regalloc use one target-neutral register-class and physical-register representation.
- [x] Selected VCode stores allocation-ready operand role, timing, constraint, preference, and tie facts without per-phase conversion.
- [x] Regalloc result locations and VCode allocation locations share one dense value representation; owner-tagged public handles remain validated at API boundaries.
- [x] `machv_regalloc` no longer maps equivalent allocation enums or records in hot loops.
- [x] Input and output ownership changes preserve exact allocation decisions, spills, reloads, moves, scratch roles, safepoint roots, frame layout, and generated code.
- [x] Seven cache-isolated AEGIS-128L runs improve complete regalloc and module compilation without moving cost into target construction or emission.
- [x] Normal and `MACHV_REGALLOC_VALIDATION=1` native tests, both targets, formatting, warning-73, generated interfaces, and module boundaries pass.

## Relationships
- Depends on: ISS-449
- Blocks: ISS-450
- Parent: none
- Related: ISS-443, ISS-444
- Discovered from: ISS-450

## Notes
- 2026-08-29: Work started with the smallest shared value: register-class identity. The first stage preserves all existing class semantics while moving ownership below both VCode and regalloc.
- 2026-08-29: `RegClass` now lives in `machv/allocation_types`; VCode and regalloc re-export the same identity, and the adapter's class conversion is an identity operation. `Float` and `FpVector` deliberately share the existing dense storage bank so the migration does not enlarge allocator hot tables. Seven alternating cold-cache pairs against commit `47406a57` measured regalloc at 33,255 us baseline versus 33,516 us current (+0.8%) and module compilation at 91,574 us versus 91,292 us (-0.3%). This stage claims no speedup; it preserves the exact 57,396-byte code, 350-spill, 1,023-reload, and 83-move vector while removing one duplicate type. Capture: `_build/aegis-iss452-paired.wZrhG4PC`.
- 2026-08-29: Rejected indexed-input captures: `_build/aegis-iss450-direct-values.QWjGDkLM`, `_build/aegis-iss450-indexed-view.lb8yQD4n`, `_build/aegis-iss450-dense-index.5X8AzmkB`, and `_build/aegis-iss450-index-scratch.bZm7G88n`.
- 2026-08-29: Rejected event-sink capture: `_build/aegis-iss450-sink-prelim.xufRl8tR`. The sink reduced plan translation to 1,480 us but raised total regalloc to 34,772 us and module compilation to 98,130 us.
- 2026-08-30: `OperandTiming` now lives in `machv/allocation_types` and both VCode and regalloc re-export the canonical identity. The adapter's timing conversion is deleted. Focused VCode, regalloc, and adapter tests pass; a cold-cache AEGIS semantic capture preserves 57,396 total code bytes, func_56 at 15,256 bytes, 350 spills, 1,023 reloads, and 83 moves. Capture: `_build/aegis-iss452-timing.G0DSa8kt`.
- 2026-08-30: `OperandRole` now shares the same allocation-types identity across VCode and regalloc, deleting the adapter's role conversion. The canonical type retains `UseDef` for standalone non-SSA allocator clients while VCode builders continue producing only SSA `Use` and `Def` operands. Strict validation passes 2,686/2,686, both target suites pass, and a cold-cache AEGIS capture preserves the exact code and allocation vector. Capture: `_build/aegis-iss452-role.xMm6y12f`.
- 2026-08-30: `PhysicalReg` is now one compact value type shared by VCode and regalloc. The adapter no longer rebuilds clobbers, fixed constraints, preferences, machine-environment registers, or allocated register locations. The canonical Debug form is the existing VCode `pN:class` notation; four regalloc test files received diagnostic-only snapshot updates after all 26 observed diffs were checked for unchanged register IDs, locations, edits, and spills. Strict validation passes 2,686/2,686 and both target suites pass. Seven alternating cold-cache pairs against commit `47c200f7` reduced median regalloc from 32,106 us to 31,735 us, plan translation from 3,182 us to 2,989 us, and module compilation from 87,498 us to 85,468 us. All 14 runs preserved 57,396 total code bytes, func_56 at 15,256 bytes, 350 spills, 1,023 reloads, and 83 moves. Capture: `_build/aegis-iss452-preg-paired.OZPfOEm7`.
- 2026-08-30: Sealed VCode operands now store the shared normalized `AllocationConstraint`; selection-facing `Input` and `Output` keep the target-friendly `Any`, `Fixed`, and `TiedTo(index)` interface, and the builder lowers it once before storage. Ties use the input operand index as an opaque group label, allowing direct VCode verification while remaining allocation-ready. Duplicate outputs tied to one input are rejected before IR mutation and covered by a public builder regression. The adapter's constraint mapping is deleted. An initial scanning verifier prototype was rejected (`_build/aegis-iss452-constraint-paired.aDGmvveX`). Seven final alternating cold-cache pairs against `5880c6b3` measured function-view construction at 1,188 us baseline versus 1,168 us current, aggregate regalloc at 31,910 us versus 31,996 us, and module compilation at 85,886 us versus 86,194 us. The sub-percent aggregate differences are treated as noise, not a speedup or material regression. All 14 runs preserved the exact code and allocation vector. Retained capture: `_build/aegis-iss452-constraint-direct-tie.Z3OzSq9O`. Strict validation passes 2,687/2,687.
- 2026-08-30: VCode private storage and regalloc now share one `VirtualReg` and immutable `AllocationOperand` record. Target-facing operand views still reconstruct owner-tagged `Value` handles through the owning `Function`; the allocation adapter reads canonical operands only through an instruction-owner-checked accessor. The adapter's per-operand six-field reconstruction is deleted. A `#valtype` operand prototype was rejected because copying the six-field record raised function-view construction from 1,162 us to 1,612 us and regalloc from 31,847 us to 32,611 us (`_build/aegis-iss452-operand-paired.iXbAdtCh`). The retained shared-reference representation measured seven alternating cold-cache pairs against `c793e9ac`: function view 1,170 to 745 us (-36.3%), aggregate regalloc 32,037 to 31,060 us (-3.0%), plan translation 3,029 to 2,788 us (-8.0%), and module compilation 86,089 to 84,377 us (-2.0%). All 14 runs preserved the exact code and allocation vector. Capture: `_build/aegis-iss452-operand-ref-paired.oNF2yt1D`. Strict validation passes 2,687/2,687.
- 2026-08-30: Regalloc plans and VCode allocation tables now share one packed `AllocationLocation` value. The first shared heap-enum implementation was rejected because owner-handle rematerialization raised plan translation from 2,746 us to 2,914 us, aggregate regalloc from 30,903 us to 31,778 us, and module compilation from 84,091 us to 87,900 us (`_build/aegis-iss452-location-paired.5W9iy1RQ`). A two-word value prototype was also rejected after moving cost into frame planning and emission (`_build/aegis-iss452-location-valtype2-paired.qu7bggqt`). The retained representation packs the full signed 32-bit identity and location tag into one `Int64`; checked owner-tagged `StackSlot` views remain at public API boundaries, while compiler-owned verification, frame planning, and register-only emission read the canonical value directly. Seven alternating cold-cache pairs against `e2aaed15` reduced module compilation from 84,980 us to 82,779 us (-2.6%), aggregate regalloc from 30,965 us to 29,862 us (-3.6%), plan translation from 2,772 us to 2,310 us (-16.7%), frame planning from 3,257 us to 3,074 us (-5.6%), and emission from 3,610 us to 3,567 us (-1.2%). All 14 runs preserved 57,396 total code bytes, func_56 at 15,256 bytes, 350 spills, 1,023 reloads, and 83 moves. Capture: `_build/aegis-iss452-location-unchecked-paired.3xApOB0o`.

## Close Notes

Closed after the complete allocation vocabulary became shared without a second
production cache or a VCode-to-regalloc dependency. Strict native validation
passes 2,689/2,689, both target suites pass (AArch64 228/228 and x64 117/117),
and formatting, warning 73, generated interfaces, module boundaries, and diff
validation are clean. No corpus sweep was run; the requested AEGIS-128L gate
used seven isolated pairs and an exact code/allocation vector.
