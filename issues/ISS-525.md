# ISS-525: Port the complete runtime to native Windows AMD64

## Metadata
- Type: feature
- Status: in_progress
- Priority: 1
- Labels: windows, runtime, jit, abi, ci
- Assignee: codex
- Created: 2026-09-15
- Updated: 2026-09-15
- External ref: none

## Description
Provide the native Windows runtime required to validate WASIp1 descriptor readiness through both actual execution engines. The current C runtime still depends on POSIX fiber stacks, signal recovery and host descriptor operations; Windows-specific stubs alone do not establish support.

## Acceptance Criteria
- [ ] Native Windows CI builds and executes the CLI and tests.
- [ ] Host/JIT transitions preserve the Windows ABI, stack alignment and nonvolatile registers.
- [ ] Fibers, traps, GC roots, executable memory and cancellation work on Windows.
- [ ] Both engines execute core/component regressions and the Windows readiness guest tests.
- [ ] Linux AMD64 and macOS ARM64 gates remain green.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-177
- Discovered from: ISS-177

## Close Notes

## Progress

- 2026-09-15: Initial native Windows CI 34920939926 reached C compilation and exposed POSIX headers, signal jumps, file type macros and ELF object generation. Added Windows fibers, explicit Win64-to-private-SysV boundaries, non-unwinding register recovery and COFF debug sections. This is an intermediate port; Windows execution and readiness acceptance remain pending.

- 2026-09-15: Windows diagnostic CI 34921531342 reduced build failures to missing signal/page-size APIs and one export declaration. Fixed these and ported native concurrency and fiber guard probes; added pointer-sized socket ownership for the forthcoming readiness backend. macOS native tests pass 2450/2450; Windows execution remains pending.

- 2026-09-15: Windows CI 34922786050 reached the linker. Fixed shared trap finalization and random-source linkage. Introduced a native host I/O package, routed Windows JIT WASIp1 imports through the existing shared hostcall implementation, and implemented Windows reactor registrations, timers, cancellation and wakeups. Added Windows executable/process handling to corpus runners and an actual dual-engine core CI gate. Local native tests remain 2450/2450; Windows results are still pending.

- 2026-09-15: CI 34923273069 compiled the complete native Windows runtime and passed all 8 native readiness probes. The executable-copy step used the wrong default output directory; switched to an explicit build directory. Added UTF-8 Windows file opens and handle-relative NT opens so the existing capability walker can preserve held parents, symlink limits and traversal restrictions. Filesystem operations beyond opening remain in progress.

- 2026-09-15: Windows CI 34923642809 passed the dual-engine core suite and is running native tests. Replaced permissive Windows path containment and empty directory enumeration stubs with canonical containment and handle-based directory scans. Added filesystem primitive regressions for Unicode, binary contents, rename-stable handles and no-follow truncation. Additional filesystem hostcalls and component acceptance remain pending.

- 2026-09-15: CI 34924509763 passed Windows filesystem/readiness probes, both core engines, and all six real readiness guests. Component regressions remain failing. Cancellation evidence from the earlier run showed eight outstanding C compiler processes before native test execution; reduced compilation to two workers and added bounded build/package logs with inventory count checks. Added handle-based Windows metadata, timestamps, directory creation, deletion and rename operations.

- 2026-09-15: CI 34925159459 again passed Windows native probes, dual-engine core and readiness guests. Component execution was blocked by Git autocrlf changing pinned snapshot bytes; the observed hash exactly matches an LF-to-CRLF conversion. Added exact-byte checkout attributes and a real Git filter regression while retaining snapshot verification. Local native tests pass 2450/2450; component and Windows native package acceptance remain pending.

- 2026-09-15: Reused the asynchronous resolver state machine on Windows with SRW locks, CRT worker threads and pipe readiness. Added console non-consumption, APC interruption, and case-sensitive directory containment regressions. Changed canonical containment to compare normalized path spelling exactly. macOS native tests pass 2450/2450; the new Windows-specific tests await CI.

- 2026-09-15: Added Windows hard links using handle-relative NT file information, including retained-parent and absolute-host-path tests. Extended Windows CI to the complete misc corpus and explicit-rights WASIp1 suite with upstream Windows fixture environment flags. Native package checks now run even when earlier conformance gates fail so bounded logs expose independent failures. Local native tests pass 2450/2450.

- 2026-09-15: CI 34926134832 passed all component suites in both engines and reached bounded Windows native tests. Implemented shared descriptor flags, append toggling, duplicate ownership, and cursor-independent positioned I/O. Fixed shared WASIp1 fdstat reporting after successful flag changes. Added real Windows file/socket regressions; local native tests pass 2451/2451. These new Windows branches still require CI validation.
