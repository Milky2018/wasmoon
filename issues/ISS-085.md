# ISS-085: Move table and context-pointer lowering behind wasm_isa_lower

## Metadata
- Type: task
- Status: closed
- Priority: 0
- Labels: isa_target, wasm_isa_lower, reusable-infra, abstraction-leak, agent
- Assignee: Codex
- Created: 2026-07-07
- Updated: 2026-07-07
- External ref: none

## Description
`isa_target/lower` still owns helper state and APIs for function-table and table0-base lowering. Even after Wasm ExtOp dispatch moved to `wasm_isa_lower`, the generic lowerer still exposes table-shaped caches and emits context loads using fixed runtime-context fields.

## Design
Move table0/function-table context load helpers and caches into `wasm_isa_lower`, or replace them with generic context-slot helper APIs that do not carry table-specific names. Preserve the existing generated instruction shape by mechanically moving the current implementation.

## Acceptance Criteria
- [x] `isa_target/lower` no longer exposes `emit_load_table0_base`, function-table caches, table0 caches, or table-specific invalidation APIs.
- [x] Wasm call/ref lowering in `wasm_isa_lower` still emits the same logical context loads through generic context-slot APIs or local Wasm-owned helpers.
- [x] Reusable `isa_target` public API no longer contains table0/function-table wording except in documentation explaining boundaries.
- [x] Native tests and rebuilt WAST gates pass.

## Relationships
- Depends on: ISS-084
- Parent: ISS-083
- Related: ISS-077
- Discovered from: ISS-083

## Notes
- 2026-07-07: Current offenders include `modules/isa_target/lower/lower_memory.mbt`, `LoweringContext::cached_func_table`, and `invalidate_table0_base_cache`.

## Close Notes

- 2026-07-07: Moved function-table and table0-base context loading to
  `wasm_isa_lower` using generic context slot ids and the reusable
  `LoweringContext::cache_context_pointer` cache. Deleted the generic
  `isa_target/lower` `emit_load_table0_base` helper and removed function-table
  and table0-specific cache APIs from its generated interface. Remaining
  function-table/global-storage wording is isolated to LICM/CSE and tracked by
  ISS-086. Verified with `moon info`, `moon fmt`,
  `moon check --target native --warn-list +73 --diagnostic-limit=200`,
  `moon test --target native --warn-list +73 --diagnostic-limit=200`, and
  `./install.sh && python3 scripts/run_all_wast.py --rec` (interpreter
  258/258, JIT 258/258).
