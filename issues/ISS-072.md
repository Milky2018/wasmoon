# ISS-072: Move Wasmoon GC allocation pseudo-ops out of MachV instruction core

## Metadata
- Type: task
- Status: in_progress
- Priority: 0
- Labels: machv, instr, machv_emit, wasmoon_jit, gc, runtime, safepoint, architecture, migration, agent
- Assignee: unassigned
- Created: 2026-07-06
- Updated: 2026-07-06
- External ref: none

## Description
MachV instruction core currently contains Wasmoon GC allocation pseudo-ops:
`GCAllocStructInline`, `GCAllocStructSlowPrecise`,
`GCAllocArrayFixedInline`, and `GCAllocArraySlowPrecise`. These opcodes encode
Wasmoon GC object layout, allocation cursor/limit layout, helper symbols,
outgoing stack buffer conventions, and safepoint metadata behavior.

## Design
Move the existing allocation behavior out of MachV instruction core one opcode
family at a time. Preserve behavior by mechanically migrating current emitter
logic into Wasmoon-owned lowering, target hooks, or generic MachV instruction
sequences.

The migration order inside this issue should be:
1. slow precise struct allocation.
2. slow precise array allocation.
3. inline struct allocation fast path plus slow path.
4. inline fixed-array allocation fast path plus slow path.

Do not hand-write a new allocator. Copy the current AArch64 and AMD64 emission
logic as the behavioral source of truth, then generalize names and ownership.

## Acceptance Criteria
- [ ] `GCAllocStructInline` is removed from MachV instruction core.
- [x] `GCAllocStructSlowPrecise` is removed from MachV instruction core.
- [ ] `GCAllocArrayFixedInline` is removed from MachV instruction core.
- [x] `GCAllocArraySlowPrecise` is removed from MachV instruction core.
- [ ] Safepoint metadata emitted for slow allocation paths matches current behavior.
- [ ] Inline fast paths preserve object layout, cursor/limit checks, registration calls, and slow fallbacks on AArch64 and AMD64.
- [ ] Relevant Wasm GC WAST tests pass on the native host.
- [ ] AMD64 WAST correctness is verified with Docker before closing.
- [ ] Public `.mbti` diffs are reviewed with `moon info --target native`.
- [ ] `git diff --check` passes.
- [ ] `moon check --target native --warn-list +73 --diagnostic-limit=200` passes.
- [ ] `moon test --target native --warn-list +73 --diagnostic-limit=200` passes.
- [ ] `python3 scripts/run_all_wast.py --rec --dump-failures --timeout-sec 20` passes.

## Relationships
- Depends on: ISS-071
- Parent: ISS-062
- Related: ISS-063, ISS-065
- Discovered from: ISS-062

## Notes
- 2026-07-06: This is the highest-risk child issue because it crosses GC layout, safepoints, outgoing stack buffers, helper fixups, and two native emitters.
- 2026-07-06: Migrated slow precise struct/array allocation to generic `LoadSafepointId` plus `CallExternal`; inline fast-path opcodes remain.

## Close Notes
