# ISS-057: Move production bundle merging into regalloc

## Metadata
- Type: task
- Status: open
- Priority: 1
- Labels: regalloc, machv_regalloc, architecture, migration, agent
- Assignee: unassigned
- Created: 2026-07-06
- Updated: 2026-07-06
- External ref: none

## Description
The production backtracking allocator now delegates allocation-loop, probe,
eviction, split, spill, and result policies to `regalloc`, but bundle
coalescing still keeps its production union-find merge algorithm inside
`modules/machv_regalloc/backtracking_adapter.mbt`. This leaves one important
allocator-core decision path owned by the MachV adapter instead of reusable
compiler infrastructure.

## Design
Move the current production bundle-merge/coalescing decision logic into
`modules/regalloc` without changing behavior. The generic planner must preserve
MachV's current semantics by accepting pre-ordered spans and callback/payload
inputs for class and fixed-register compatibility instead of encoding
`ProgPoint` positions through ad-hoc integer tricks. `machv_regalloc` should
project `LiveRangeSet` and move/block-param edges into the generic planner,
then materialize the returned plan into MachV `BundleSet` and range `bundle_id`
updates.

## Acceptance Criteria
- [ ] `regalloc` owns the production bundle merge/coalescing union-find,
  overlap-scan, fixed-constraint compatibility, and root-span merge policy.
- [ ] `machv_regalloc` no longer contains a duplicate production merge
  algorithm; it only projects MachV ranges/edges and materializes `BundleSet`.
- [ ] MachV `ProgPoint` ordering, `Before`/`After` positions, and fixed-bank
  compatibility are preserved through typed projection/callbacks, not encoding
  hacks.
- [ ] Existing `regalloc.plan_bundle_merges` tests are updated or supplemented
  to cover the generic production planner.
- [ ] `moon info --target native` is run and generated `.mbti` diffs are
  reviewed.
- [ ] `git diff --check` passes.
- [ ] `moon check modules/regalloc modules/machv_regalloc --target native --warn-list +73 --diagnostic-limit=200` passes.
- [ ] `moon test modules/regalloc modules/machv_regalloc --target native --warn-list +73 --diagnostic-limit=200` passes.
- [ ] `python3 scripts/run_all_wast.py --rec --only-jit --dump-failures --timeout-sec 20` passes.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-050, ISS-052, ISS-053, ISS-055
- Discovered from: ISS-050

## Notes
- 2026-07-06: Created after auditing the post-ISS-050 state. The remaining
  concern is not an interpreter fallback or legacy JIT path; it is production
  bundle merge policy still being owned by the MachV adapter.

## Close Notes
Leave empty until closing.
