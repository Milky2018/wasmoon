# ISS-198: Implement the AArch64 Target VCode pipeline

## Metadata
- Type: task
- Status: in_progress
- Priority: 1
- Labels: aarch64, target-vcode, regalloc, emitter, migration, agent
- Assignee: codex
- Created: 2026-07-16
- Updated: 2026-07-17
- External ref: none

## Description

Implement the complete AArch64 vertical pipeline from verified semantic MachV through selection, legalization, allocation, frame planning, emission, and metadata production while production still uses the legacy AArch64 path.

## Design

- Mechanically reuse known-good AArch64 behavior where possible, but express it in the final target seams.
- Keep target sequences, temporary registers, addressing modes, conditions, ABI details, and encodings inside AArch64 packages.
- Use shadow comparison only in tests and diagnostics.

## Acceptance Criteria

- [ ] AArch64 lowers every production semantic MachV operation into verified AArch64 VCode or a structured unsupported-operation error.
- [ ] Selection, legalization, regalloc constraints, frame planning, emission, relocations, fixups, traps, GC metadata, stack maps, and unwind data use the final seams.
- [ ] Tests compare normalized VCode, metadata, disassembly, and executed JIT behavior with the legacy path.
- [ ] JIT/interpreter differential coverage includes arithmetic, calls, memory, SIMD, GC, exceptions, tail calls, and stack arguments.
- [ ] The complete macOS AArch64 native, WAST, component, and relevant WASI gates from ISS-193 pass in shadow mode.
- [ ] Production AArch64 remains on the legacy path until ISS-199.
- [ ] The common ISS-192 quality gates pass.

## Relationships
- Depends on: ISS-197
- Parent: ISS-180
- Related: none
- Discovered from: ISS-192

## Notes

- This issue expands the new implementation; ISS-199 performs the cutover and contraction.
- 2026-07-17: Claimed on `milky/improve-machv`. Implementation starts with a shadow-only vertical slice that copies proven AArch64 selection and encoding behavior into the final typed VCode, Allocation, FrameLayout, and emission seams. Unsupported semantic operations must be returned as structured errors rather than falling through to legacy code.
- 2026-07-17: Replaced the placeholder VCode metadata ids and string symbol with semantic `SourceLocation`, `TrapReason`, and a closed code/external/data symbol reference. Whole-function snapshots now expose these facts together with safepoints and live roots, so target lowering and later code-object comparison cannot silently erase or reinterpret metadata.
- 2026-07-17: Added the first real semantic MachV-to-AArch64 selector. It reconstructs independent typed blocks, values, edge arguments, and signatures; selects integer constants, copies, nontrapping integer unary/binary operations, comparisons, pointer offsets, jumps, branches, and ABI-constrained returns; preserves source metadata; and verifies the complete target VCode before success. Valid but unsupported operations return a typed error with the MachV block and instruction site. Division remains deliberately unsupported until its zero/overflow trap legalization is emitted rather than incorrectly relying on AArch64 hardware behavior.
- 2026-07-17: Established the first complete executable vertical slice. AArch64 now owns ABI parameter/result materialization, a target register environment, allocation verification, aligned spill/callee-save frame planning, scalar instruction encoding, stack edits, prologue/epilogue emission, and checked local-branch fixups. The final output is a verified unlinked code object with typed relocation/trap/safepoint seams. Floating-point and SIMD allocation now share one `FpVector` bank to model the architectural register alias instead of permitting impossible simultaneous assignments. Whole-object snapshots cover allocated integer arithmetic and conditional control flow. Production coverage, metadata emission, edge-move resolution, calls, memory, SIMD, GC, exceptions, tail calls, shadow comparison, and gate closure remain open under this issue.
- 2026-07-17: Completed the final-seam CFG copy and metadata slice. Generic VCode now resolves typed parallel copies, stack-to-stack transfers, register cycles, and mixed scalar-float/SIMD cycles without aborting; AArch64 supplies reserved scratch registers, full-width FP/SIMD moves, stack encodings, and isolated conditional-edge stubs. Emission now records general source sites, typed safepoints, and allocated register/stack GC roots in the verified code object. Whole-object tests cover jump-edge copies, conditional copies, explicit register cycles, source maps, and precise GC roots. Trap-site production awaits the corresponding trapping instruction families rather than attaching false traps to nontrapping scalar instructions.
- 2026-07-17: Added the first complete direct-call slice for the platform ABI. Semantic external calls now select to verified target VCode with allocatable arguments, explicit caller-saved clobbers, ABI-ordered result materialization, and structured rejection of unsupported internal, returns-twice, multi-result, and stack-argument forms. AArch64 emission resolves call arguments as parallel moves, preserves and restores `x30` for non-leaf functions, emits `BL` with a typed `AArch64Call26` relocation, and records source plus GC/cancellation safepoints at the actual call instruction. Explicit keepalive uses make precise GC roots cross the clobber point; allocation and whole-code-object snapshots verify a live root in `x19`, typed relocation offsets, LR frame state, and result/clobber contracts. Stack arguments and internal/tail calls remain open in this issue.
- 2026-07-17: Extended platform calls through AAPCS64 stack arguments. One derived call layout assigns the independent integer and FP/SIMD register banks, preserves source-order stack overflow with 8/16-byte alignment, and computes the maximum 16-byte-aligned outgoing area per function. Frame planning reserves that area below spills and saved registers; incoming stack parameters remain addressed above the complete frame. Emission stores overflow arguments before resolving register arguments as a parallel copy, so source registers cannot be overwritten before their stack values are captured. Mixed-layout, frame, and whole-code-object snapshots cover the ninth integer argument, outgoing-area placement, stack load/store encodings, call relocation offset, and LR restoration. Internal and tail-call ABIs remain open.
- 2026-07-17: Replaced the implicit internal convention with an explicit, validated AArch64 `InternalAbi` and required `LoweringContext`. The contract contains only execution-environment, user integer/FP argument, and integer/FP result registers; it rejects illegal, duplicate, and context-reused registers before lowering. Target lowering derives immutable entry and per-call plans, including grouped overflow stack arguments and structured extra-result-area rejection, then materializes verified `InternalCall` VCode. The emitter shares the parallel argument mover and outgoing-area frame machinery with platform calls, preserves LR, and emits typed `Code` rather than `External` relocations. Whole-layout, selected-VCode, invalid-contract, and code-object snapshots cover context-first entry placement, internal call arguments/results, safepoints, and code-symbol relocation. Indirect calls, return areas, and tail calls remain open.
- 2026-07-17: Added internal indirect calls without introducing a target-union fallback. The semantic callee remains outside the logical call signature, while target VCode verifies the leading `Ptr64` operand and the same derived internal call plan used by direct calls. Emission resolves the callee move to reserved `x17` together with register arguments in one parallel-copy graph, removes `x17` from the cycle-scratch set, stores stack arguments first, and emits `BLR x17` with no relocation. Selected-VCode and whole-code-object snapshots lock operand order, context placement, the `BLR` encoding, LR preservation, and empty relocation metadata. Return areas and true internal tail calls remain open.
- 2026-07-17: Implemented true internal AArch64 tail calls as terminators rather than degrading them to call-plus-return. Target VCode accepts only Internal caller/callee protocols, proves that the callee stack-argument area fits the caller's incoming area, requires result-layout equality, and rejects precise roots until post-epilogue stack maps exist. Emission forwards overflow arguments above the current frame, resolves direct or indirect argument moves, tears the frame down, and emits either `B` with a typed `AArch64Jump26` code relocation or `BR x17` without a relocation. Whole-object snapshots cover direct, indirect, and compatible stack-argument forwarding; a negative snapshot locks rejection of oversized tail layouts. Extra return areas and post-epilogue tail-root maps remain open.
- 2026-07-17: Added the non-atomic scalar memory vertical slice. Semantic little-endian loads and stores select to typed AArch64 VCode whose verifier independently checks integer extension, pointer/GC-reference width, FP width, operand shape, and result shape. Emission covers byte, halfword, word, doubleword, F32, and F64 encodings; unencodable unsigned offsets are materialized through reserved `x16` before the actual memory instruction. Trap and source metadata are attached to the faulting `LDR/STR`, not to address setup. Whole-VCode and whole-code-object snapshots cover signed and zero-extending loads, narrow and FP stores, large offsets, and multiple trap sites; valid non-native-endian memory remains a structured unsupported operation. Atomic and vector memory remain open.
- 2026-07-17: Legalized all integer division and remainder operations instead of selecting AArch64's non-trapping hardware behavior directly. Target VCode now represents zero-divisor and signed-division-overflow checks separately and verifies their exact semantic trap metadata. Emission places `BRK` at distinct code-object trap sites after `CBNZ` and the conditional overflow sequence, then uses `SDIV`/`UDIV`; remainder computes `lhs - quotient * rhs` with reserved `x16` and `MSUB`, so signed remainder traps only on zero and preserves the defined `INT_MIN % -1 == 0` behavior. VCode and whole-object snapshots lock 32/64-bit encodings, source attribution, and division-by-zero versus overflow sites.
- 2026-07-17: Added the non-trapping scalar floating-point core. Target VCode independently verifies F32/F64 raw-bit constants, negate/absolute/square-root, add/subtract/multiply/divide, and ordered comparison shapes. Constants materialize through reserved `x16` and bit-exact `FMOV`, preserving signed zero and NaN payloads; arithmetic emits scalar S/D instructions, while comparisons use `FCMP` plus NaN-safe AArch64 conditions before `CSET`. Whole-VCode and whole-code-object snapshots cover both precisions, constant bit patterns, shared FP/vector allocation, arithmetic, and comparison results. Min/max, rounding, copy-sign, fused operations, conversions, and vector FP remain explicitly unsupported pending exact semantic legalization.
- 2026-07-17: Added non-trapping scalar conversions with an explicit target contract. AArch64 VCode now verifies I64-to-I32 wrapping, signed/unsigned I32 extension, 8/16/32-bit sign extension, F32/F64 promote and demote, scalar equal-width bitcasts, and every signed/unsigned I32/I64-to-F32/F64 conversion. The bitcast contract excludes GC references and vectors and admits only valid same-width scalar register-bank transfers. Emission uses `SBFM`, W-register zero extension, `FCVT`, `SCVTF`/`UCVTF`, and bit-exact `FMOV` encodings. Data-flow-preserving VCode and whole-object snapshots cover every conversion family and cross-bank allocation. Float-to-int remains unsupported until saturation and trapping bounds are legalized explicitly.
- 2026-07-17: Completed scalar float-to-integer conversion for every F32/F64, I32/I64, and signed/unsigned combination. Saturating conversions use AArch64 `FCVTZS`/`FCVTZU`; a native Arm64 boundary probe confirms their required NaN-to-zero and signed/unsigned saturation behavior. Trapping conversions first check unordered input and exact lower and upper bounds, then emit the same conversion only after the three checks succeed. Target VCode verifies the conversion shape and requires `InvalidConversionToInteger` metadata on every check; whole-VCode and whole-code-object snapshots lock the bound constants, branch conditions, instruction encodings, and three distinct source-attributed trap sites. Scalar min/max, rounding, copy-sign, fused operations, and vector FP remain open.
- 2026-07-17: Completed scalar floating-point rounding and NaN predicates without widening the semantic operation surface. F32/F64 ceil, floor, truncate, and nearest select to verified target unary operations and emit `FRINTP`, `FRINTM`, `FRINTZ`, and ties-to-even `FRINTN`. Ordered and unordered comparisons share the existing `FCMP` result path but use the architectural `VC` and `VS` conditions, preserving the required NaN distinction. Whole-VCode and whole-code-object snapshots cover both precisions, all four rounding modes, both predicates, allocation across the shared FP/vector bank, and exact instruction bytes. Scalar min/max, copy-sign, fused operations, and vector FP remain open.
- 2026-07-17: Completed scalar F32/F64 min, max, and copy-sign. Native Arm64 probes verify that `FMIN` and `FMAX` select negative and positive zero respectively for either operand order, so the direct instructions satisfy the Wasm signed-zero rule while retaining the architecture's NaN behavior. Copy-sign is deliberately bitwise: reserved `x16` and `v16` materialize the precision-specific sign mask, then `BSL` selects only the sign bit from the second operand and every other bit from the first. Native probes preserve representative F32/F64 NaN payloads exactly, and whole-VCode/code-object snapshots lock the typed operation shapes and emitted sequence. Scalar fused operations and vector FP remain open.
- 2026-07-17: Completed all scalar F32/F64 fused ternary operations with explicit target names and operand order. Native Arm64 execution confirms the architecture's non-obvious accumulator convention: `FMADD` computes `a*b+c`, `FMSUB` computes `c-a*b`, `FNMSUB` computes `a*b-c`, and `FNMADD` computes `-(a*b)-c`. The selector therefore maps semantic add, negated-add, subtract, and negated-subtract to those four instructions in that order rather than matching names mechanically. Target VCode verifies three equal float inputs and one equal result; whole-VCode/code-object snapshots lock every semantic mapping, F32/F64 encoding, accumulator field, and allocated operand position. The scalar floating-point family is now complete; vector FP remains open.
- 2026-07-17: Completed the remaining pure scalar integer operations for both widths. Count-trailing-zeros legalizes explicitly to `RBIT` plus `CLZ`; rotate-left negates the modulo-masked shift and reuses `RORV`; population count uses the reserved vector scratch for `FMOV`, byte `CNT`, horizontal `ADDV`, and a zero-extending move back to the integer bank. Signed/unsigned I64 high multiply use `SMULH`/`UMULH`, while I32 widens with `SMULL`/`UMULL` and extracts the high word. Target VCode verifies every intermediate and pseudo shape independently, and whole-VCode/code-object snapshots lock both widths, signednesses, shared FP scratch use, and exact sequences. Multi-result overflow arithmetic remains open as a separate flag-sensitive slice.
- 2026-07-17: Added the production scalar carrier and selection slice. Pointer and GC-reference nulls have a typed target materialization instead of pretending to be I64 constants; `GcRef64`-to-`Ptr64` and I64-to-`GcRef64` bit transfers are explicit `CarrierMove` operations whose verifier rejects every other pairing. Reference equality independently accepts only matching pointer or GC-reference carriers. Scalar select verifies an I32 condition plus equal values and emits width-correct `CSEL` for I32/I64/pointer/reference values or `FCSEL` for F32/F64 after one condition compare. Whole-VCode/code-object snapshots cover every carrier and scalar register bank. V128 select remains structurally unsupported until the vector slice.
- 2026-07-17: Added embedding-neutral environment and symbol addresses. `LoweringContext` now owns immutable field-to-offset-path data and replacement semantics; no product field names, callback closures, or runtime resolver enter target VCode. Environment access expands during selection into verified `Ptr64` intermediate loads and one typed final load, rejecting absent, empty, negative, or vector paths structurally. Code, external, and data addresses retain distinct target identities and emit `ADRP` plus `ADD` with paired `AArch64Page21`/`AArch64PageOffset12` relocations. Whole-VCode/code-object snapshots lock path types, offsets, relocation targets, instruction offsets, and result ABI placement. Semantic stack objects remain open for the dedicated frame-layout slice.
- 2026-07-17: Added semantic stack objects as a dedicated AArch64 frame area rather than conflating them with regalloc spill slots. Selection derives one aligned function-wide layout and records verified per-object offset, size, and alignment descriptors in target VCode. Frame planning places the area after outgoing arguments and before spills and saved registers, rejects inconsistent or overlapping target descriptors, and exposes only validated final SP offsets to emission. Address emission uses immediate `ADD` forms when possible and materializes arbitrary larger offsets through reserved `x16`; prologue and epilogue SP adjustment now split large frames across shifted and unshifted immediates instead of imposing the old 4095-byte ceiling. Whole-VCode, frame, code-object, and negative snapshots cover aligned holes, coexistence with outgoing arguments, spills, callee saves, large frames, and overlap rejection. Overflow arithmetic, atomics, vectors, GC and exception operations, extra return areas, and shadow parity remain open.
- 2026-07-17: Completed multi-result integer overflow arithmetic for I32 and I64 add, subtract, and multiply under both signed and unsigned interpretation. Add and subtract emit flag-setting instructions followed immediately by the matching `VS`, carry-set, or carry-clear materialization. Multiply computes the full-width high half and compares it with the low half's required sign or zero extension; this avoids treating signed and unsigned overflow as the same condition. The target verifier requires exactly two matching integer operands and `[integer, i32]` results, while allocation keeps both definitions distinct. Whole-VCode, whole-code-object, and exhaustive encoding snapshots cover all twelve combinations, and every raw encoding sequence was cross-checked against the system AArch64 assembler. Atomics, vectors, GC and exception operations, extra return areas, and shadow parity remain open.
- 2026-07-17: Established the AArch64 vector foundation without reintroducing a cross-target opcode union. Target VCode now represents raw V128 constants, integer and floating splats for every lane shape, all six bitwise operations, and scalar-condition V128 select with independent shape verification. Constant materialization mechanically reuses the proven `MOVI` plus GPR-to-lane insertion sequence. Bitselect handles destination aliasing with the mask, second input, or neither input without fixed vector temporaries; V128 select uses a three-instruction alias-aware move and local `CBZ`/`CBNZ` sequence. The emission hot path uses fixed register arguments rather than allocating operand arrays or closures. Whole-VCode, whole-code-object, exhaustive alias/encoding, and malformed-bank snapshots cover the slice, and its NEON plus branch encodings were cross-checked against the system AArch64 assembler. Lane extraction/replacement, shuffle/swizzle, vector arithmetic, comparisons, predicates, conversions, relaxed SIMD, and vector memory remain open.
- 2026-07-17: Completed vector lane extraction and replacement for all integer and floating lane shapes. Narrow integer extraction requires explicit signedness and emits `SMOV` or `UMOV`; I32/I64 extraction uses `UMOV`, while F32/F64 extraction stays in the shared FP/vector bank through scalar `DUP`. Replacement is a destructive `INS`, so target VCode makes the result explicitly tied to the source-vector operand and independently rejects malformed untied forms before allocation. Whole-VCode, allocation, whole-code-object, and exhaustive boundary-lane byte snapshots cover all six lane shapes; all fourteen emitted `SMOV`, `UMOV`, `DUP`, and `INS` forms were cross-checked against the system AArch64 assembler. Shuffle/swizzle, vector arithmetic, comparisons, predicates, conversions, relaxed SIMD, and vector memory remain open.
- 2026-07-17: Completed vector shuffle and swizzle with target-owned table lookup sequences. Runtime swizzle maps directly to one `TBL`; constant two-input shuffle builds two byte-index vectors in reserved `v16` and `v17`, performs independent single-register `TBL` lookups, and combines the disjoint results with `ORR`. This avoids the consecutive-register constraint of `TBL2`, exposes no artificial temporary SSA values to regalloc, and remains safe for every allocatable input/result alias. The target verifier independently enforces the 16-byte mask and 0-31 lane range. Whole-VCode, malformed-mask, helper-byte, and whole-code-object snapshots cover the slice, and every `TBL`/`ORR` form was cross-checked against the system AArch64 assembler. Vector arithmetic, comparisons, predicates, conversions, relaxed SIMD, and vector memory remain open.
- 2026-07-17: Added the direct basic integer-vector arithmetic subset: wrapping add/subtract and absolute/negate for I8x16, I16x8, I32x4, and I64x2. Target VCode uses closed AArch64 operation enums rather than admitting every semantic integer operation before its legalization exists, and its verifier independently rejects floating lane shapes. Emission selects the exact NEON arrangement bits from the semantic lane type and returns a structured vector-emission error for malformed target input. Whole-VCode, malformed-lane, exhaustive byte, and whole-code-object snapshots cover all sixteen forms; every encoding was cross-checked against the system AArch64 assembler. Population count, multiply, min/max, average, saturating and widening arithmetic, shifts, comparisons, predicates, conversions, relaxed SIMD, and vector memory remain open.
- 2026-07-17: Completed integer-vector shifts for all four lane widths and left, signed-right, and unsigned-right semantics. The target instruction retains the scalar I32 shift operand; emission masks it modulo the lane width with `UBFX`, negates the count for right shifts, broadcasts it through reserved `v16`, and selects `SSHL` or `USHL`. This keeps the legalization inside AArch64, uses no allocatable temporary or heap-built operand list, and handles I64 negative counts with a full-width GPR operation. Whole-VCode, exhaustive twelve-form byte, and whole-code-object snapshots cover the slice; every `UBFX`, `DUP`, `SSHL`, `USHL`, and negate encoding was cross-checked against the system AArch64 assembler. Population count, multiply, min/max, average, saturating and widening arithmetic, comparisons, predicates, conversions, relaxed SIMD, and vector memory remain open.
- 2026-07-17: Completed the full integer-vector comparison matrix for all four lane widths: equality, inequality, signed/unsigned less-than, less-or-equal, greater-than, and greater-or-equal. Less relations deliberately swap operands and reuse AArch64 greater relations; inequality emits `CMEQ` followed by in-place `NOT`; signed and unsigned orderings select `CMGT`/`CMGE` or `CMHI`/`CMHS`. The result remains the required per-lane all-zero/all-one mask. Whole-VCode covers all forty semantic combinations, exhaustive byte snapshots cover all emitted forms, and a whole code object covers inversion, operand swapping, unsigned and signed relations. Representative arrangement and relation encodings were cross-checked against the system AArch64 assembler. Population count, multiply, min/max, average, saturating and widening arithmetic, predicates, conversions, relaxed SIMD, and vector memory remain open.
- 2026-07-17: Completed wrapping integer-vector multiplication for every lane width. Contrary to the legacy backend's stale abort, the current AArch64 assembler accepts direct `MUL .16B`; I8x16, I16x8, and I32x4 therefore use one NEON instruction. I64x2 has no vector multiply, so emission first preserves both inputs in reserved `v16`/`v17`, then extracts each 64-bit lane to `x16`/`x17`, computes the modular low product with scalar `MUL`, and inserts it into the destination. This sequence is safe for every allocatable input/result alias and exposes no fake SSA temporary. Whole-VCode, exhaustive helper-byte, and whole-code-object snapshots cover direct and legalized forms; all thirteen machine instructions were cross-checked against the system AArch64 assembler. Population count, min/max, average, saturating and widening arithmetic, predicates, conversions, relaxed SIMD, and vector memory remain open.
- 2026-07-17: Completed signed and unsigned saturating vector add/subtract for I8x16, I16x8, I32x4, and I64x2. All sixteen semantic combinations map directly to `SQADD`, `UQADD`, `SQSUB`, or `UQSUB`, with the lane arrangement selected only in the AArch64 emitter. Whole-VCode and exhaustive byte snapshots cover the full matrix, while a whole code object crosses operation, signedness, and lane widths. All sixteen raw encodings were cross-checked against the system AArch64 assembler. Population count, min/max, average, widening arithmetic, predicates, conversions, relaxed SIMD, and vector memory remain open.
- 2026-07-17: Completed signed and unsigned vector min/max for every integer lane width. I8x16, I16x8, and I32x4 use direct `SMIN`/`UMIN`/`SMAX`/`UMAX`. Because AArch64 has no corresponding I64x2 instructions, that lane emits a signed or unsigned greater-than mask and feeds it into the existing alias-safe bitselect sequence, selecting the correct operand for min or max without exposing target temporaries to regalloc. Whole-VCode and helper-byte snapshots cover all sixteen combinations, and a whole code object exercises direct plus signed/unsigned I64 legalization. All direct instructions and the I64 compare/select sequence were cross-checked against the system AArch64 assembler. Population count, average, widening arithmetic, predicates, conversions, relaxed SIMD, and vector memory remain open.
- 2026-07-17: Completed unsigned rounded vector average and corrected its semantic boundary. MachV now rejects I32x4 and I64x2 forms because MilkIR and WebAssembly define this operation only for I8x16 and I16x8; the target VCode independently enforces the same closed contract. Both legal forms lower directly to `URHADD`, with whole semantic-error, VCode, helper-byte, and code-object snapshots. Their encodings were cross-checked against the system AArch64 assembler. Population count, widening arithmetic, predicates, conversions, relaxed SIMD, and vector memory remain open.
- 2026-07-17: Completed vector population count and corrected its semantic boundary. MachV and target VCode now both reject every lane shape except I8x16, matching the MilkIR and WebAssembly contract instead of representing operations the backend cannot implement. The legal operation lowers directly to `CNT .16B`; whole semantic-error, target-error, VCode, helper-byte, and code-object snapshots cover the slice, and the encoding was cross-checked against the system AArch64 assembler. Widening arithmetic, predicates, conversions, relaxed SIMD, and vector memory remain open.

## Close Notes
