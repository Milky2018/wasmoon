# ISS-175: Preserve unsigned poll timeout semantics

## Metadata
- Type: bug
- Status: open
- Priority: 1
- Labels: wasi, wasip1, poll, interpreter, jit, correctness, tests, agent
- Assignee: unassigned
- Created: 2026-07-15
- Updated: 2026-07-15
- External ref: none

## Description
WASIp1 defines `subscription_clock.timeout` as an unsigned 64-bit nanosecond count, but the interpreter and JIT `poll_oneoff` implementations load and compare it as signed `Int64`/`int64_t`. Values at or above `2^63` are therefore treated as negative durations. In a two-clock probe containing a relative `2^63` timeout and an immediate timeout, Wasmoon emits both events while Wasmtime emits only the immediate event.

The single-relative-clock fast path has a related failure mode: a timeout with the high bit set fails the `timeout > 0` check and completes immediately instead of representing a far-future duration.

## Design
Preserve all 64 timeout bits through decoding, ordering, remaining-duration calculation, and expiration checks. The implementation must avoid actually sleeping for impractically large durations in tests; use a mixed-subscription readiness case to verify ordering. Choose the concrete arithmetic representation after reviewing interpreter/JIT sharing and host clock limits.

## Acceptance Criteria
- [ ] Relative and absolute timeout calculations preserve the full WASIp1 `u64` domain.
- [ ] A far-future `2^63` timer is not reported when an immediate timer makes the same call return.
- [ ] Interpreter and JIT produce the same event set at the `2^63 - 1`, `2^63`, and `u64::MAX` boundaries.
- [ ] Host-duration conversion handles unrepresentable waits without signed wraparound or premature readiness.
- [ ] `myspec/wasip1/poll.wast` and targeted interpreter/JIT WASI tests pass.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-174
- Discovered from: none

## Notes
- 2026-07-15: Differential probe: Wasmtime 40 returned only the immediate event for a relative `2^63` timer paired with a zero-duration timer; Wasmoon returned both. The regression is recorded as Test 11 in `myspec/wasip1/poll.wast`.

## Close Notes
