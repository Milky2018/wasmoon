# ISS-204: Support x64 internal-call extra-results areas

## Metadata
- Type: bug
- Status: closed
- Priority: 1
- Labels: x64, amd64, abi, multi-value, jit, correctness, agent
- Assignee: codex
- Created: 2026-07-18
- Updated: 2026-07-18
- External ref: none

## Description

The production AMD64 JIT rejects a valid internal call when its results exceed the register-return capacity. A function returning nine `i32` values fails with `UnsupportedOperation(message="internal call requires an extra-results area")`, while the interpreter returns all nine values.

This was exposed by the AArch64 result-area regression test added while fixing reversed address/value operands. It is an existing x64 backend gap, not a regression in the AArch64 emitter.

## Design

Implement the extra-results area in the target-owned x64 call ABI rather than deepening the legacy backend. Model result-area allocation, pointer passing, callee stores, caller loads, forwarding, and tail-call constraints through the x64 Target VCode pipeline and its verifier.

## Acceptance Criteria
- [x] Direct and indirect internal calls support results beyond the x64 register-return capacity.
- [x] Callees store overflow results through a valid extra-results pointer and callers load them with the correct types and offsets.
- [x] Tail calls forward a compatible result area and verifier tests lock the pointer contract.
- [x] A JIT/interpreter differential test returns nine `i32` values on AMD64.
- [x] Mixed integer, floating-point, reference, and vector overflow results have target-owned ABI tests.
- [x] Ubuntu AMD64 native, WAST, component, and sanitizer gates pass.

## Relationships
- Depends on: ISS-199
- Parent: none
- Related: ISS-200, ISS-201
- Discovered from: ISS-199

## Notes
- 2026-07-18: PR #436 CI reproduced the gap on Ubuntu AMD64 while the AArch64-specific test passed locally and on macOS.
- 2026-07-18: Implemented x64 result-area planning, frame reservation, pointer passing, callee stores, caller loads, and tail-call forwarding. The nine-result differential test now runs on both native targets, and an indirect-call variant was added; Ubuntu AMD64 execution remains the closing gate.
- 2026-07-18: The first final-pipeline CI run progressed past result-area lowering but exposed a separate x64 installation bug: external `call rel32` targets beyond 2 GiB had no veneer. The target linker now emits deduplicated absolute-jump veneers and carries their offsets through JIT/Cwasm fixups; the large-function-index regression remains the AMD64 execution gate.
- 2026-07-18: The nine-`i32` direct and indirect differential regressions pass on AMD64. PR #436 CI run 29647269153 passed the Ubuntu AMD64 native/component/WAST and sanitizer gates.

## Close Notes

x64 internal calls now allocate, pass, forward, store, and reload extra-results areas consistently for direct, indirect, and tail-call paths, with mixed-type ABI coverage and native AMD64 execution evidence.
