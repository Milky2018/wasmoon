# ISS-197: Establish Target VCode and Allocation seams

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: machv, target-vcode, regalloc, verifier, architecture, agent
- Assignee: codex
- Created: 2026-07-16
- Updated: 2026-07-17
- External ref: none

## Description

Create the typed target VCode, allocation, operand-side-table, register-allocation adapter, and verifier boundaries that both AArch64 and x64 will use, without switching either production target.

## Design

- Target VCode is selected, target-specific code before allocation; Allocation remains separate data.
- Stable instruction handles and program points survive layout and allocation.
- Operand constraints, clobbers, fixed and tied operands, edge moves, stack slots, and safepoints are explicit side data rather than encoded by mutable opcode conventions.
- Target packages share interfaces and invariants, not a union of target opcodes.

## Acceptance Criteria

- [x] AArch64 and x64 VCode instruction types are statically distinct and cannot be mixed.
- [x] Stable instruction handles and program points remain valid across layout, allocation, spill insertion, and metadata production.
- [x] Allocation is represented separately from target VCode and can be verified against operand constraints.
- [x] The regalloc adapter and checker cover fixed, tied, clobbered, spill, reload, edge-move, stack-slot, and safepoint cases.
- [x] Verifier checkpoints exist for selected VCode, allocated VCode, frame planning, and final emission input.
- [x] Neither production target is switched by this issue.
- [x] The common ISS-192 quality gates pass.

## Relationships
- Depends on: ISS-196
- Parent: ISS-180
- Related: none
- Discovered from: ISS-192

## Notes

- Target-specific instruction richness is allowed when it improves code quality; the shared seam must not force an abstract universal instruction set.
- 2026-07-17: Claimed for implementation on `milky/improve-machv`. The first red tests will lock static AArch64/x64 instruction separation, stable handle identity, independent Allocation data, and the selected/allocated/frame/emission verifier checkpoints before either production target is switched.

## Close Notes

- 2026-07-17: Added `Milky2018/machv/vcode`, a generic dense SSA/CFG shell parameterized by a target-owned instruction type. AArch64 and x64 now expose closed, statically distinct instruction types and target legality verifiers without a shared target opcode union.
- 2026-07-17: Stable function-owned value, block, instruction, stack-slot, and program-point handles back compact operand, clobber, successor, metadata, layout, and allocation side tables. Semantic `ValueType` remains the single fact source; physical `RegClass` separately models integer, floating-point, and vector register banks so reference clobbers cannot be missed.
- 2026-07-17: Allocation remains separate from unchanged Target VCode and uses dense constant-time value and operand-location lookup. Its verifier covers source binding, classes, fixed and tied constraints, interference, clobbers, spill/reload and register edits, edge parallel moves, stack slots, and safepoint roots. Frame layouts bind both Function and Allocation owners.
- 2026-07-17: Added the `allocate_vcode` adapter over `Milky2018/regalloc`. It projects indexed side-table facts without inspecting target instructions, translates spills and edge moves into stable-point edits, records safepoint root locations, and re-verifies the completed Allocation before success.
- 2026-07-17: Verified with `moon info`, `moon fmt`, `python3 scripts/audit_module_boundaries.py`, `moon check --target native --warn-list '+73'`, `MACHV_REGALLOC_VALIDATION=1 moon test --target native --warn-list '+73'` (2031/2031), and `git diff --check`. No production target was switched.
