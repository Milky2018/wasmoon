# ISS-314: Complete Component Async future and stream state transitions

## Metadata
- Type: bug
- Status: closed
- Priority: 0
- Labels: component-model, async, future, stream, conformance, agent
- Assignee: codex
- Created: 2026-07-28
- Updated: 2026-07-28
- External ref: none

## Description
Future and stream operations do not yet implement all current Component Async
state transitions. Cancellation, empty waits, partial copies, and transfers
within the same component can reach guest `unreachable` assertions.

## Design
Make the future/stream state machine explicit for pending, readable/writable,
closed, cancelled, and transferred endpoints. Preserve partial progress and
wake the owning task exactly once when a transition makes an operation ready.

## Acceptance Criteria
- [x] `async/cancel-stream.wast` passes.
- [x] `async/empty-wait.wast` passes.
- [x] `async/partial-stream-copies.wast` passes.
- [x] `async/same-component-stream-future.wast` passes.
- [x] Existing closed, dropped, cross-task, and zero-length tests remain
  passing in interpreter and JIT modes.

## Relationships
- Depends on: none
- Parent: ISS-298
- Related: ISS-304, ISS-310
- Discovered from: ISS-310

## Notes
- 2026-07-28: Split from the pinned async 0.3 conformance run at
  `d346e5a335b772909ac4c8072f56d7ed7ea49927`.

## Close Notes
- 2026-07-28: Re-ran the pinned snapshot after ISS-316 fixed the CLI
  `--no-jit` flag. All listed cases pass in the real interpreter path, so the
  reported shared state-machine failure was not reproducible.
- 2026-07-28: The JIT-only failures occurred before the future/stream
  intrinsic ran: hostcall slot validation rejected slots on the native fiber
  stack. ISS-317 fixed that boundary. The complete async-0.3 snapshot now
  passes 24/24 files and 153/153 commands in both interpreter and JIT modes.
  No speculative future/stream state-machine rewrite was required.
