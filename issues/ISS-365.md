# ISS-365: Fix AMD64 JIT out-of-bounds traps in large algorithm functions

## Metadata
- Type: bug
- Status: open
- Priority: 0
- Labels: amd64, jit, correctness, memory, algorithms
- Assignee: unassigned
- Created: 2026-07-30
- Updated: 2026-07-30
- External ref: CI run 30547508968

## Description

Native Linux AMD64 collection for ISS-212 successfully compiled
`examples/algorithms/core3.wasm` and
`examples/algorithms/pwhash_scrypt_ll.wasm`, then both executions trapped with
an out-of-bounds memory access. The corresponding workloads execute on the
AArch64 JIT.

The observed failures were:

```text
core3:
JIT Trap: out of bounds memory access
wasm=48 'func_48'+0x2572, caller=49 'func_49'+0x8f7

pwhash_scrypt_ll:
JIT Trap: out of bounds memory access
wasm=37 'func_37'+0x2572, caller=36 'func_36'+0x7ef
```

Both failing functions use the same machine-code offset, which suggests a
shared x64 lowering, allocation, or address-emission defect rather than two
independent workload errors.

## Design

Reproduce on native AMD64, minimize the common failing instruction sequence,
and compare the final allocated VCode and machine code with the interpreter and
AArch64 paths. Preserve bounds-check and trap semantics; do not disable guard
pages, weaken memory validation, or special-case either workload.

## Acceptance Criteria

- [ ] A minimized public-path regression reproduces the shared AMD64 failure.
- [ ] The incorrect address or allocation decision is identified and fixed at
      its general lowering, allocation, or emission boundary.
- [ ] `core3.wasm` and `pwhash_scrypt_ll.wasm` both execute successfully on the
      native AMD64 JIT and agree with the interpreter.
- [ ] Focused x64 tests, native tests, and recursive JIT WAST tests pass.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-212, ISS-221, ISS-261
- Discovered from: ISS-212

## Notes

- 2026-07-30: The schema-v3 metrics files were complete before each trap, so
  the compiler traces remain diagnostic evidence; their failed executions are
  not included in the retained successful-workload trend summary.

## Close Notes
