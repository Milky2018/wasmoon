# ISS-497: Preserve full memory64 offsets in initialization and interpreter accesses

## Metadata
- Type: bug
- Status: closed
- Priority: 1
- Labels: correctness, conformance, runtime
- Assignee: codex
- Created: 2026-09-09
- Updated: 2026-09-09
- External ref: none

## Description
The high-memory misc script passes in the interpreter only because active data initialization and scalar loads both truncate offsets. A low-address contrast control reveals corruption in both engines. Use one checked 64-bit byte-offset path for scalar, SIMD, bulk and initialization operations.

Evidence: `tmp/iss496-high-memory/summary.json` (unmodified upstream inputs).

## Acceptance Criteria
- [x] Reproduce with independent expected results and contrast controls.
- [x] Fix the runtime boundary without changing upstream inputs or masking failures.
- [x] Focused and external regressions pass on both engines.

## Relationships
- Depends on: none
- Parent: ISS-478
- Related: ISS-469
- Discovered from: ISS-478

## Close Notes
- Added a canonical 64-bit byte-offset path for typed memory access; existing 32-bit APIs delegate through explicit address conversion. Interpreter scalar, SIMD, bulk and data initialization retain full offsets until after bounds checks.
- Removed the duplicate integer-only offset evaluator; initialization now uses the shared constant-expression evaluator and checks table offsets before narrowing.
- Reproduction with distinct low/high data failed on both old engines: the interpreter previously hid initialization truncation by truncating loads too. Both now pass. A broader explicit-value test covers scalar, SIMD, memory.init, memory.copy (including cross-memory), memory.fill and unchanged low-address bytes. Matching pinned Wasmtime passes the same fixture.
- Native: 2,372 passed; the new memory64 regression also passes under AddressSanitizer. Core WAST: 258 files / 62,563 assertions per engine. Runtime scripts, all component suites and macOS P1 explicit-rights passed. Warning-73 and module-boundary audit passed.
- Full misc including all high-memory scripts: 564 pass, 68 script-only, 122 fail, 10 unsupported, no deferred cases (`tmp/iss497-all-misc/summary.json`). `memory64/more-than-4gb.wast` now passes both engines. Remaining high-memory failures are ISS-498 through ISS-501; ordinary misc verdicts remain unchanged.
