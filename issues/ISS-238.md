# ISS-238: Cut JITModule over to the transactional installer

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: wasmoon-jit, installer, runtime, rollback, gc, tests, agent
- Assignee: codex
- Created: 2026-07-22
- Updated: 2026-07-22
- External ref: none

## Description

`JITModule::load_with_imports` still allocates imports and functions, mutates the function table, installs GC metadata, and patches code incrementally through the v8 loader.

## Design

Resolve direct imports and build owned hostcall trampolines without publication, then install the verified v9 artifact through `JitCodeInstaller`. Stage the JIT context and every GC/debug/address-range registration, and expose the new module plus function table only after the complete transaction commits. Preserve the previous module on every failure.

## Acceptance Criteria
- [x] `JITModule` consumes verified v9 artifacts and does not allocate or patch function bodies itself.
- [x] Direct imports and generated hostcall trampolines participate in the same rollback lifetime.
- [x] Function-table, GC stack-map, trap/debug, unwind, and address-range state publishes once after all registrations succeed.
- [x] Registration failures are structured and leave no partial module or executable mapping.
- [x] JIT/interpreter regression tests cover direct imports, host imports, calls, GC safepoints, traps, and replacement rollback.

## Relationships
- Depends on: ISS-237
- Parent: ISS-236
- Related: ISS-235
- Discovered from: ISS-236

## Notes
- 2026-07-22: Split from ISS-236 because import-trampoline ownership and JITContext metadata registration form a distinct transaction from artifact production.
- 2026-07-22: Claimed after ISS-237 closed. The cutover will add the v9 load path without forwarding through `PrecompiledModule`; ISS-239 will then switch cache and compile callers, and ISS-240 will delete the temporarily coexisting v8 entry points.
- 2026-07-22: `JITFunction` now carries a non-owning entry pointer while `JITModule` retains either the opaque installed transaction or transitional standalone mappings. This preserves the old test helper without allowing function-body allocation or patching in the v9 loader.
- 2026-07-22: The v9 loader stages direct imports and generated hostcall trampolines against a private context, installs all compiled functions through `JitCodeInstaller`, registers typed code-object safepoints, and only then fills the context function table and publishes lookup/debug/address-range state.

## Close Notes

Closed with `JITModule::load_artifact*` over verified `LoadedArtifact` input. Named runtime helpers and imported `Code("wasm.func.N")` targets resolve symbolically; hostcall trampolines and the context remain local until the function installation and metadata registration complete. Installation and metadata failures are structured, and a failed replacement leaves the previously returned module executable.

Targeted native tests execute installed code, link direct imports, retain generated hostcall trampolines, resolve the Wasmoon runtime whitelist, and exercise rollback. Validation passed with 2321/2321 native tests, strict warning-73 checks for native and default targets, generated-interface review, all three compiler-boundary audits, formatting, and `git diff --check`. The v8 entry points intentionally coexist until the caller/cache cutover in ISS-239 and are deleted by ISS-240.
