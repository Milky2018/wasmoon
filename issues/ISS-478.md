# ISS-478: Complete external test host contracts and full-suite acceptance

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: correctness, conformance, runtime
- Assignee: codex
- Created: 2026-09-09
- Updated: 2026-09-10
- External ref: none

## Description
Implement missing host contracts without no-op substitutes, exercise high-memory inputs, and run all checked-in external conformance lanes without masking failures.

## Acceptance Criteria
- [x] Reproduce the original external failure and identify its cause.
- [x] Fix the runtime or adapter contract and add focused regressions.
- [x] Unmodified external inputs pass on both engines with no regression in existing passing cases.

## Relationships
- Depends on: ISS-497, ISS-498, ISS-499, ISS-500, ISS-501, ISS-502, ISS-503, ISS-505, ISS-506, ISS-507, ISS-508, ISS-509, ISS-510, ISS-511, ISS-476, ISS-477, ISS-485, ISS-516
- Parent: ISS-469
- Related: ISS-469
- Discovered from: ISS-469

## Historical Progress Notes

## Notes
- High-memory sweep at `7f157d53`: five passes and eleven failures across sixteen executions (`tmp/iss496-high-memory/summary.json`). The two big-strings failures are adapter-internal timeouts, not successful executions; the outer runner currently reports them as failures.
- Linux AMD64/macOS ARM64 CI dispatched at `7f157d53`: GitHub Actions run 34342075364 failed on Linux P1; follow-up run 34348217790 passed both platforms.
- ISS-497 complete high-memory-inclusive sweep: 564 pass, 68 script-only, 122 fail, 10 unsupported, zero deferred (`tmp/iss497-all-misc/summary.json`). Do not compare this directly with the 112 failures from runs that deferred high-memory inputs.
- Linux native tests at `7f157d53` passed; full Linux CI is blocked by the four P1 failures recorded in ISS-503.

- CI run 34348217790 at 9b071869 passed all Linux AMD64 and macOS ARM64 gates. Full Linux P1: 116 passes, two intentional not-applicable executions. ISS-499 fixes four high-memory misc executions and ISS-500 fixes two more; a fresh complete misc sweep is still required after the remaining work.

- Full sweep at d099059d: 570 pass, 68 script-only, 116 fail, 10 unsupported, zero deferred (tmp/iss505-all-misc/summary.json). CI run 34353328945 validates this revision; both native jobs passed, remaining gates were still running when recorded.
- ISS-498 component lane: 190 pass, two big-strings failures, four host/config unsupported, zero deferred. No other component misc failures remain.

- CI run 34353328945 at d099059d completed successfully on Linux AMD64 and macOS ARM64.
- ISS-501 full misc sweep: 574 pass, 68 script-only, 112 fail, 10 unsupported, zero timeout/deferred/harness-error (tmp/iss501-all-misc/summary.json). Every high-memory script passes; remaining failures are threads and typed continuations.

- ISS-506 full misc sweep: 576 pass, 68 script-only, 112 fail, eight unsupported, zero deferred. The actual component implements validator switch is now exercised by the formerly excluded fixture.
- NaN configuration audit exposed permissive WAST floating-point assertion matching (ISS-507). Previously green float assertions do not prove exact NaN payload or signed-zero behavior until that issue is resolved.

- ISS-507 repaired scalar/SIMD assertion matching and the 112 newly exposed interpreter float failures. Official core assertions now pass under exact bit matching in both engines. Full misc remains 576 pass, 68 script-only, 112 fail, eight unsupported (tmp/iss507-final-misc/summary.json).
- CI run 34356823409 at 6094b0d1 completed successfully on Linux AMD64 and macOS ARM64, covering the canonical memory64 and string transport fixes.

- ISS-509 implements actual bulk-memory disablement. Full misc is 576 pass, 70 script-only, 112 fail, six unsupported; the remaining host contracts are NaN canonicalization (two files) and concurrent resource capacity (one file), each in both engines.

- ISS-508 adds deterministic WAST resource release and fixes Store.close mutating borrowed AST type arrays. Full external baseline remains unchanged. CI run 34360374301 at dc57451a passed Linux AMD64 and macOS ARM64, validating the strict float matcher and interpreter NaN fixes.

- ISS-510 adds actual engine-wide arithmetic NaN canonicalization. Full misc: 580 pass, 70 script-only, 112 fail, two unsupported capacity executions, zero deferred (tmp/iss510-all-misc/summary.json). All other external gates pass.

- ISS-511 implements the last missing host contract against actual concurrent resource owners. Full misc: 582 pass, 70 script-only, 112 thread/typed-continuation failures, zero unsupported/timeout/deferred/harness errors (`tmp/iss511-all-misc/summary.json`). All other external gates pass. CI run 34366579219 at 55bb08f0 passed Linux AMD64 and macOS ARM64.

- CI run 34405150498 at c74ac176 passed Linux AMD64 and macOS ARM64. ISS-512 through ISS-514 preserve the complete misc baseline while adding the core synchronization and suspension required by ISS-476.

- ISS-515 full misc: 602 pass, 72 script-only, 90 typed-continuation failures, zero unsupported/timeout/deferred/harness errors (`tmp/iss515-final-all-misc/summary.json`). All local core, runtime, component and P1 gates pass. Linux CI run 34418806887 at 03f8b898 failed one SIMD lane assertion; ISS-516 tracks reproduction and repair. This supersedes the prior green platform baseline for that revision.

- ISS-485 closes the source-phase audit with independent pinned controls, correct explicit/implicit quoted modules, and build-before-link assertions. Full misc remains 602/72/90 with no unsupported/deferred cases; official core and other local external lanes pass. Typed continuations remain ISS-477, and ISS-516 awaits native Linux CI confirmation.

## Close Notes

Completed the missing host contracts, strict assertion matching, high-memory execution, threads and typed continuations through the linked implementation issues. The pinned misc fixtures and assertions remain unchanged. Existing documented component-spec corrections remain explicit and follow the user-approved Wasmtime behavior. Native Linux AMD64 and macOS ARM64 CI [34449494701](https://github.com/Milky2018/wasmoon/actions/runs/34449494701) passed at 1a9a4ced. Each platform passed 2,450 native tests; all 764 misc executions including high-memory inputs (692 assertion-bearing passes and 72 successfully executed assertion-free scripts); core WAST with 62,563 assertions per engine; stable, async and future-gated component suites in both engines; and explicit-rights P1 with 116 passes. The only two not-applicable P1 executions are the agreed p1_cli_hostcall_fuel case, once per engine. No misc failures, unsupported cases, timeouts, deferrals or harness errors remain.
