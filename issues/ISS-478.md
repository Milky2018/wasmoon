# ISS-478: Complete external test host contracts and full-suite acceptance

## Metadata
- Type: task
- Status: in_progress
- Priority: 1
- Labels: correctness, conformance, runtime
- Assignee: codex
- Created: 2026-09-09
- Updated: 2026-09-09
- External ref: none

## Description
Implement missing host contracts without no-op substitutes, exercise high-memory inputs, and run all checked-in external conformance lanes without masking failures.

## Acceptance Criteria
- [ ] Reproduce the original external failure and identify its cause.
- [ ] Fix the runtime or adapter contract and add focused regressions.
- [ ] Unmodified external inputs pass on both engines with no regression in existing passing cases.

## Relationships
- Depends on: ISS-497, ISS-498, ISS-499, ISS-500, ISS-501, ISS-502, ISS-503, ISS-505, ISS-506, ISS-507, ISS-508, ISS-509
- Parent: ISS-469
- Related: ISS-469
- Discovered from: ISS-469

## Close Notes

## Notes
- High-memory sweep at `7f157d53`: five passes and eleven failures across sixteen executions (`tmp/iss496-high-memory/summary.json`). The two big-strings failures are adapter-internal timeouts, not successful executions; the outer runner currently reports them as failures.
- Linux AMD64/macOS ARM64 CI dispatched at `7f157d53`: GitHub Actions run 34342075364 failed on Linux P1; follow-up run 34348217790 passed both platforms.
- ISS-497 complete high-memory-inclusive sweep: 564 pass, 68 script-only, 122 fail, 10 unsupported, zero deferred (`tmp/iss497-all-misc/summary.json`). Do not compare this directly with the 112 failures from runs that deferred high-memory inputs.
- Linux native tests at `7f157d53` passed; full Linux CI is blocked by the four P1 failures recorded in ISS-503.

- CI run 34348217790 at 9b071869 passed all Linux AMD64 and macOS ARM64 gates. Full Linux P1: 116 passes, two intentional not-applicable executions. ISS-499 fixes four high-memory misc executions and ISS-500 fixes two more; a fresh complete misc sweep is still required after the remaining work.

- Full sweep at d099059d: 570 pass, 68 script-only, 116 fail, 10 unsupported, zero deferred (tmp/iss505-all-misc/summary.json). CI run 34353328945 validates this revision; both native jobs passed, remaining gates were still running when recorded.
- ISS-498 component lane: 190 pass, two big-strings failures, four host/config unsupported, zero deferred. No other component misc failures remain.

- CI run 34353328945 at d099059d completed successfully on Linux AMD64 and macOS ARM64.
- ISS-501 full misc sweep: 574 pass, 68 script-only, 112 fail, 10 unsupported, zero timeout/deferred/harness-error (tmp/iss501-all-misc/summary.json). Every high-memory script passes; remaining failures are threads and typed continuations.

- ISS-506 full misc sweep: 576 pass, 68 script-only, 112 fail, eight unsupported, zero deferred. The actual component implements validator switch is now exercised by the formerly excluded fixture.
- NaN configuration audit exposed permissive WAST floating-point assertion matching (ISS-507). Previously green float assertions do not prove exact NaN payload or signed-zero behavior until that issue is resolved.

- ISS-507 repaired scalar/SIMD assertion matching and the 112 newly exposed interpreter float failures. Official core assertions now pass under exact bit matching in both engines. Full misc remains 576 pass, 68 script-only, 112 fail, eight unsupported (tmp/iss507-final-misc/summary.json).
- CI run 34356823409 at 6094b0d1 completed successfully on Linux AMD64 and macOS ARM64, covering the canonical memory64 and string transport fixes.

- ISS-509 implements actual bulk-memory disablement. Full misc is 576 pass, 70 script-only, 112 fail, six unsupported; the remaining host contracts are NaN canonicalization (two files) and concurrent resource capacity (one file), each in both engines.
