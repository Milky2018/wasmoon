# ISS-046: Remove legacy wording from JIT value conversion comments

## Metadata
- Type: chore
- Status: closed
- Priority: 3
- Labels: wasmoon, testsuite, comments, cleanup, migration, agent
- Assignee: unassigned
- Created: 2026-07-06
- Updated: 2026-07-06
- External ref: none

## Description
`modules/wasmoon/testsuite/compare.mbt` comments `value_to_jit_arg` as `Convert @types.Value to Int64 for JIT call (legacy)`. The helper is still part of the test comparison support, so the `legacy` wording makes live test support look like historical compatibility code.

## Design
Reword the comment only. Prefer wording such as `default untyped JIT argument conversion` if that matches the current helper role. Do not change conversion behavior or test semantics.

## Acceptance Criteria
- [x] `value_to_jit_arg` is no longer documented with `legacy` wording.
- [x] The new comment distinguishes the helper from typed/multi-slot JIT argument conversion.
- [x] No behavior changes are included.
- [x] `git diff --check` passes.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-042
- Discovered from: ISS-042

## Notes
- 2026-07-06: Split from ISS-042 after discussion of remaining source-facing `legacy` wording.

## Close Notes
- 2026-07-06: Reworded `value_to_jit_arg` as the default single-slot JIT argument encoding helper for call sites without static parameter type context. No conversion behavior changed.
- Verification: `git diff --check`; `moon check --target native --warn-list +73 --diagnostic-limit=200`.
