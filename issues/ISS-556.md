# ISS-556: Require warning-free native builds

## Metadata
- Type: chore
- Status: in_progress
- Priority: 1
- Labels: ci, warnings
- Assignee: codex
- Created: 2026-09-17
- Updated: 2026-09-17
- External ref: none

## Description
Clean all diagnostics enabled by the standard native check plus warning 73. Require warnings to fail CI on Linux, macOS and Windows, and the local pre-commit hook. Preserve runtime behavior and upstream WIT snapshots.

## Acceptance Criteria
- [x] Native check with `--warn-list +73 --deny-warn` succeeds without warnings.
- [x] Full native tests and HTTP dual-engine integration pass.
- [ ] CI rejects warnings on all supported native platforms.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-554
- Discovered from: ISS-549

## Close Notes
Local verification: 2,522 native tests passed; strict native check and generated interfaces emitted no warnings. An isolated unused-function fixture exits 0 without `--deny-warn` and 255 with it. HTTP integration passes both engines. All three Component Model suites pass both engines (1,385 commands per engine). CI acceptance is pending.

The TLS test peer intentionally uses the pinned async dependency's test-only server constructor. Its `internal` alert is acknowledged only on the two platform-specific test helper functions; production code and CI retain the full warning policy.
