# ISS-486: Enforce stream handle ownership without numeric guessing

## Metadata
- Type: bug
- Status: closed
- Priority: 1
- Labels: correctness, runtime, component
- Assignee: codex
- Created: 2026-09-09
- Updated: 2026-09-09
- External ref: none

## Description
The stream.read adapter searches all component instances for a readable endpoint with the same numeric handle when the current instance has no such endpoint, then transfers that endpoint implicitly. Numeric handles are local to their owning component; this workaround can bypass canonical ownership transfer. Reproduce with an integer passed between components, remove guessing, and fix any real transfer path that depended on it.

## Acceptance Criteria
- [x] An independent cross-component invalid-handle regression traps in Wasmoon and Wasmtime.
- [x] Stream lookup uses the current instance only; legitimate transfers use the canonical ownership path.
- [x] Existing component and misc corpora pass without new skips or fixture changes.

## Relationships
- Depends on: none
- Parent: ISS-469
- Related: ISS-484
- Discovered from: ISS-484

## Notes
- 2026-09-09: Independent two-component control passes a raw u32 endpoint index, without a canonical stream transfer, to the second component. Wasmtime 40 reports "handle is not a stream"; Wasmoon unexpectedly returns from the read. The reproduction is /tmp/iss486-repro/handle-isolation.wast.

## Close Notes

Removed the cross-instance numeric-handle search and its now-unused reservation helper. Added public WAST controls for rejected integer-based access and successful canonical stream transfer, independently verified with Wasmtime 40. Native check (+73) has zero warnings and all 2,357 native tests pass. Local component corpus: six files / 50 commands in each engine. Stable: 845 commands; async: 153; future-gated retains exactly the four ISS-484 failures. Misc component scan plus a serial retry covers all 184 passing execution cases with no status regressions. The massive-send cases timed out during parallel compilation, then passed under the unchanged 30-second limit in 4.709s and 4.723s with compilation paused (tmp/iss486-massive-serial). Original reports and upstream fixtures remain unchanged.
