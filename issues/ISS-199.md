# ISS-199: Cut over AArch64 and remove its legacy backend

## Metadata
- Type: task
- Status: in_progress
- Priority: 1
- Labels: aarch64, migration, deletion, jit, agent
- Assignee: codex
- Created: 2026-07-16
- Updated: 2026-07-18
- External ref: none

## Description

Switch production AArch64 compilation to the final Target VCode pipeline and delete the AArch64 branch of the legacy adapter and backend in the same coherent milestone.

## Design

- Production has one AArch64 path and no automatic fallback.
- A new-backend failure is returned as a structured compilation error.
- AMD64 remains on the temporary legacy path until its own cutover.

## Acceptance Criteria

- [ ] All production AArch64 JIT entry points use the final semantic MachV, AArch64 VCode, allocation, frame, emitter, and code-object seams.
- [ ] The AArch64 branch of the semantic-to-legacy adapter and all legacy AArch64 target instructions, selection, allocation glue, and emitter logic are deleted.
- [ ] No runtime flag, fallback, public compatibility API, or hidden retry can select the legacy AArch64 path.
- [ ] macOS AArch64 native, WAST, component, WASI, artifact, and JIT/interpreter differential gates from ISS-193 pass.
- [ ] Relocation, fixup, trap, GC, stack-map, unwind, and normalized-disassembly comparisons meet their acceptance thresholds.
- [ ] AMD64 CI remains green on its unchanged path.
- [ ] The common ISS-192 quality gates pass.

## Relationships
- Depends on: ISS-198
- Parent: ISS-180
- Related: none
- Discovered from: ISS-192

## Notes

- If a cutover gate fails, revert this milestone instead of restoring a production fallback.
- 2026-07-18: Claimed after ISS-198 closed with the complete AArch64 shadow matrix green. The cutover will first route the product-owned AArch64 composition through the final code-object linker and installer, then delete the AArch64 legacy branch and shadow flag in the same milestone. AMD64 remains unchanged.
- 2026-07-18: Production Wasm bodies now compile through semantic MachV, AArch64 Target VCode, allocation, frame planning, emission, and code-object link preparation. Production entry trampolines use the direct AArch64 generator, the shadow flag and dual-run path are deleted, and legacy lowering rejects AArch64 with a structured pipeline error. The remaining work is deleting the unreachable AArch64 implementation from the legacy adapter/backend and running the complete cutover matrix.
- 2026-07-18: The production cutover exposed and fixed three target ABI defects that compile-only shadowing could not detect: caller-owned stack arguments were incorrectly popped, ABI inputs/results were emitted as sequential rather than parallel moves, and the cwasm adapter omitted allocation-root indices. Direct and indirect tail calls that need a larger stack area now return through a cleanup thunk so the original caller SP is restored. The complete native suite passes with allocation validation enabled (2219/2219); legacy AArch64 implementation deletion and the external WAST/component/WASI matrix remain.

## Close Notes
