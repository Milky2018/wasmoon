# ISS-005: Lower WebAssembly into MilkIR through an embedding environment

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: wasm_frontend, milkir, lowering, agent
- Assignee: unassigned
- Created: 2026-07-04
- Updated: 2026-07-04
- External ref: none

## Description
Move Wasm-to-IR translation behind a Wasm Frontend that lowers `wasm_core` modules into MilkIR through an Embedding Environment. WebAssembly-specific operations must lower into ordinary MilkIR constructs rather than MilkIR-specific Wasm opcodes.

## Design
Model the boundary after `cranelift-wasm`: the frontend owns Wasm stack/control translation, while the embedding environment resolves memories, tables, globals, references, runtime calls, hidden parameters, external symbols, traps, and metadata. Wasm heap types and canonical type information remain frontend/runtime concerns and lower to generic carriers plus metadata.

## Acceptance Criteria
- [x] Wasm functions can be translated into MilkIR functions through an embedding environment interface.
- [x] `memory.*`, `table.*`, `global.*`, ref/GC, exception, and runtime helper operations are not introduced as core MilkIR opcodes.
- [x] Existing Wasm correctness tests still pass through the updated lowering path or have follow-up issues.
- [x] Known incomplete lowering cases include scoped `TODO(milkir)` or `TODO(wasm_frontend)` comments.

## Relationships
- Depends on: ISS-003, ISS-004
- Parent: ISS-001
- Related: none
- Discovered from: none

## Notes
- 2026-07-04: Runtime participation is allowed during lowering, but the emitted MilkIR must remain generic.

## Close Notes
Closed after adding `modules/wasmoon/wasm_frontend`, with an `EmbeddingEnvironment`, Wasm value-type to MilkIR type mapping, function signature lowering, function skeleton lowering, and tests for empty functions, unsupported-body trap lowering, and missing functions. Unsupported instruction-body lowering is explicit through `TODO(wasm_frontend)` comments and generic MilkIR trap constructs; no Wasm-specific MilkIR opcodes were added. Validation passed: `moon fmt`; `moon check --target native --warn-list +73 --diagnostic-limit=200`; `moon test --target native --warn-list +73 --diagnostic-limit=200` (1939 passed); `moon info`.
