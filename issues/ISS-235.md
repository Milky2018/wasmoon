# ISS-235: Install JIT code transactionally

## Metadata
- Type: task
- Status: open
- Priority: 1
- Labels: wasmoon-jit, installer, executable-memory, rollback, code-object, tests, agent
- Assignee: unassigned
- Created: 2026-07-22
- Updated: 2026-07-22
- External ref: none

## Description

`JITModule::load_with_imports` allocates executable code and publishes function-table entries incrementally, then invokes patch helpers that can abort. A later allocation, relocation, GC, unwind, or registration failure can therefore expose a partially installed module.

## Design

Implement the Wasmoon-owned `JitCodeInstaller` transaction selected by ISS-190. Validate and resolve all inputs before allocation, stage writable mappings and metadata, patch symbolically, register address-dependent metadata, transition to executable memory, and publish entry points only at commit. Return opaque installed-code ownership and roll back mappings and registrations on every failure.

## Acceptance Criteria
- [ ] Installation returns structured resolution, allocation, relocation, protection, registration, and publication errors without aborting.
- [ ] No function-table or module entry becomes visible before the full transaction commits.
- [ ] Failure injection at each phase proves rollback and preserves any previous installation.
- [ ] Live JIT objects and decoded artifacts enter the same installer seam and exhibit equivalent behavior.
- [ ] Executable mappings follow platform RW-to-RX/MAP_JIT policy and flush instruction caches before publication.

## Relationships
- Depends on: ISS-234
- Parent: ISS-202
- Related: ISS-188, ISS-190
- Discovered from: ISS-202

## Notes
- 2026-07-22: The current loader sets import and function pointers while allocation is still in progress and ignores the boolean result of GC safepoint installation.

## Close Notes
