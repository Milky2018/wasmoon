# ISS-196: Cut over semantic MilkIR-to-MachV production

## Metadata
- Type: task
- Status: in_progress
- Priority: 1
- Labels: migration, milkir, wasm, machv, lowering, agent
- Assignee: codex
- Created: 2026-07-16
- Updated: 2026-07-16
- External ref: none

## Description

Make verified semantic MachV the sole production result of MilkIR and Wasm lowering. Use the private semantic-to-legacy adapter only to preserve the downstream backend temporarily, and delete the old direct MilkIR-to-union lowering in the same milestone.

## Design

- Implement target-neutral `milkir_machv` and `wasm_machv` producers.
- Compare the new semantic route with the old route only in tests or diagnostics.
- Keep the semantic-to-legacy adapter private to migration internals and absent from generated interfaces.
- Cut production over atomically; do not retain a fallback or supported backend selector.

## Acceptance Criteria

- [ ] MilkIR and Wasm producers construct verified semantic MachV with matching signatures, CFG edges and arguments, operations, effects, traps, calls, and safepoints.
- [x] A test-only normalization harness compares the old and new semantic behavior across representative and regression inputs.
- [ ] Production JIT compilation consumes semantic MachV and reaches the legacy backend only through the private adapter.
- [ ] The old direct MilkIR-to-union lowering and all of its production entry points are deleted in the cutover.
- [ ] Existing JIT/interpreter differential tests and WAST suites show no new failures.
- [ ] No public API, generated interface, README, or reusable package exposes the adapter or a dual-backend option.
- [ ] The common ISS-192 quality gates pass.

## Relationships
- Depends on: ISS-195
- Parent: ISS-180
- Related: none
- Discovered from: ISS-192

## Notes

- Rollback is the complete producer cutover, not a runtime fallback to old lowering.
- 2026-07-16: Claimed after ISS-195 closed. Implementation will begin with black-box semantic lowering snapshots and a test-only normalized comparison seam, then cut the production producer over atomically while keeping the legacy adapter private and deleting the old direct MilkIR-to-union entry points.
- 2026-07-16: `d5b4c195` added the target-neutral core producer for scalar SSA, typed CFG edges and block parameters, conversions, and high-half multiplication. Core lowering rejects unresolved dialect operations structurally instead of consulting a target or falling back to legacy lowering.
- 2026-07-16: `e5367878` added explicit pointer arithmetic, scalar memory effects, direct Platform calls, indirect Internal calls, and explicit enclosing call protocols. `73daf61d` made lowering follow CFG reverse postorder and computes precise live managed-reference roots for call safepoints.
- 2026-07-16: `365d256c` completed core vector lowering, including normalized integer, floating-point, conversion, relaxed-SIMD, vector-load, and vector-store families. It also converts known textual MilkIR traps into structured semantic MachV reasons and rejects unknown or product-specific trap exits before target lowering. Native warning-73 checks, 1,973 tests with register-allocation validation, the module-boundary audit, formatting, interface generation, and diff checks passed.
- 2026-07-16: `234b0524` preserves MilkIR source labels on every semantic instruction emitted from an original instruction, including explicit pointer conversion and address-calculation expansion, while combining call source metadata with precise live GC roots.
- 2026-07-16: `e0d5803c` implemented the abstract `milkir_machv/adapter` construction seam decided by ISS-191. Dialect adapters now receive verified operands, explicit environment values, source and GC metadata, and safe target-neutral operation, call, control-flow, trap, and tail-call construction without access to MachV storage or target details. The root lowering entry validates one named dialect and requires every extension instruction to complete its result contract.
- 2026-07-16: `f2da634f` established the new reusable `wasm_machv` module and its first vertical producer slice. Direct Wasm calls now lower through the semantic adapter to unresolved internal code symbols with an explicit execution-environment operand and complete logical signatures, protocols, effects, and safepoints. The module is included in the reusable-boundary audit and has no target or Wasmoon product dependency.
- 2026-07-16: `6035e42d` added direct tail calls and callable-reference ordinary and tail calls. Tail-call extensions explicitly replace MilkIR placeholder terminators, tagged callable references are normalized to indirect pointers through target-neutral operations, and dialect safepoints receive precise managed-reference roots rather than relying on core `CallOp` classification.
- 2026-07-16: `7e16f972` added target-neutral reference primitives: explicit GC-reference bit construction for i31 values, representation-preserving extern/any conversions, reference equality, and i31 signed/unsigned extraction with ordinary branch-to-trap CFG rather than target trap pseudo-operations.
- 2026-07-16: `009b5e73` corrected the dialect environment contract to select and semantically type an existing MilkIR parameter instead of inserting a duplicate hidden context. Wasm now contracts parameter zero from its legacy I64 carrier to MachV `Ptr64`. The same slice added opaque typed embedding-field identities and lowers memory-base access as a target-neutral environment read without exposing VMContext byte offsets.
- 2026-07-16: `d5a5596c` lowered `get_func_ref` through an opaque function-table field, explicit target-neutral pointer arithmetic and typed load, and visible funcref tagging. No target addressing mode, fixed register, or concrete runtime offset enters the Wasm producer.
- 2026-07-16: `b55a6548` added ordinary and tail `call_indirect` lowering for both table32 and table64 indices. It computes entry addresses without truncating the index, loads typed entry fields, fast-paths canonical exact type matches, calls an opaque Platform subtype-check capability only on mismatch, untags the function pointer explicitly, preserves GC roots, and replaces tail-call placeholder traps.
- 2026-07-16: `13efbc9a` generalized product runtime identities behind the Wasm environment and lowered typed exception-state queries plus `try_table_end` as explicit Platform capability calls. Non-GC runtime calls deliberately omit root metadata, while their memory and trap behavior remains explicit.
- 2026-07-16: `570a5685` lowered `ref.test`, `ref.cast`, and `array.len` as typed Platform capabilities. Their signatures retain managed-reference types, the calls are explicitly non-safepoint operations, and their read/trap summaries match the current runtime ABI. Native warning-73 checks, 1,987 tests with register-allocation validation, the module-boundary audit, formatting, interface generation, and diff checks passed.
- 2026-07-16: `00c3eed9` lowered scalar and packed struct/array get and set operations through an explicit 64-bit runtime-word ABI. Float and managed-reference values now cross visible bit-conversion operations, packed values use explicit 8/16-bit extension, and unsupported V128 aggregate slots return a structured lowering error instead of selecting the wrong register class. Native warning-73 checks, 1,989 tests with register-allocation validation, the module-boundary audit, formatting, interface generation, and diff checks passed.
- 2026-07-16: `d436c903` lowered `array.fill` and `array.copy` as explicit read-write, trapping, non-safepoint Platform calls. Fill values cross the same typed runtime-word encoding seam as aggregate set operations, and reference operands remain visible as managed references. Native warning-73 checks, 1,990 tests with register-allocation validation, the module-boundary audit, formatting, interface generation, and diff checks passed.
- 2026-07-16: `44393aab` lowered initialized/default array allocation and default struct allocation as explicit GC safepoint calls. Semantic signatures contain only logical runtime operands; the code-position-derived safepoint identifier remains a hidden embedding ABI concern. Whole-function snapshots lock changing precise-root sets across consecutive allocations. Native warning-73 checks, 1,991 tests with register-allocation validation, the module-boundary audit, formatting, interface generation, and diff checks passed.
- 2026-07-16: `7e210097` lowered data/element segment array allocation and initialization with explicit type and segment identities. Allocation capabilities are semantic GC safepoints with precise roots, while initialization remains a trapping read-write call. The current product C helpers must be upgraded to honor this allocation contract before production cutover. Native warning-73 checks, 1,992 tests with register-allocation validation, the module-boundary audit, formatting, interface generation, and diff checks passed.
- 2026-07-16: `2fef08b9` added the first producer-required address-visible semantic stack object. Functions own opaque object handles and expose only size/alignment facts plus `stack.address`; target stack pointer, frame offset, outgoing-area, and register details remain absent. Builder and verifier boundaries reject foreign objects and invalid layouts. Native warning-73 checks, 1,995 tests with register-allocation validation, the module-boundary audit, formatting, interface generation, and diff checks passed.
- 2026-07-16: `02584c7f` lowered fixed struct and array allocation through typed address-visible value buffers. Each scalar/reference value crosses explicit runtime-word encoding and ordinary semantic stores; allocation calls retain precise original SSA roots. Unsupported V128 aggregate values fail structurally before buffer construction. Native warning-73 checks, 1,997 tests with register-allocation validation, the module-boundary audit, formatting, interface generation, and diff checks passed.
- 2026-07-16: `35c4f028` lowered exception local snapshots through the same address-visible runtime-word buffer seam. The producer no longer assumes that the current stack pointer aliases a preallocated outgoing area; target frame planning will assign the object. Native warning-73 checks, 1,998 tests with register-allocation validation, the module-boundary audit, formatting, interface generation, and diff checks passed.
- 2026-07-16: `65599e06` added first-class semantic noreturn calls as CFG terminators. They carry the same typed call, behavior, source, and optional root contracts as other calls but have no successor and cannot declare results. Dialect adapters can now replace source placeholder traps without inventing a second trap site. Native warning-73 checks, 2,000 tests with register-allocation validation, the module-boundary audit, formatting, interface generation, and diff checks passed.
- 2026-07-16: `4cd647d9` lowered `throw`, `throw_ref`, and `delegate` through semantic noreturn calls. Payload throws use address-visible runtime-word buffers, source placeholder traps are replaced, and non-GC transfers omit root metadata while retaining read-write/trap behavior. Native warning-73 checks, 2,003 tests with register-allocation validation, the module-boundary audit, formatting, interface generation, and diff checks passed.
- 2026-07-16: `f16c96a4` added explicit returns-twice call behavior and lowered try-table setup as `try_begin` followed by a returns-twice `sigsetjmp` capability. Tail and noreturn calls reject this behavior. Every current `WasmOpcode` variant now has an exhaustive semantic lowering arm, so future unclassified variants fail compilation instead of reaching a runtime fallback. Native warning-73 checks, 2,006 tests with register-allocation validation, the module-boundary audit, formatting, interface generation, and diff checks passed.
- 2026-07-16: Added the first product-owned, test-only migration normalizer. It compares legacy and semantic Wasm lowering by logical function signature, CFG shape, calls, and terminators instead of target instruction spelling, while separately snapshotting semantic call protocol, effect, trap/unwind, safepoint, and precise-root facts. The first direct-call tracer bullet exposed and normalized the legacy `num_args` convention that omits the fixed execution environment. Native warning-73 checks, 2,008 tests with register-allocation validation, the module-boundary audit, formatting, interface generation, and diff checks passed.
- 2026-07-16: Expanded the migration normalizer across conditional edge arguments, callable-reference indirect calls with precise GC roots, explicit traps, and known non-safepoint runtime helpers. It collapses only legacy forwarding blocks beyond the source MilkIR block count and recovers known call targets through `LoadExternalFuncAddr`/`LoadCodeAddr` def-use chains, so comparisons operate on source-level behavior rather than union-backend encoding accidents. Native warning-73 checks, 2,012 tests with register-allocation validation, the module-boundary audit, formatting, interface generation, and diff checks passed.
- 2026-07-16: Began the private semantic-to-legacy adapter inside the legacy target-lowering package as white-box-only migration code, keeping it absent from generated interfaces until production cutover. The first direct adapter slice maps semantic parameters, results, SSA values, integer constants and binary operations, copy, jump, return, trap, and conditional edge arguments directly into verified legacy MachV without reconstructing MilkIR. Native warning-73 checks, 2,014 tests with register-allocation validation, the module-boundary audit, formatting, interface generation, and diff checks passed.
- 2026-07-16: Expanded the private adapter through nontrapping integer and floating scalar operations, comparisons, representation conversions, reference/pointer carriers, and explicit pointer loads/stores including narrow forms. Trapping division/remainder and float-to-int remain structured unsupported operations until their complete check sequences are adapted; the temporary seam does not silently rely on target hardware behavior. Native warning-73 checks, 2,017 tests with register-allocation validation, the module-boundary audit, formatting, interface generation, and diff checks passed.
- 2026-07-16: Added explicit division and remainder legalization to the private adapter. Every path emits a structured divide-by-zero trap, signed division also emits the integer-overflow trap, AMD64 remainder retains its fixed RAX/RDX/divisor constraints, and AArch64 remainder uses quotient plus multiply-subtract semantics. Float-to-int remains unsupported until its full NaN and range checks are migrated. Native warning-73 checks, 2,017 tests with register-allocation validation, the module-boundary audit, formatting, interface generation, and diff checks passed.
- 2026-07-16: Added the first complete call-ABI slice to the private adapter. Platform helpers use AAPCS64/SysV argument registers, overflow stack storage, return constraints, and foreign clobbers; internal direct and indirect calls use the embedding-supplied context/user/result layout and internal clobbers. Code-symbol conversion is injected by a resolver rather than hard-coding Wasm identities. Calls with live precise roots remain explicitly rejected until stack-slot and safepoint-ID adaptation lands. Native warning-73 checks, 2,020 tests with register-allocation validation, the module-boundary audit, formatting, interface generation, and diff checks passed.
- 2026-07-16: Adapted address-visible semantic stack objects without exposing target frame details upstream. The private adapter first computes the maximum outgoing-call overflow area, then lays out stack objects after it with their declared alignment and reserves the combined area once. Whole-function snapshots cover aligned object addresses and prove that AMD64 internal-call overflow arguments at `[sp+0]` cannot overlap an object beginning at offset 16. Native warning-73 checks, 2,022 tests with register-allocation validation, the module-boundary audit, formatting, interface generation, tracker derivation, and diff checks passed.
- 2026-07-16: Adapted the existing allocation-safepoint ABI without teaching the reusable adapter Wasm helper names. A product-supplied private predicate selects Platform GC calls that require a hidden safepoint ID; the adapter spills precise roots into the emitter-defined leading outgoing slots, emits `LoadSafepointId`, appends the hidden argument, and places semantic stack objects after the maximum temporary root/overflow area. Rootless allocations still record safepoints, while calls whose overflow arguments would overwrite live roots fail structurally. Native warning-73 checks, 2,025 tests with register-allocation validation, the module-boundary audit, formatting, interface generation, and diff checks passed.
- 2026-07-16: Added the private embedding-field adaptation seam. The embedding resolves each opaque semantic field identity to a finite pointer-load path; the adapter emits that path without interpreting product names or copying VMContext offsets. Snapshots cover both a direct field load and the multi-memory pattern `context -> memory directory -> descriptor -> base`, while an absent resolver remains a structured error. Native warning-73 checks, 2,027 tests with register-allocation validation, the module-boundary audit, formatting, interface generation, and diff checks passed.
- 2026-07-16: Adapted rootless semantic tail and noreturn control. Direct internal tails resolve a code symbol and both direct/indirect tails use the existing fixed-register and overflow-stack ABI before `ReturnCallIndirect`; overflow planning still places address-visible stack objects after the tail-call argument area. Noreturn calls emit the real call and an unreachable trap for an unexpected return. Tail calls with precise roots remain a structured gap until generic caller stackmaps exist. Native warning-73 checks, 2,031 tests with register-allocation validation, the module-boundary audit, formatting, interface generation, and diff checks passed.
- 2026-07-16: Added the product runtime half of generic caller-root preservation. Nested ordinary calls can push heap-owned copies of precise roots, GC merges every active scope, normal return pops one scope, and exception longjmp restores the marker captured by the destination handler so no copied root points into an unwound caller. Stable runtime symbols and cwasm fixup IDs expose the push/pop helpers without adding a Wasmoon dependency to reusable compiler modules. Native warning-73 checks, 2,032 tests with register-allocation validation, the module-boundary audit, formatting, interface generation, and diff checks passed.
- 2026-07-16: The milestone remains open. Wasmoon runtime-ABI adaptation, normalized legacy comparison, the private semantic-to-legacy adapter, production cutover, and deletion of the direct MilkIR-to-union entry points are not yet complete.

## Close Notes
