# ISS-237: Produce final artifacts from the live JIT compiler

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: wasmoon-jit, artifact, compiler, code-object, migration, tests, agent
- Assignee: codex
- Created: 2026-07-22
- Updated: 2026-07-22
- External ref: none

## Description

The production Wasm compiler still converts target code objects into v8 `CompiledEntry` records, losing symbolic relocation and typed metadata structure before installation and serialization.

## Design

Add one Wasmoon-owned producer that converts verified AArch64/x64 target compilation results directly into v9 `artifact.FunctionCode` records and builds a complete compatibility manifest from the validated module, compilation policy, host target, and SHA-256 module identity. Do not route through v8 adapters or negative runtime fixup indices.

## Acceptance Criteria
- [x] Live AArch64 and x64 compilation retains unlinked bytes, symbolic relocations, sources, traps, safepoints, unwind directives, frame size, entry offset, function identity, and logical signature.
- [x] The manifest records exact target/OS ABI, CPU requirements, JIT ABI, codegen revision, module identity, and compilation policy.
- [x] Runtime and internal function targets remain named symbols; no negative fixup identity is produced.
- [x] Unit tests snapshot complete produced artifacts and verify encode/decode plus live verification parity.

## Relationships
- Depends on: ISS-235
- Parent: ISS-236
- Related: ISS-233, ISS-234
- Discovered from: ISS-236

## Notes
- 2026-07-22: Split from ISS-236 after inventory showed that production target adapters still flatten final code objects into v8 patch tables.
- 2026-07-22: The final producer now converts target compilation results directly from verified `UnlinkedCodeObject` values. Direct calls to imports retain their canonical `Code("wasm.func.N")` relocation and resolve through a same-name import alias during installation; neither target adapter nor a negative fixup index participates.
- 2026-07-22: Manifest construction hashes real source and semantic-feature bytes with SHA-256, owns JIT ABI 3 and codegen revision `machv-vcode-1`, records explicit or structurally detected host target/OS ABI, and treats AArch64 Advanced SIMD and x64 SSE2 as baseline architecture requirements rather than optional feature entries.

## Close Notes

Closed with a Wasmoon-owned live artifact producer for AArch64 and x64, canonical Wasm import/function identities and logical signatures, exact compatibility-manifest construction, and structured rejection of unsupported host identities. Complete object snapshots cover both live targets and every retained code-object metadata field; the same artifact passes live verification and bounded encode/decode verification.

Validation passed with `moon info --target native`, `moon fmt`, strict warning-73 checks for native and default targets, 2316/2316 native tests, all three compiler-boundary audits, tracker derivation, and `git diff --check`.
