# ISS-487: Compare canonical task identities when avoiding scheduler reentry

## Metadata
- Type: bug
- Status: closed
- Priority: 1
- Labels: correctness, runtime, component
- Assignee: codex
- Created: 2026-09-09
- Updated: 2026-09-09
- External ref: none

## Description
progress_one_subtask_nonblocking compares entries_subtasks identifiers directly with task_stack entries. The former exposes guest handle aliases when available; push_task stores resolved canonical task IDs. Audit the scheduler comparisons and reproduce reentry through a bound subtask before fixing the identity boundary.

## Acceptance Criteria
- [x] A regression proves that an active bound subtask is not stepped recursively.
- [x] Scheduler active-task checks resolve guest handles to canonical task IDs.
- [x] Existing async and component conformance cases pass without new exclusions.

## Relationships
- Depends on: none
- Parent: ISS-471
- Related: ISS-484
- Discovered from: ISS-484

## Notes
- 2026-09-09: A host future invokes the scheduler during its second poll. The old scheduler recursively polls the same bound subtask, producing three polls instead of two. Canonical identity comparison fixes this without depending on the unresolved scheduling-version question in ISS-484.

## Close Notes

- Canonicalize the enumerated guest handle before comparing it with the active task stack. The public scheduler regression now observes exactly two host polls and no nested progress. Native validation: 2358/2358 (`/tmp/iss487-native.log`). External component verification will be recorded after the related operand-validation build.

- External gate passed with the related validation fix: component misc 184 pass / 8 existing unsupported / 4 existing deferred, zero failures/timeouts (`tmp/iss489-component-misc/summary.json`); stable 845 commands, async 153 and future-gated 387 commands per tested engine. No new exclusions.
