# ISS-357: Require dominating component validation in the audit

## Metadata
- Type: bug
- Status: closed
- Priority: 2
- Labels: component-model, security-audit, validation, tooling, tests, agent
- Assignee: codex
- Created: 2026-07-30
- Updated: 2026-08-02
- External ref: none

## Description
The validate-before-instantiate audit compares effective-code source
positions, but does not account for nested control-flow blocks. Validation in
an unreachable or conditional block can therefore appear lexically before an
unconditional linker instantiation and make the gate pass.

## Design
Do not approximate control-flow dominance with source-text ordering. Make
successful component validation part of the instantiation boundary so an
unchecked `Component` cannot be passed to `ComponentLinker::instantiate`.
Keep the source audit as a boundary regression check, not as the source of the
safety guarantee.

## Acceptance Criteria
- [x] Validation failure cannot be swallowed before component instantiation.
- [x] Every public linker instantiation path requires evidence produced by
      successful component validation.
- [x] The security audit rejects reintroduction of an unchecked instantiation
      boundary instead of attempting to infer MoonBit control flow.
- [x] Existing component runtime callers migrate to the checked boundary.
- [x] Component tests, security audit, and Python gate tests pass.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-290, ISS-294, ISS-340, ISS-353
- Discovered from: ISS-353

## Notes
- 2026-07-30: Initial issue.

## Close Notes

Reopened on 2026-07-30. The previous lexical brace-depth check rejected known
conditional cases but did not prove that validation completed successfully on
every path reaching instantiation. In particular, a nested `catch` can consume
the `raise` that the scanner treated as propagation.

The final implementation removes source-order inference entirely.
`ValidatedComponent` is an abstract validator-owned type whose only producers
perform full validation. `ComponentLinker::instantiate` now accepts that
evidence instead of a raw component, so MoonBit type checking rejects every
unchecked call path. The facade, component-spec runner, and low-level runtime
tests now pass the validation result explicitly.

The generated-interface audit rejects a raw-component linker signature, a
missing evidence producer, or a constructible evidence struct. A public
black-box facade regression verifies that a parsed but invalid component
returns structured `ValidationFailed` before instantiation. Validation passed:
the component security audit, all 59 Python gate tests, 5 validator tests, and
all 540 tests in `Milky2018/wasmoon/testsuite`.

Reopened on 2026-08-02: review found two paths that defeated the boundary.

1. `ComponentLinker::add_component` still accepted a raw `Component`. An
   imported component is instantiated by the instance sections of whichever
   component imports it (`resolve_import` pushes the closure into
   `state.components`, and `Instantiate` runs it), so a host-registered
   component reached full instantiation without ever passing validation.
   `ComponentClosure` was also `pub(all)`, so external code could forge a
   `Component(closure)` extern around an unchecked component and hand it to
   `add_import` directly.
2. `ValidatedComponent` wrapped the same reference the caller retained.
   `Component` exposes mutable arrays (including `binary.sections`, the source
   both validation and instantiation actually read), so the evidence could be
   invalidated by mutating the raw component after validation.

Final fix: `Component::isolated_copy` rebuilds a component from a copy of its
immutable binary sections; `validate_component_for_instantiation_with_config`
validates that snapshot and the evidence holds only the snapshot, so no
reference the caller retains can reach validated content. `add_component` now
requires `ValidatedComponent`, and `ComponentClosure` is abstract, so a
closure cannot be forged. Defense in depth: the validator independently
rejects root-level component imports and requires nested component imports to
be satisfied by explicit instantiate arguments, so the linker's component
import fallback is unreachable from validated components — the hardened
boundary matters if either rule is ever relaxed. The audit's
validate-before-instantiate check now also pins the `add_component` signature
and non-constructibility of `ComponentClosure`, with negative gate tests for
both. Regression tests prove evidence survives full mutation of the raw
component and that import registration requires evidence. Validation: 1535
native + 983 wasm-gc tests, all three component spec suites (23/23, 24/24,
12/12 files), component security audit, and 21 audit gate tests all pass.
