# ISS-379: Stop silently swallowing e-class type conflicts

## Metadata
- Type: bug
- Status: closed
- Priority: 2
- Labels: milkir, egraph, correctness, agent
- Assignee: unassigned
- Created: 2026-08-03
- Updated: 2026-08-03
- External ref: none

## Description
`EGraph::set_type` (`modules/milkir/egraph/egraph.mbt:550`) handles a
conflicting bit-width assignment with
`Some(existing) => if existing == bits { () } else { () }` — both branches
are no-ops, so a conflict is dropped on the floor. `EGraph::merge` behaves
the same way structurally: the root's type wins and the other class's type
entry is removed without comparison; the rebuild finalization re-roots the
type map first-wins as well.

A 32-bit class merged with a 64-bit class is a soundness violation, and the
bit-width-driven rules (`rules_extends` alone is 1,290 lines keyed on
`get_bits`) would keep rewriting with the wrong width — silently.

The constant caches have the same hole with a sharper consequence.
`EGraph::merge` (`egraph.mbt:784`) merges `const_value`/`fconst_value` by
"prefer non-None": when **both** classes carry a constant and the values
differ — the loudest possible evidence that some rule performed an unsound
union, since it equates two distinct constants — the root's value silently
wins. `get_simplified_opcode` then rewrites every value of that class to
the surviving, possibly wrong, constant. This case must be part of the
same loud-failure mechanism, not just the width conflict.

## Design
Type conflicts must be loud. On conflicting `set_type` or a merge of two
classes with differing widths, at minimum record and surface a diagnostic
(debug assertion in test builds); ideally refuse the merge or mark the
class poisoned so width-dependent rules skip it. Decide and document which.
Add a whitebox regression asserting the chosen behavior.

## Acceptance Criteria
- [x] A conflicting `set_type` no longer silently returns; the chosen
      mechanism (assert, diagnostic, or poison) is observable in a test.
- [x] A merge of classes with differing known widths triggers the same
      mechanism.
- [x] A merge of two classes carrying differing `const_value` (or
      `fconst_value`) triggers the same mechanism, verified by a whitebox
      regression.
- [x] Existing e-graph and optimizer suites stay green.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-377
- Discovered from: none

## Notes
- 2026-08-03: Found during an e-graph audit.
- 2026-08-03: Amended after a coverage check: scope extended from the type
  map to the constant caches, whose silent conflict resolution can rewrite
  values to a wrong constant via `get_simplified_opcode`.

## Close Notes

Chosen mechanism: poison plus diagnostic counters, not abort — a constant
class legitimately serves several IR widths (the `RepKey` comment documents
multi-typed classes), so a hard assert would fire on sound inputs.

- Width conflicts (`set_type`, `merge`, `subsume`, and rebuild's re-rooting)
  poison the class: its `type_map` entry is dropped, it never regains a
  width, and `get_bits` returns `None`, so width-dependent rules skip the
  class instead of rewriting with whichever width was recorded first — the
  previous first-wins behavior was itself a latent wrong-width bug for
  shared constants. Observations are counted by `type_conflict_count`.
- Constant-cache conflicts (merge of two classes carrying different
  `const_value`/`fconst_value`, and rebuild's cache refresh finding two
  different constants in one class) clear the cache so the wrong constant
  can never be harvested by `get_const`, and are counted by
  `const_conflict_count`.

Three whitebox regressions pin the behavior (conflicting `set_type`,
conflicting-width merge surviving rebuild, conflicting-constant merge
surviving rebuild). Egraph 194/194, milkir 173/173, testsuite 562/562,
`moon check --warn-list @73` clean.
