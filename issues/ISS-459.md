# ISS-459: Rebuild MilkIR around an authoritative DFG and staged optimizer

## Metadata
- Type: epic
- Status: closed
- Priority: 0
- Labels: milkir, dfg, egraph, gvn, architecture, compile-time, performance, migration, agent
- Assignee: codex
- Created: 2026-09-03
- Updated: 2026-09-03
- External ref: none

## Description
MilkIR currently stores complete instructions directly in block layout while
the verifier and several optimization passes independently reconstruct dense
`Value`-to-definition tables. This repeats work, couples SSA definition
identity to instruction placement, and prevents the optimizer from expressing
unplaced alternative instructions directly in the canonical IR.

Replace that ownership model with an authoritative data-flow graph and a
separate instruction layout. Build the bounded aegraph optimizer on that DFG
so GVN, alternative construction, cost extraction, LICM, rematerialization,
and final placement share one value graph instead of rebuilding partial
definition models in each pass.

## Design
The next pre-1.0 minor release may make a clean source-level break to the
MilkIR interface. Do not retain the current `Block.instructions : Array[Inst]`
ownership model beside the new representation merely for compatibility.

`Function` will own a DFG containing stable `Value` and `Inst` identities and
a separate layout containing ordered `InstId` references. `ValueDef` is the
authoritative definition relation and supports function parameters, block
parameters, instruction results, and an optimizer-private `Union` state.
Instruction and value IDs remain stable for the lifetime of a function;
deletion leaves tombstones until an explicit compaction phase. Persistent use
lists are out of scope: use counts and use lists remain derived optimizer state.

The optimizer may temporarily admit typed `Union` definitions and unplaced
pure instructions, but these states are private to `OptimizerSession`. Normal
verification and every optimizer exit require a fully elaborated function.
The session stages placement and commits layout only after successful
extraction; an optimizer failure must leave the input function verifiable.

Only pure, non-trapping, single-result instructions participate in aegraph
alternatives initially. Multi-result, effectful, trapping, call, memory-write,
and control-flow instructions remain in the side-effecting skeleton. A single
central `InstEffects` classification defines memory, trapping, call, and other
observable effects. Rewrites preserve exact WebAssembly and MilkIR semantics;
fast-math and trap-timing relaxation are out of scope.

MilkIR optimization remains target-neutral. Machine addressing, immediate
folding, and instruction fusion stay in direct x64 and AArch64 lowering. O2
uses bounded deterministic candidate and recursion limits with work
proportional to reachable pure IR. O3 uses a larger bounded budget but never
unbounded saturation. Budget exhaustion may stop producing alternatives but
must not disable canonicalization, GVN, memory correctness, or verification.

The intended high-level order is:

```text
normalize
-> loop canonicalization / unrolling / induction transforms
-> one bounded aegraph + GVN + scoped elaboration
-> CFG cleanup
```

Do not run a complete O2 pipeline both before and after loop transforms.

All rewrite rules are handwritten MoonBit. Dispatch is direct and organized by
root opcode, with shared typed helpers for definition lookup, constants,
commutative operands, legality, and candidate construction. Do not add a rule
schema, generator, second opcode language, generic runtime matcher, rule-bucket
scan, or copied e-graph nodes. Mechanical duplication is preferable to owning
and compiling a second rule language; factor helpers only when they preserve a
local, readable execution path and serve multiple concrete rules.

## Acceptance Criteria
- [x] MilkIR has one authoritative DFG for value definitions and instruction data, separate from block layout.
- [x] Blocks order stable `InstId` references rather than owning duplicate complete instructions.
- [x] Builders and mutation methods maintain `ValueDef`; the verifier validates it instead of reconstructing it as the primary definition source.
- [x] Instruction and value IDs remain stable across ordinary transformations, with deletion represented safely until explicit compaction.
- [x] Optimizer-private typed `Union` values and unplaced pure instructions cannot escape a successful or failed optimization session.
- [x] Use lists, dominance, GVN scopes, alias state, memory versions, costs, and selections remain derived session state rather than permanent `Function` state.
- [x] A central effect model prevents rewrites and placement from changing observable traps, memory behavior, calls, or exact floating-point semantics.
- [x] The initial aegraph covers pure single-result instructions while multi-result and effectful instructions remain in the skeleton.
- [x] MilkIR cost and rewrite legality remain target-neutral; target-specific selection stays in x64 and AArch64 lowering.
- [x] O2 and O3 have deterministic bounded work without instruction-count cliffs or unbounded saturation.
- [x] O3 performs structural loop transforms before one full aegraph/elaboration pipeline rather than invoking O2 twice.
- [x] Every rewrite rule is handwritten MoonBit behind direct per-opcode dispatch, with no schema, generator, generic runtime matcher, or bucket scan.
- [x] Public documentation describes the source-breaking MilkIR interface migration for the next minor release.
- [x] Focused optimizer, verifier, JIT/interpreter differential, AEGIS, and representative corpus gates show preserved correctness and no material compile-time regression.

## Relationships
- Depends on: none
- Parent: none
- Related: ISS-149, ISS-458
- Discovered from: ISS-458

## Notes
- 2026-09-03: Architecture decisions were consolidated after ISS-458 removed the duplicate production e-graph IR.
- 2026-09-03: The deleted Wasmoon matcher described 276 rules and 360 guards in 690 schema lines, used a 488-line Python generator, and emitted 3,951 lines; all old rule files together were 12,897 lines. At the pinned local Wasmtime revision, Cranelift expresses 708 optimization rules in 2,353 ISLE lines plus a 569-line optimization prelude and generates a 19,269-line Rust matcher. The reusable ISLE compiler itself is approximately 8,863 Rust lines. These measurements establish that generation can reduce per-rule source size and produce direct trie dispatch, but only by accepting a substantial compiler/tooling Module and much larger generated compilation input.
- 2026-09-03: Rewrite-rule authoring was resolved in favor of handwritten MoonBit. The project accepts the additional per-rule source in exchange for direct debugging, no generator maintenance, no generated-source compilation cost, and no second rule-language seam.
- 2026-09-03: Implementation started with the authoritative DFG/Layout ownership model. Union values and broad rule migration remain later stages after the core representation and verifier are stable.
- 2026-09-03: The first implementation stage added construction-time `ValueDef` binding and a stable function instruction arena. Verification now checks the stored DFG definition against final placement instead of rebuilding definitions. Builder constant lookup, DCE, direct acyclic rewriting, and rematerialization now use authoritative O(1) definition lookup. CFG block-parameter retention updates DFG definitions through one mutation method; this fixed nine strict native failures exposed by the initial implementation.
- 2026-09-03: Stage-one validation passed 199 MilkIR tests, all 2,202 native tests with `VCODE_REGALLOC_VALIDATION=1`, warning-73 checking, generated-interface refresh, and the module-boundary audit. Three measured AEGIS-128L runs produced a 67,266 us compile median and stable 67,792-byte code, versus the recent ISS-458 record of 69,780 us and 67,792 bytes. Capture: `/tmp/wasmoon-iss459-aegis.NI8PcS`.
- 2026-09-03: Block layout writes are now localized behind `Block` mutation methods. LICM, strength reduction, CFG merging, DCE, GVN, and rematerialization no longer mutate the instruction array directly. This is the preparatory seam for replacing layout-held instruction references with stable IDs. Strict native validation remained 2,202/2,202 after the change.
- 2026-09-03: The second implementation stage replaced `Block.instructions : Array[Inst]` with a private `Array[InstId]` layout backed by the function instruction arena. Public consumers now use count/index queries or a zero-copy ID view plus arena lookup. Native selection and lowering traverse the ID view without materializing instruction arrays. A white-box regression verifies that DCE removes an ID from layout while both the deleted arena entry and surviving instruction ID remain stable. The MilkIR module documentation records the source-breaking pre-0.15 API migration.
- 2026-09-03: Stage-two validation passed 200 MilkIR tests, all 2,203 native tests with `VCODE_REGALLOC_VALIDATION=1`, warning-73 checking, generated-interface refresh, install, and the module-boundary audit. The AEGIS-128L focused gate remained code-size identical at 67,792 bytes and measured a 67,426 us compile median, +0.24% versus the 67,266 us stage-one baseline (within noise); elapsed median improved from 872 ms to 842 ms. Capture: `/tmp/wasmoon-iss459-layout-aegis.ZJvmHL`.
- 2026-09-03: A first attempt to distinguish unplaced instructions from deletions stored `Unplaced` / `Placed(block)` / `Deleted` for every arena entry and updated it on every placement. Although semantically direct, the extra per-instruction array push and placement write regressed the AEGIS compile median to 75,274 us (+11.90%). That implementation was removed rather than accepted.
- 2026-09-03: The retained deletion model allocates a lazy dense tombstone bitset only when an instruction is actually deleted. Moves do not create tombstones, and verification rejects both unplaced non-tombstone instructions and tombstones that remain in layout. The final focused AEGIS gate measured a 64,274 us compile median (-4.45% from stage one), 785 ms elapsed median, and unchanged 67,792-byte code. Capture: `/tmp/wasmoon-iss459-layout-tombstone-aegis.EQ9A0L`.
- 2026-09-03: The optimizer's legacy block callback still materialized `Array[Inst]` snapshots after layout moved to IDs, repeating copies during budget counting, pointer analysis, memory-state propagation, and block rewriting. It now passes zero-copy `ArrayView[InstId]` throughout and resolves arena entries only at use sites; the only retained `Array[Inst]` is the single bounded whole-function/prefix input required by pointer analysis. Five measured AEGIS runs reduced the compile median from 64,274 us to 61,824 us (-3.81%) with unchanged 67,792-byte code. Capture: `/tmp/wasmoon-iss459-id-view-aegis.KrB1Ry`.
- 2026-09-03: The dominance-scoped alias state is now explicitly owned by the private `OptimizerSession` as directional typed-union parents with rollback markers. A regression test locks nested-scope canonicalization and rollback. This is session-state groundwork only: the acceptance item remains open until unplaced alternatives and success/failure commit boundaries are implemented.
- 2026-09-03: A standalone union object and an extra method layer on canonical lookup/union insertion regressed the focused AEGIS compile measurement by roughly 12-23%. Those designs were removed; the two dense arrays remain inline in `OptimizerSession`, and the existing single-loop capacity growth was preserved. Ten tightly interleaved current/baseline cold runs against `6cf9da76` measured 68,035.5 us versus 67,480 us (+0.82%, within run noise), with identical 67,792-byte output. Capture: `/tmp/wasmoon-iss459-session-pairs.KuHTUe`.
- 2026-09-03: The existing central opcode semantics table was audited as the single source used by DCE, LICM, memory dependency tracking, and GVN legality. It classifies exact scalar/vector operations, trapping integer division and float-to-int conversion, memory reads/writes, calls, dialect extensions, and mutable global dependencies; no duplicate effect policy was added.
- 2026-09-03: O3 now performs mandatory normalization, structural LICM/unrolling/strength reduction, and exactly one acyclic rewrite/GVN plus final CFG-cleanup pipeline. A metrics regression test locks both the single invocation and its ordering after loop transforms. The split explicitly carries loop-change state into final DCE, preventing obsolete strength-reduction constants from surviving. Moving loop transforms before CFG cleanup also exposes the already-supported header/body/latch form for structured continue branches; the integration test now locks that optimization after JIT/interpreter differential validation, while dedicated white-box tests continue to reject multiple latches, early exits, dynamic bounds, overflow, and excessive growth without mutation. All 2,206 strict native tests pass. Ten interleaved default-O2 AEGIS runs measured 69,245.5 us versus the preceding binary's 70,595.5 us (-1.91%) with identical 67,792-byte output, confirming that the shared O2 path did not regress. Capture: `/tmp/wasmoon-iss459-o3-pairs.dSsVJQ`.
- 2026-09-03: The first staged-alternative tracer bullet migrates integer multiply-by-power-of-two to real unplaced `Inst`/`Value` nodes in the authoritative DFG. Only centrally classified pure, non-trapping opcodes may be staged. Selection commits candidates in dependency order; rejection tombstones them, and optimizer exit discards any unfinished scope. White-box tests verify commit, discard, effectful rejection, and post-session verification. The original root becomes a scoped union alias and ordinary final cleanup removes it, preserving the 67,792-byte AEGIS output.
- 2026-09-03: An eager empty candidate array on every optimizer session produced an approximately +2.14% AEGIS compile median across 20 interleaved runs and was rejected. Candidate storage is now allocated only in functions that actually stage an alternative. A fresh 20-run alternating-order comparison measured 60,173.5 us versus 59,736 us (+0.73%, within cold-run noise), with near-identical lower quartiles and unchanged output. Capture: `/tmp/wasmoon-iss459-lazy-candidate-pairs.DCOzz9`.
- 2026-09-03: All handwritten rewrites that create helper instructions now use the same staged-candidate transaction; the old direct `append_int_constant` mutation path was deleted. Extraction uses a small target-neutral IR-complexity cost and commits only strict improvements. Nested integer reassociation and nested shifts replace two pure operations with one. Multiply-by-power-of-two replaces the more complex multiply primitive with a shift. The former `x + x -> x << 1` rewrite was removed because it was equal-cost at MilkIR level and introduced a new constant; target lowering remains free to select the best machine form. Tests lock equal-cost rejection and preservation of the original add.
- 2026-09-03: Twenty alternating-order AEGIS comparisons for cost-based extraction measured 70,135.5 us versus 69,850 us (+0.41%, within cold-run noise) with identical 67,792-byte output. Capture: `/tmp/wasmoon-iss459-cost-pairs.4uPSPi`.
- 2026-09-03: Integer rewriting now uses one handwritten `IntBinaryOp` dispatch. Each opcode branch enumerates only its applicable identity, reassociation, shift-combination, or strength-reduction rules. The previous identity -> nested-shift -> nested-integer matcher chain and its repeated operand/definition checks were deleted; div/rem/mul-high operations now return directly without entering unrelated matchers. The remaining scalar root dispatch is likewise an explicit opcode match, and the repository contains no rule schema, generator, runtime matcher, or bucket scan.
- 2026-09-03: Twenty alternating-order AEGIS comparisons for direct dispatch measured 65,129 us versus 66,462.5 us (-2.01%) with identical 67,792-byte output. Capture: `/tmp/wasmoon-iss459-dispatch-pairs.pV2Thc`.
- 2026-09-03: The acyclic value graph and staged alternatives now share the central `OpcodeSemantics::can_stage_as_pure_candidate` boundary. Constants remain session-indexed; other pure single-result scalar, float, vector, and global-value instructions participate in GVN. Trapping division/conversion, memory, calls, dialect extensions, and multi-result instructions remain in the effect skeleton. A table-driven test locks both eligibility and GVN behavior.
- 2026-09-03: An initial `Inst::is_pure_single_result` convenience wrapper measured approximately +1.9% in a ten-run AEGIS comparison because MoonBit retained the extra hot call. It was removed; GVN performs its structural result-count check directly and consults the central opcode semantics once. The final ten-run comparison measured 71,251 us versus 71,886 us (-0.88%) with identical 67,792-byte output. Capture: `/tmp/wasmoon-iss459-boundary-final.cxT37c`.
- 2026-09-03: The GVN available-expression table is now dominance-scoped. Each key holds a reversible stack for the active dominator path; exiting a subtree rolls back insertions and bounded-reuse updates. Lookup therefore examines only the nearest active definition instead of scanning entries retained from prior siblings. A mutable-global stress test with 64 invalidations now records exactly 64 probes; the old retained-history algorithm would perform 2,080. `value_lookup_probes` is exported through the existing compiler performance metrics. Ten interleaved AEGIS pairs measured 66,013.5 us versus 68,643 us (-3.83%) with identical 67,792-byte output. Capture: `/tmp/wasmoon-iss459-scoped-gvn-pairs.11n9YJ`.
- 2026-09-03: Acyclic optimization work is now deterministically tiered. Handwritten dispatch and scoped GVN perform constant bounded work per reachable instruction. Precise memory analysis retains the O2 limit of 300 source instructions and uses a larger but finite O3 limit of 1,200; budget exhaustion still leaves canonicalization, pure GVN, effect versioning, and verification active. O3 continues to invoke the acyclic pipeline only once. Twenty alternating-order default-O2 AEGIS comparisons measured 63,660 us versus 64,191.5 us (-0.83%) with identical output. Capture: `/tmp/wasmoon-iss459-budget-pairs.1CnMnD`.
- 2026-09-03: The closing representative corpus gate ran three cold compiles for AEGIS-128L and the five previously identified high-wall-ratio workloads against the pre-scoped-GVN binary. All 36 executions exited successfully and every workload retained identical generated-code size. Median compile-time changes were AEGIS-128L -13.50%, `box_seed` -8.53%, `hash` -11.99%, `scalarmult2` -1.29%, `secretbox_easy` -9.55%, and `auth2` -11.31%. Current and baseline captures: `/tmp/wasmoon-iss459-representative-current.3wYXu4` and `/tmp/wasmoon-iss459-representative-baseline.CN0ebq`.

## Close Notes

Closed after replacing MilkIR's duplicated block-owned instruction representation with one authoritative DFG and stable-ID layout, rebuilding O3 as one staged bounded acyclic pipeline, migrating handwritten integer rewrites to direct opcode dispatch and transactional candidate extraction, and making GVN dominance-scoped. Strict validation passed 2,212 native tests with `VCODE_REGALLOC_VALIDATION=1`, including optimizer/verifier and JIT/interpreter differential coverage; warning-73 checking and the module-boundary audit also passed. AEGIS and the representative high-wall-ratio corpus showed no code-size or compile-time regression.
